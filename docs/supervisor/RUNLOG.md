# گزارشِ اجراهای ناظر

> یک بخش برای هر اجرا، **جدیدترین در انتها**. هرگز بخشی را پاک نکن.
> سنجه‌های ماشین‌خوان در `runs.jsonl` و نمودارهایشان در `HANDBOOK.md`.

---

## اجرای ۰ — ۲۰۲۶-۰۹-۲۳ (راه‌اندازی، دستی)

**وضعیت:** 🟡 زردِ قابلِ‌تحمل — ابزارها ساخته و آزموده شدند؛ اولین اجرای زمان‌بندی‌شده هنوز نیامده.

**چه چیزی اجرا شد**
- `inventory.py` → ۳۲ صفحه · ۱۹ آیتمِ منو · ۳۳۳ دکمه · ۳۴۲ ورودی · ۲۱۳ مسیرِ API · ۱۷۰ متدِ کلاینت · ۳۹ سرویس · ۲۶ مدل.
- `runtime_check.py` → برنامه روی SQLiteِ یک‌بارمصرف بالا آمد؛ ۶۰ endpoint صدا زده شد؛ ۳۱ صفحه در Chromiumِ واقعی باز شد؛ ۱۳۰ دکمه کلیک شد.
- `db_audit.py` → روی دیتابیسِ **محلیِ خالی** (دادهٔ واقعی در دسترس نیست).
- ۹ تستِ جدید برای خودِ قواعدِ بازرسی.

**یافته‌ها**
1. `/api/simulate-unhandled-error` عمداً ۵۰۰ می‌دهد (probeِ مانیتورینگ) — به فهرستِ «انتظارمی‌رود» اضافه شد تا هر هفته الکی گزارش نشود.
2. `/audit` یک ۴۰۴ در کنسول دارد (منبعی که بارگذاری نمی‌شود) — موردِ باز برای اجرای بعد.
3. **یافتهٔ روشیِ خودِ ابزار:** نسخهٔ اولِ سنجهٔ زنده‌بودن، «کند» را «مرده» گزارش می‌کرد و ۱۴ صفحه را به‌دروغ خراب نشان داد. با تکرار + backoff و تفکیکِ «پروسه خارج شده» از «دیر جواب می‌دهد» اصلاح شد. یک ناظرِ گرگ‌گرگ‌گو خوانده نمی‌شود.

**چه ماند**
- دسترسیِ شبکه به production بسته است ⇒ دادهٔ واقعی بازرسی نشد (موردِ ۱ در `OPEN_ITEMS.md`).

---

## اجرای ۱ — ۲۰۲۶-۰۹-۲۳ (دستی، به درخواستِ مالک)

**وضعیت:** 🟠 یک نقصِ **بالا** پیدا و رفع شد؛ بقیهٔ سامانه سبز.

### چه چیزی واقعاً اجرا شد
| گام | نتیجه |
|---|---|
| `pytest -q` | ۹۰۶ passed / ۷ skipped |
| `tsc --noEmit` | سبز |
| `jest` | ۱۰۳ passed |
| `next build` | سبز |
| `runtime_check` | ۶۰ endpoint · **۰ خطای غیرمنتظره** (۱ خطای عمدی) · ۳۱ صفحه · **۰ صفحهٔ خالی** · **۱۳۰ دکمه کلیک شد** · ۰ موردِ ازکارافتادنِ سرور |
| `db_audit` | روی **SQLiteِ محلیِ خالی** — دادهٔ واقعی در دسترس نیست |
| Drive | پوشهٔ `BankingOps-Backups` و `BankingOps-Sync` بازرسی شد |

### یافته‌ها
1. **🔴 بالا — بکاپِ دیتابیس ۳ روز اجرا نشده بود.** آخرین snapshot در Drive:
   `allin1__db-snapshot__system__scheduled__20260920-105158Z`. حفره‌های قبلی هم
   در ۱۴ و ۱۷ سپتامبر. **ریشه:** حلقهٔ زمان‌بندی مهلتش را فقط در حافظه داشت و
   هر دیپلوی/ری‌استارت صفرش می‌کرد؛ در روزهای پُردیپلوی هرگز به ۲۴ ساعت نمی‌رسید.
   **رفع شد (v136)** + ۱۰ تست.
2. **🟠 متوسط — `attachments/cust-unknown` در Drive.** پیوست‌هایی بدونِ شمارهٔ
   حساب. → موردِ ۵.
3. **🟡 کم — `/audit` یک ۴۰۴ در کنسول.** → موردِ ۶.
4. **✅ خوبِ تأییدشده:** پوشهٔ `attachments/` فعال است (پوشهٔ `cust-324480` امروز
   ساخته شد) — یعنی آپلودِ پیوست به Drive کار می‌کند.

### اقدام
- `v136` — بکاپ حالا زمانِ آخرین اجرایش را در `system_settings` نگه می‌دارد، پس
  ری‌استارت دیگر زمان‌بندی را صفر نمی‌کند.
- `run_all.sh` خروجی را با `tee` هم می‌نویسد تا پیشرفتِ اجرا دیده شود.

### چه ماند
- دسترسیِ production (موردِ ۱) — بازرسیِ دادهٔ واقعی هنوز ممکن نیست.
- موارد ۲ تا ۶ در `OPEN_ITEMS.md`.

---

## اجرای ۲ — ۲۰۲۶-۰۹-۲۶ (اولین اجرای **زمان‌بندی‌شده**، Routine)

**وضعیت:** 🟠 دو نقص رفع شد (یکی در خودِ ناظر، یکی در ایمپورت)؛ بقیهٔ سامانه سبز.

### ⚠️ اولین چیزی که دیدم: ناظر داشت به من دروغ می‌گفت

کانتینر بازیافت شده بود و **هیچ وابستگی‌ای نصب نبود**. دو ابزارِ خودِ ناظر این را
به‌جای «نمی‌توانم بسنجم»، به شکلِ خبرِ غلط گزارش کردند:

1. `inventory.py` → **`routes: 0`** (اجرای قبل: ۲۱۳). طبقِ بندِ ۱ همین دستور،
   کاهشِ هر سنجه «هشدارِ جدی، قابلیتی حذف شده؟» است — یعنی ناظر می‌خواست با
   صدای بلند اعلام کند «۲۱۳ endpoint حذف شده»، در حالی که فقط `fastapi` نصب
   نبود. `backend_routes()` هر استثنا را می‌بلعید و `[]` برمی‌گرداند؛ «صفر» و
   «نتوانستم بسنجم» در جدولِ totals یک شکل دارند.
2. `run_all.sh` → قدمِ pytest **`exit=0`** ثبت می‌کرد در حالی که خروجی
   `No module named pytest` بود. علت: `cmd | tail` کدِ خروجیِ **`tail`** را
   می‌دهد که همیشه صفر است. یعنی ابزاری که کارش گرفتنِ قرمز است، قرمز را سبز
   بایگانی می‌کرد. (اثباتِ مستقیم: با `pipefail` ⇒ `1`، بدونش ⇒ `0`.)

هر دو رفع شد: `MeasurementFailed` (صفر ≠ اندازه‌گیری‌نشده، خروجِ غیرصفر،
«⚠️ اندازه‌گیری نشد» در INVENTORY)، `set -o pipefail` روی همهٔ قدم‌های لوله‌ای،
و یک preflightِ وابستگی پیش از هر سنجش. ۹ تستِ تازه.

### چه چیزی واقعاً اجرا شد (پس از نصبِ وابستگی‌ها)
| گام | نتیجه |
|---|---|
| `inventory.py` | ۳۲ صفحه · ۱۹ منو · **۳۳۴** دکمه (۳۳۳+۱ = دکمهٔ v137) · ۳۴۲ ورودی · **۲۱۳** مسیرِ API · ۱۷۰ متدِ کلاینت · ۳۹ سرویس · ۲۶ مدل — **هیچ کاهشی نسبت به اجرای ۱** |
| `pytest -q` | ۹۳۵ passed / ۷ skipped |
| `tsc --noEmit` | سبز |
| `jest` | ۱۰۹ passed |
| `next build` | سبز |
| `runtime_check` | ۶۰ endpoint · **۰ خطای غیرمنتظره** · ۳۱ صفحه · **۰ صفحهٔ خالی** · **۱۲۴ دکمه کلیک شد** (۱ دکمهٔ مخرب عمداً رد شد) · ۰ ازکارافتادنِ سرور |
| `db_audit` | روی **SQLiteِ محلی** — `is_local_placeholder: true`، **دادهٔ production بازرسی نشد** |
| `/api/crm/data-quality` | پاسخ می‌دهد (۲۰۰، ساختارِ کامل) — ولی عددش از دادهٔ نمونهٔ محلی است و **بی‌معناست** |
| Drive | بکاپ‌ها + `attachments/` بازرسی شد |

### یافته‌ها
1. **🟠 متوسط — ایمپورتی که هیچ حسابی را تأیید نمی‌کند، فایلش را در Drive یتیم
   می‌گذارد.** ریشه در `imports.py`: رکوردِ پیوست داخلِ `for r in saved:` نوشته
   می‌شود؛ وقتی `saved` خالی است آن حلقه اجرا نمی‌شود، ولی آپلود به Drive
   (زیرِ `cust-unknown`) انجام شده. فایل گم نمی‌شود — ولی **پیدا هم نمی‌شود**،
   که برای کارمندی که دنبالش می‌گردد یکی است. **رفع شد.**
2. **🟢 رفع شد — ۴۰۴ی که به‌نامِ `/audit` ثبت شده بود، مالِ `/audit` نبود.**
   کلِ برنامه **favicon نداشت**؛ مرورگر روی **هر** صفحه `/favicon.ico` می‌خواهد.
   ناظر فقط روی `/audit` گیرش انداخته بود. (تأییدِ مستقیم: `GET /favicon.ico` ⇒ ۴۰۴.)
3. **✅ تأییدِ v136 در production:** snapshotهای Drive در ۲۳، ۲۴ و ۲۵ سپتامبر —
   سه روزِ پیاپی، بعد از حفرهٔ سه‌روزهٔ ۲۰ تا ۲۳. بکاپِ روزانه واقعاً برگشته.
4. **🟡 اصلاحِ یک هشدارِ الکی پیش از آنکه ساخته شود:** قاعدهٔ تازهٔ «پیوستِ یتیم»
   اولش `cust-general` را هم یتیم می‌شمرد؛ ولی پیوستِ **نامهٔ عمومی** عمداً حساب
   ندارد و از راهِ `fac-LTR-<id>` کاملاً قابلِ‌دسترس است. در Drive ده‌ها از این‌ها
   هست — قاعده اصلاح شد به «از هیچ‌جا قابلِ‌دسترس نیست»، نه «حساب ندارد».
5. **🟡 موردِ ۷ (تازه):** هر اجرای زمان‌بندی‌شده روی کانتینرِ خالی شروع می‌شود و
   ~۱۰ دقیقه صرفِ نصبِ وابستگی‌ها می‌کند.

### اقدام (v137)
- ناظر: `MeasurementFailed` + `pipefail` + preflight + ثبتِ **URLِ** پاسخ‌های ≥۴۰۰
  (قبلاً فقط پیامِ عمومیِ کروم ثبت می‌شد، پس یافته‌ای بود که نمی‌شد رویش کار کرد)
  + `runtime_check.py /audit` برای بازآزماییِ یک صفحه به‌جای جاروبِ ۲۰دقیقه‌ای.
- ایمپورت: رکوردِ گم‌شده اضافه شد (آپلود دست‌نخورده) + قاعدهٔ بازرسی + ۵ تست.
- favicon از همان تک‌منبعِ `BANK_LOGO` ساخته شد.

### چه ماند
- موردِ ۱ (دسترسیِ production) — همچنان بازرسیِ دادهٔ واقعی ممکن نیست. تا وقتی
  باز است، هر عددِ کیفیتِ داده در این گزارش **محلی** است، نه واقعی.
- موارد ۲، ۳، ۴، ۵ و ۷ در `OPEN_ITEMS.md`.
