# کارهای بازِ ناظر

> هر اجرا این فهرست را می‌خواند و به‌روز می‌کند. موردِ بسته‌شده به بخشِ «بسته‌شده»
> می‌رود (حذف نمی‌شود) تا تاریخچه بماند.

## باز

| # | تاریخِ کشف | شدت | موضوع | وضعیت |
|---|---|---|---|---|
| 1 | 2026-09-23 | بالا | **دسترسیِ شبکه به production بسته است** — ناظر فقط نسخهٔ محلی را می‌سنجد و دادهٔ واقعی را نمی‌بیند. | نیازمندِ اقدامِ مالک: مجازکردنِ میزبان در Network access، یا افزودنِ `SUPERVISOR_DATABASE_URL` فقط-خواندنی |
| 2 | 2026-09-23 | متوسط | پیوست‌هایی که روی **دیسکِ کانتینر** ذخیره شده‌اند با هر دیپلوی از بین می‌روند (`render.yaml` دیسکِ پایدار ندارد). | تصمیمِ مالک: دیسکِ پایدار، یا صفِ آپلودِ مجدد به Drive |
| 3 | 2026-09-23 | متوسط | فونت‌های **فارسی** هنوز با `local()` از دستگاهِ کاربر خوانده می‌شوند (همان کلاسِ باگی که v129 برای لاتین بست). | در انتظارِ تأییدِ مالک، چون ظاهرِ همهٔ اسناد عوض می‌شود |
| 4 | 2026-09-23 | متوسط | «آشغال و تکراری‌ها»: گسترشِ `db_cleanup` به تشخیصِ مقادیرِ غیرممکن — انتخابِ دومِ مالک، هنوز شروع نشده. | برنامه‌ریزی‌شده |
| 5 | 2026-09-23 | متوسط | **یک** فایلِ باقی‌مانده در `attachments/cust-unknown`: `…cust-unknown__fac-general--Letter No 182-1-841-2026   Date  2026-09-21…pdf` (۲۲۶KB، ۲۰۲۶-۰۹-۲۱). ریشه‌اش در v137 رفع شد، ولی این یکی **قبل** از رفع ساخته شده و هیچ رکوردی در دیتابیس ندارد. | نیازمندِ انتسابِ دستی: شمارهٔ نامه `182-1-841-2026` در نامش هست؛ با دسترسیِ production می‌شود حسابش را یافت. فایل‌های **جدید** از این پس خودکار ثبت می‌شوند. |
| 7 | 2026-09-26 | متوسط | اجرای هفتگیِ ناظر روی کانتینرِ **بازیافت‌شده** شروع می‌شود: نه وابستگیِ پایتون نصب است، نه `node_modules`، نه Playwright. هر اجرا حدود ۱۰ دقیقه صرفِ نصب می‌کند. | پیشنهاد به مالک: یک setup script برای محیط، تا ناظر مستقیم به بازرسی برسد |

## بسته‌شده

| تاریخ | موضوع | چطور بسته شد |
|---|---|---|
| 2026-09-26 | **موردِ ۶ — ۴۰۴ در کنسولِ `/audit`.** | ریشه‌اش اصلاً مربوط به `/audit` نبود: **کلِ برنامه favicon نداشت**. مرورگر روی هر صفحه `/favicon.ico` می‌خواست، ۴۰۴ می‌گرفت و همان پیامِ عمومیِ کنسول را می‌نوشت؛ ناظر فقط روی `/audit` گیرش انداخته بود. v137: آیکون از همان تک‌منبعِ `BANK_LOGO` ساخته شد (نه طراحیِ دوباره) + `apple-touch-icon` + `metadata.icons`. تأیید در مرورگرِ واقعی: خطای کنسول روی `/audit`، `/dashboard`، `/customers` از ۱ به **۰** رسید. |
| 2026-09-26 | **ریشهٔ موردِ ۵ — پیوست‌های `cust-unknown`.** | `imports.py`: وقتی ایمپورت هیچ حسابی را تأیید نمی‌کرد، فایلِ منبع به Drive می‌رفت ولی رکوردِ آن داخلِ `for r in saved:` نوشته می‌شد که اصلاً اجرا نمی‌شد — فایل در Drive بود و **هیچ‌چیز در دیتابیس از وجودش خبر نداشت**. v137: آپلود عمداً حفظ شد (از دست دادنِ منبعِ ایمپورتِ ناموفق بدتر است) و رکوردِ گم‌شده زیرِ همان نشانگرِ `unknown` اضافه شد + قاعدهٔ `db_audit` که این‌ها را می‌شمارد. ۳ تست (هر دو تستِ اصلی بدونِ اصلاح **قرمز** می‌شوند — بررسی شد). |
| 2026-09-26 | **تأییدِ v136 در production.** | snapshotهای Drive: ۲۰۲۶-۰۹-۲۳، ۲۴، ۲۵ — سه روزِ پیاپی بعد از رفع، در حالی که قبلش سه روز حفره بود (۲۰ تا ۲۳). بکاپِ روزانه واقعاً برگشته است. |
| 2026-09-23 | **بکاپِ دیتابیس ۳ روز اجرا نشده بود** — آخرین snapshot در Drive: ۲۰۲۶-۰۹-۲۰. ریشه: حلقهٔ زمان‌بندی مهلت را فقط در حافظه نگه می‌داشت و هر دیپلوی/ری‌استارت صفرش می‌کرد؛ در روزهای پُردیپلوی اصلاً فرصتِ اجرا پیدا نمی‌کرد. | v136: زمانِ آخرین snapshot در `system_settings` **ماندگار** شد؛ حلقه از «چقدر از آخرین بکاپ گذشته» تصمیم می‌گیرد نه «چقدر از بوت». تأخیرِ settle برای پرهیز از OOMِ استارتاپ حفظ شد. ۱۰ تست. |
