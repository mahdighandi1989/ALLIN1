# Audit Log

Running record of every finding, decision, change, and revert. Newest entries are
appended at the end. Format:
`[date] [type: FINDING|DECISION|CHANGE|REVERT|VERIFY] — detail + rationale`.

این سند بعد از **هر** تسک به‌روزرسانی می‌شود (قانون ۵ در `CLAUDE.md`) — append-only.

---

## 2026-07-02 — Adoption of the living-documentation workflow

- **[DECISION]** گردش‌کار مستندسازی زنده (الگوبرداری از ریپوی trading-system مالک) در این
  ریپو مستقر شد: `CLAUDE.md` (قوانین الزام‌آور + دستورات دائمی مالک)، همین
  `docs/AUDIT_LOG.md` (append-only)، و ثبت درس‌ها در `experiences/` طبق فرمت موجود.
  دستور دائمی مالک: بعد از سبزِ محلی (pytest + type-check + build) **مستقیم به `main`
  مرج شود**، بدون PR و بدون پرسش مجدد؛ مستندسازی بعد از هر تسک خودکار و بدون یادآوری.
- **[FINDING]** Baseline در شروع (کامیت `67b798d`، برابر `origin/main`):
  - Frontend: `npm run type-check` + `npm run build` → ✅ سبز (static export، ۳۰+ صفحه).
  - Backend: suite کامل pytest در حال اجرا — نتیجه در ادامه ثبت می‌شود.
- **[FINDING]** `Makefile` ریشه فقط حاوی متن placeholder فارسی
  («محتوای کامل Makefile با دستور اضافه شده») است — محتوای واقعی هرگز کامیت نشده.

## 2026-07-02 — Deep audit (کل سیستم، فرانت تا بک) + اصلاحات

- **[FINDING]** Baseline کامل شد: backend `576 passed, 7 skipped`؛ frontend
  type-check + build سبز. `backend/static` کامیت‌شده با سورس فرانت هماهنگ بود
  (فقط buildId متفاوت). اسکن secret چیزی در سورس پیدا نکرد.
- **[DECISION]** بازرسی با ۴+۲ ایجنت موازی (روترها/امنیت، سرویس‌ها + دو زیر-بازرس،
  مدل‌ها/مهاجرت/دیپلوی، فرانت‌اند) انجام شد؛ هر یافته قبل از اصلاح در کد راستی‌آزمایی شد.

### CHANGE `2aa9c8e` — استقرار گردش‌کار مستندسازی
CLAUDE.md + AUDIT_LOG + REMOVAL_CANDIDATES + Makefile واقعی؛ `cleanup_unused_files.py`
(فقط placeholder، بدون هیچ ارجاع) به `archive/quarantine/` قرنطینه شد.

### CHANGE `b2b52c2` — امنیت/RBAC (root cause: دو پیاده‌سازی موازی `get_current_user`)
- `utils/security.get_current_user` چک `is_active` نداشت (نسخه‌ی `routers/auth.py`
  داشت) → کاربر غیرفعال‌شده تا انقضای JWT دسترسی کامل داشت. اضافه شد.
- نقش viewer (فقط-خواندنی طبق سیاست صریح `tests/test_rbac.py`) می‌توانست بنویسد:
  facility status/restore، customer restore، همه‌ی mutationهای offer-letter،
  trash restore، ایمپورت اکسل/AI، stats snapshot — همگی `require_editor` گرفتند؛
  تغییر وضعیت facility و status/delete نامه‌ها audit هم می‌شوند.
- trash restore تسهیلات `cascade_restore_facility` را صدا نمی‌زد (چک‌لیست/تسک‌ها
  حذف‌شده می‌ماندند) — هم‌رفتار با restore خود روتر شد.
- لاگین JSON مدعی پذیرش `{"email"}` بود ولی فقط با username جستجو می‌کرد — حالا
  username یا email. فایل مرده‌ی `auth.py` ریشه قرنطینه شد. +۱۱ تست.

### CHANGE `<offer-widths>` — ستون‌های offer_letters (باگ production-only)
`facility_id` VARCHAR(8) بود در حالی که IDهای واقعی ۹+ کاراکترند → **هیچ نامه‌ای
هرگز نمی‌توانست به تسهیلات لینک شود** (Postgres: value too long). نرخ‌ها Numeric(5,4)
(سقف 9.9999) در حالی که API تا 100 می‌پذیرد → هر نرخ ≥۱۰٪ overflow. عرض‌ها اصلاح؛
PKهای فرزند uuid کامل؛ **db_init حالا precision عددی را هم widen می‌کند** (فقط
هم‌scale و رو به بالا). تست‌ها روی متادیتای مدل‌اند چون SQLite این‌ها را enforce
نمی‌کند — دقیقاً دلیل دیده‌نشدن باگ در CI.

### CHANGE — amortization (منطق مالی، rule 3: root cause + مسیر برگشت)
Root cause: `round(tenor_months*ppy/12)` + نرخ کاملِ هر دوره ⇒ bullet شش‌ماهه = بهره‌ی
یک سال (۲×)، ۱۸ماهه = ۲۴ ماه بهره، ۴ماههٔ فصلی = کم‌شماری. اصلاح: تجزیه به دوره‌های
کامل + stub نهایی با بهره‌ی متناسب (divmod). تاریخ‌ها تقویمی واقعی (day-clamped) شدند.
مضرب‌های کامل بیت‌به‌بیت مثل قبل. مسیر قدیمی پشت `AMORT_LEGACY_ROUNDING=1`. +۱۲ تست.

### CHANGE `13aca82` — سرویس‌ها (فلسفه‌ی conservative/review-first تثبیت شد)
- db_cleanup: طبقه‌بندی certain/probable حالا **همه‌ی ستون‌های داده** را مقایسه می‌کند
  (کمکی مرده‌ی `_conflict` دقیقاً برای همین بود) — ضامنِ متفاوت با چکِ هم‌شماره دیگر
  auto-remove نمی‌شود؛ همان قانون در `dup_status` (گاردهای ورود).
- data_merge: enrich تسهیلات fill-empty-only شد و مرده را زنده نمی‌کند (قبلاً هر
  استارتاپ/دیپلوی حذف/اصلاحِ اپراتور را برمی‌گرداند!). +۴ تست.
- customer_link.ensure_customer: مشتری soft-delete با همان account_no را restore
  می‌کند به‌جای IntegrityError (ذخیره‌ی نامه/ملک/ضامن ۵۰۰ می‌داد).
- excel_import: سقف ۵۰۰۰ ردیف حالا خطای صریح است نه truncation خاموش؛ هدر تکراری
  fail-fast (قبلاً ستون راست‌تر بی‌صدا برنده می‌شد).
- exporters: صفر ابتدای شماره‌حساب/تلفن در XLSX حفظ می‌شود؛ CSV تزریق فرمول اکسل
  را خنثی می‌کند.
- fx: تبدیل ۱:۱ ارز ناشناخته باقی است (fail-open) ولی حالا **یک‌بار به‌ازای هر ارز
  WARN** می‌شود؛ جدول خالی نرخ‌ها → پیش‌فرض‌ها با لاگ.
- telegram: `TELEGRAM_CHAT_ID` چند-آی‌دی («111,222») همه‌ی اعلان‌ها را 400 می‌کرد —
  اولین id مقصد پیش‌فرض شد (مطابق مستند config)؛ `save_prefs` دیگر شکست DB را
  نمی‌بلعد (allow-list امنیتی بی‌صدا برنمی‌گردد) و روت prefs از session درخواست
  می‌نویسد.
- دو باگ `\b` داخل رشته/کلاس کاراکتر (backspace واقعی!) در draft_extract و imports.

### CHANGE `5f8d06e` — بک‌اند (اعلان‌ها، هشدارها، ingest، سخت‌سازی)
- جدول جدید `notification_reads`: خواندن broadcast per-user شد — اولین خواننده
  دیگر زنگوله‌ی بقیه را خالی نمی‌کند (broadcastهای قدیمیِ خوانده، خوانده می‌مانند).
- expiry: اسکن حالا هشدارهای برطرف‌شده (تمدید/حذف) را deactivate می‌کند
  (`tasks_resolved`) — قبلاً تسک ALERT-* برای همیشه می‌ماند.
- doc_ingest: `account_type` فقط از پیش‌فرض retail ارتقا می‌یابد (sme کیوریت‌شده
  دیگر پاک نمی‌شود)؛ تسهیلات بی‌نوع به‌عنوان OTHER مچ می‌شود (re-import دیگر
  duplicate و دوبرابرشماری exposure نمی‌سازد)؛ آنتروپی PK از ۲ به ۸ کاراکتر hex.
- آپلودها bounded-read شدند (بدنه‌ی چندگیگی دیگر قبل از چک سایز، RAM را پر نمی‌کند)؛
  Content-Disposition دانلود Drive با RFC6266/5987؛ برچسب متریک مسیرهای unmatched
  ثابت شد (اسکن بات‌ها سری نامحدود نمی‌سازد)؛ `/api/simulate-unhandled-error`
  admin-only شد؛ gunicorn به requirements اضافه شد.

### CHANGE `068456b` — فرانت‌اند (خطرناک‌ترین: نشت داده بین حساب‌ها)
چهار فرم credit-file-retail/corporate، sanction و offer-letter هنگام بارگیری حساب
جدید state حسابِ قبلی را نگه می‌داشتند (fallback `s.X`، مپ روی ردیف‌های قبلی، جایگزینی
مشروط ماتریس وثایق/شرکا) → «ذخیره» داده‌ی مشتری A را روی مشتری B می‌نوشت و حتی
تسهیلاتِ A را آپدیت می‌کرد. همگی حالا از state تمیز reset می‌شوند. + رفع race جستجو
(الگوی صفحه‌ی audit)، جستجوی سرورساید مشتری در مودال New Facility (سقف ۱۰۰ مشتری
آخر برداشته شد)، لینک Google login با `NEXT_PUBLIC_API_URL`، هم‌ترازی نتایج import
بعد از حذف فایل، اعتبارسنجی per-field نرخ ارز، رفع bidi در ۶ صفحه، پروکسی `/api`
در nginx فرانت (مسیر داکر). `backend/static` + `frontend/out` بازساخته و کامیت شد.

### CHANGE — مسیر دیپلوی docker/alembic (مسیر Render دست‌نخورده)
mount فایل ناموجود init.sql حذف؛ alembic در compose هم best-effort؛ مهاجرت 001 از
enum بومیِ ناموجود به VARCHAR (alembic روی DB تازه همیشه می‌مرد)؛ escape کردن `%` در
env.py؛ stage «development» فرانت + `NEXT_PUBLIC_API_URL` به‌عنوان build ARG؛
`SECRET_KEY` غایب در production حالا خطای سخت است (قبلاً هر worker کلید تصادفی
خودش را می‌گرفت → 401های تصادفی و مرگ همه‌ی sessionها در هر دیپلوی).

### [FINDING — ثبت‌شده، عمداً اصلاح‌نشده در این نوبت]
- `docker-compose.prod.yml`: پورت 443 بدون TLS در nginx؛ bind-mount کد از base
  در prod هم فعال است (با read_only تضاد دارد). مسیر داکر ثانویه است — تصمیم با مالک.
- `render.yaml` مقدار `CORS_ORIGINS` را خالی می‌کند (same-origin فعلی امن است ولی
  جداکردن فرانت آن را می‌شکند)؛ `/docs` و `/openapi.json` در prod باز است
  (validate_environment_security هر بوت هشدار می‌دهد).
- pdf/workbook split در `doc_ingest` روی event loop اجرا می‌شود (CPU-bound) — برای
  فایل‌های بزرگ چند ثانیه بلاک می‌کند؛ کاندید انتقال به thread.
- `record_audit(db=…)` تراکنش caller را commit می‌کند — امروز همه‌ی call-siteها بعد
  از commit خودشان صدا می‌زنند (بازرسی شد، باگ زنده نیست) ولی الگوی خطرناکی است.
- `customers.account_no` روی DBهای قدیمی/heal-شده unique-constraint واقعی ندارد
  (db_init ایندکس non-unique می‌سازد) — نیازمند پاکسازی داده قبل از افزودن قید.
- **[VERIFY — نهایی، قبل از مرج به main]** backend: `607 passed, 7 skipped`
  (baseline: 576 → +۳۱ تست جدید، صفر شکست)؛ frontend: `npm run type-check` +
  `npm run build` سبز؛ `backend/static` از build تازه سینک و کامیت شده.
  طبق دستور دائمی مالک، مرج مستقیم به `main` (بدون PR) انجام شد — Render
  خودکار دیپلوی می‌کند.

## 2026-07-03 — Offer Letter هوشمندسازی (درخواست مالک) + کاتالوگ نوع تسهیلات + ضامن‌ها

- **[OWNER REPORT]** فرم Offer Letter هوشمند نیست: (۱) برای هر قالب همه‌ی فیلدها نمایش
  داده می‌شوند حتی نامرتبط‌ها؛ (۲) عنوان فیلدها گیج‌کننده است؛ (۳) فیلد «نوع تسهیلات» باید
  هم لیست انتخابی باشد هم تایپ آزاد، و نوع کاملاً جدید باید خودش در دیتابیس جا باز کند و
  به لیست اضافه شود؛ (۴) بخش ضامن‌ها که در نمونه‌ی امضاشده‌ی بانک هست در فرم جایی ندارد؛
  (۵) نمونه‌ی docx پیوست کامل پوشش داده نشده (یادداشت‌های زیر جدول مدارک).
- **[CHANGE — فرم هوشمند]** `frontend/src/app/offer-letter/page.tsx`: فیلدها بر اساس قالبِ
  مؤثر گروه‌بندی و شرطی شدند — گروه مشترک (گیرنده/شعبه/سریال/نوع تسهیلات)، گروه English
  (RequestDate/CreditLimit/InterestRate/ValidUntil/ProcessingFee/AccountSuffix/Remarks/
  RequiredSecurities) فقط در قالب English، و گروه وام (SubjectDate/LoanAmount/…/LienAmount/
  NotesPersonal/ضامن‌ها) فقط در قالب دوزبانه. لیبل‌ها فارسی-اول با زیرنویس انگلیسی و اشاره
  به این‌که هر فیلد کجای نامه چاپ می‌شود.
- **[BUG FIXED ضمنی]** ورودی `RequestDate` اصلاً وجود نداشت درحالی‌که متن صفحه‌ی ۱ قالب
  English به آن ارجاع می‌دهد («letter Dated: …») — هرگز قابل پر کردن نبود. اضافه شد.
  همچنین `ProcessingFee`/`AccountSuffix`/`RequestDate` هنگام بارگیریِ snapshot ذخیره‌شده
  restore نمی‌شدند — اضافه شدند.
- **[CHANGE — کاتالوگ نوع تسهیلات]** بک‌اند `routers/crm.py`: `GET/POST /api/crm/facility-types`
  — built-in ها + موارد سفارشی در `SystemSetting("custom_facility_types")`. افزودن با گارد
  مشابهت نام (نرمال‌سازی حروف/فاصله/علائم + difflib ratio ≥0.9): مشابه ⇒ match بدون درج،
  جدید ⇒ درج و از این به بعد در لیست. فرانت: combobox (input + datalist) و ثبت خودکار نوع
  جدید هنگام «ذخیره».
- **[CHANGE — ضامن‌ها]** پاسخ `offer-letter-data` حالا `Guarantors` (نام + حساب، بدون تکرار)
  می‌دهد؛ بخش ضامن‌ها در فرم (قالب وام): ردیف‌های قابل‌ویرایش، prefill از پرونده، درج در بند ۷
  «مدارک موردنیاز» دقیقاً مثل نمونه‌ی پرشده‌ی بانک («… borrower(s) / -Mr. NAME- A/C NO.…»)،
  و هنگام ذخیره upsert به رکوردهای ضامن مشتری.
- **[BUG FIXED — upsert ضامن]** `add_guarantor` فقط با `cheque_no` مطابقت می‌داد ⇒ ضامنِ
  بدون چک (مسیر Offer Letter) در هر ذخیره رکورد تکراری می‌ساخت. مطابقت نام (case-insensitive
  + حساب در صورت وجود) اضافه شد؛ و ارسال cheque_no خالی دیگر چکِ ذخیره‌شده را پاک نمی‌کند
  (مطابق قرارداد مستند خود endpoint).
- **[BUG FIXED — بوت SQLite]** `app/database.py` پارامترهای pool (`pool_size`/`max_overflow`/
  `pool_recycle`) را بدون شرط پاس می‌داد ⇒ بوتِ dev با `DATABASE_URL=sqlite+aiosqlite:…`
  (مسیر مستندشده) TypeError می‌داد. حالا فقط برای غیر-SQLite ارسال می‌شوند. (در وریفای E2E
  همین تسک کشف شد.)
- **[CHANGE — پوشش نمونه]** یادداشت‌های زیر جدول مدارک (`NotesPersonal`) قابل‌ویرایش شد با
  متن پیش‌فرض عینِ نمونه (Note 1: balance confirmation، Note 2: تسویه‌ی وام قبلی)؛ دکمه‌ی
  «محاسبه» قسط ماهانه با فرمول مانده‌ی نزولی — خروجی برای نمونه‌ی ۸۰٬۰۰۰/۱۲٪/۴۸ماه دقیقاً
  `2,106/71` مطابق فرم بانک. گزینه‌های قالب فارسیِ شفاف + `dir="rtl"` (قانون bidi).
- **[VERIFY]** تست‌های جدید: `test_facility_type_catalog.py` (۴) + `test_offer_letter_guarantors.py`
  (۳)؛ E2E واقعی با Playwright روی بیلد سرو-شده: بارگیری حساب، prefill ضامن‌ها (۲ ردیف)،
  EMI=2,106/71، combobox با ۱۰ گزینه، بند ۷ با نام ضامن‌ها، فیلدهای English در قالب وام مخفی
  (و برعکس)، ذخیره → facility-types 200 + دو upsert ضامن 200 بدون تکرار. suite کامل + build
  نتیجه‌اش پایین‌تر ثبت می‌شود.
- **[VERIFY — نهایی، قبل از مرج به main]** backend: `614 passed, 7 skipped`
  (+۷ تست جدید نسبت به ۶۰۷؛ صفر شکست — run کامل بعد از همه‌ی تغییرات از جمله
  fix انجین SQLite)؛ frontend: type-check + build سبز؛ `backend/static` و
  `frontend/out` از همین سورس بازساخته و کامیت شدند. E2E مرورگر روی بیلد
  سرو-شده انجام و اسکرین‌شات‌ها برای مالک ارسال شد. مرج مستقیم به `main`.

## 2026-07-04 — دستیارِ هوشمندِ نامه (AI Letter Assistant) — درخواست مالک

- **[OWNER REQUEST]** ابزاری در بخش «نامه‌ها» که با مدل‌های هوش مصنوعیِ فعال (لیست‌شده بر اساس
  اولویت و قابلِ انتخاب توسط کاربر) روی متنِ نامه با تسلط به دیتابیس و همه‌ی فیلدها کار کند:
  اصلاح املا/نگارش، چینش پاراگراف، ظاهر جداول، یافتن مغایرت‌ها، حرفه‌ای‌سازی، و اعتبارسنجیِ
  موردِ انتخاب‌شده با دیتابیس. حتماً پیش از اعمال، فهرست پیشنهادها با تیک نمایش داده شود و فقط
  موارد تیک‌خورده اعمال شوند. «با دقت و وسواسِ زیاد» چون قرار است به بخش‌های دیگر هم سرایت کند.
- **[DECISION — فلسفه: review-first، سرور ground truth]** مدل فقط «پیشنهاد» می‌دهد؛ هیچ‌چیز
  خودکار اعمال نمی‌شود و بک‌اند هیچ رکوردی را نمی‌نویسد. یک گیتِ اعتبارسنجیِ قطعی سمتِ سرور
  تصمیم می‌گیرد کدام پیشنهاد امن است؛ اعمال سمتِ کلاینت و فقط روی state نامه، و نامه صرفاً از
  مسیرِ Save موجود ذخیره می‌شود. این با ADR-001 و فلسفه‌ی محافظه‌کاریِ پروژه هم‌راستاست.
- **[CHANGE — بک‌اند]** روترِ جدید `routers/letter_ai.py` روی `/api/letter-ai`:
  - `GET /models`: مدل‌های فعال+پیکربندی‌شده به‌ترتیبِ اولویت (برای انتخابِ کاربر) + کاتالوگِ ابزارها.
  - `POST /analyze`: حقایقِ پایگاه‌داده‌ی حساب (مشتری/تسهیلات/ضامن‌ها/برشی از پروفایل) را جمع می‌کند،
    مدلِ انتخابی/خودکار را با قراردادِ خروجیِ JSONِ سخت اجرا می‌کند، و فقط تغییراتی را برمی‌گرداند که
    از اعتبارسنجی عبور کنند. read-only مطلق؛ gated به `require_editor` و audit می‌شود.
  - سرویسِ `services/letter_assistant.py`: ساختِ prompt + parse مقاوم + **گیتِ ضدِ توهم** —
    یک `text_replace` فقط وقتی می‌ماند که `find` عیناً (یا با نرمال‌سازیِ فاصله/نیم‌فاصله) در متنِ
    فعلیِ همان فیلد وجود داشته باشد؛ `set_field` فقط برای فیلدهای کوتاهِ allow-list (نه body)؛ `note`
    صرفاً مشاوره‌ای. سقفِ ۶۰ تغییر؛ category/severity سنجیده می‌شوند.
  - `ai_manager.list_usable()` اضافه شد (همه‌ی مدل‌های فعال+کلیددارِ قابلِ استفاده، بهترین-اول) —
    برخلافِ `capable_models` که فقط doc/vision می‌داد.
- **[CHANGE — فرانت‌اند]** در `app/letter/page.tsx` دکمهٔ «دستیارِ هوشمند» + یک مودالِ کامل RTL:
  انتخابِ مدل (پیش‌فرض خودکار/اولویت‌دار)، چک‌باکسِ ابزارها، ثبتِ متنِ انتخاب‌شده (برای اعتبارسنجی)،
  دستورِ اختصاصی. پس از «بررسیِ نامه»: فهرستِ پیشنهادها با badgeِ دسته/شدت و diffِ before→after؛
  تیک/برداشتنِ تیک؛ «اعمالِ N موردِ انتخاب‌شده». اعمالِ سمتِ کلاینت **جراحی‌وار و امن**:
  `applyTextReplaceHtml` فقط محتوای یک TEXT NODE را بازنویسی می‌کند (تگ‌ها/جداول/بولد سالم می‌مانند)
  و متنِ جایگزین به‌صورتِ literal درج می‌شود (نه HTML)؛ `set_field` روی فیلدهای rich با escape.
  موردی که در متنِ فعلی پیدا نشود رد و گزارش می‌شود. `lib/api.ts`: `letterAiApi` + تایپ‌ها.
- **[VERIFY]** تست‌های جدید: `test_letter_assistant.py` (۱۲ — گیتِ اعتبارسنجی/توهم، html_to_text،
  build_facts/prompt) + `test_letter_ai_endpoints.py` (۴ — models، analyze با مدلِ mock، فیلترِ
  توهم، مسیرِ friendlyِ no_model). E2E واقعی با مرورگر روی بیلدِ سرو-شده (مدل stub، بدونِ کلید):
  بارگیریِ حساب، تایپِ متن، ۷ ابزار، ۳ پیشنهاد (۱ توهمی توسطِ گیت حذف شد)، اعمالِ ۲ موردِ
  applicable (نه noteِ مشاوره‌ای) روی body و subject، toast و به‌روزشدنِ فهرست. suite کامل + build
  سبز (نتیجهٔ نهایی پایین‌تر).
- **[NEXT/EXTENSIBLE]** همین الگو (پیشنهاد→گیت→بازبینیِ تیک‌دار→اعمالِ جراحی‌وار) قابلِ سرایت به
  offer-letter/sanction/credit-file است؛ عمداً ابتدا فقط روی «نامه‌ها» پیاده شد تا با دقت تثبیت شود.

### 2026-07-04 — پیگیریِ دستیارِ نامه: زیرساختِ چند-انتخابیِ اعتبارسنجی (بازخوردِ مالک)

- **[OWNER FEEDBACK]** «اعتبارسنجیِ موردِ انتخاب‌شده» چطور می‌فهمد کدام مورد انتخاب شده؟ فقط تیک‌زدن
  که کانکشنی ندارد؛ و از متن هم‌زمان نمی‌شود چند قسمت انتخاب کرد. زیرساختش را درست فراهم کن.
- **[ROOT CAUSE]** نسخهٔ اول فقط `window.getSelection()` را لحظهٔ باز شدنِ مودال می‌گرفت: تک-انتخاب،
  و چون مودال یک overlayِ تمام‌صفحهٔ بلاک‌کننده بود، بعد از باز شدن اصلاً نمی‌شد در نامه چیزی انتخاب کرد.
- **[CHANGE — پنلِ کناریِ غیرمسدودکننده]** مودال به یک **side-panelِ راست‌چسب** تبدیل شد
  (`.lai-panelwrap` با `pointer-events:none`، فقط خودِ پنل `auto`) تا نامه هنگام باز بودنِ پنل کاملاً
  قابلِ تعامل/انتخاب بماند.
- **[CHANGE — جمع‌آوریِ چند انتخاب]** دکمهٔ ✦ به نوارِ شناورِ قالب‌بندی اضافه شد: کاربر هر عبارت را در
  متن انتخاب و با ✦ به فهرست می‌افزاید (`addAiSelection`، dedup + سقفِ ۱۲). فهرست به‌صورتِ chipهای
  قابلِ حذف در پنل زنده نمایش داده می‌شود («پاک‌کردنِ همه» + حذفِ تکی)؛ افزودنِ هر مورد ابزارِ
  «اعتبارسنجی» را خودکار روشن می‌کند.
- **[BUG FIXED — استیتِ async]** فعال‌سازیِ خودکارِ ابزارِ validation ابتدا داخلِ updaterِ
  `setAiSelections` (که React در فازِ render و **غیرهم‌زمان** اجرا می‌کند) با یک فلگ انجام می‌شد؛
  خطِ بعدی فلگ را کهنه می‌خواند و تیک روشن نمی‌شد. به فراخوانیِ هم‌زمانِ `setAiSelTools` منتقل شد
  (تستِ E2E دقیقاً همین را گرفت).
- **[CHANGE — بک‌اند]** `analyze` حالا `selections: List[str]` می‌گیرد؛ `build_user_prompt` هر مورد را
  **جداگانه و شماره‌گذاری‌شده** به‌عنوان یک هدفِ مستقلِ اعتبارسنجی در prompt فهرست می‌کند (dedup،
  سقفِ ۱۲، سازگارِ عقب‌رو با `selection`). مدل برای هر مورد یک change جدا می‌دهد.
- **[VERIFY]** +۲ تستِ واحد (شماره‌گذاریِ چند-انتخاب + ادغامِ selectionِ legacy)؛ تستِ endpoint حالا
  selections را چک می‌کند. E2E مرورگرِ واقعی: پنل باز، انتخابِ دو عبارتِ جدا در متن با ✦، دو chip،
  روشن‌شدنِ خودکارِ ابزار، و دو یادداشتِ اعتبارسنجیِ مجزا در نتایج → **MULTISELECT_E2E: PASS**.
  suite کامل + build سبز (نتیجهٔ نهایی پایین‌تر).

### 2026-07-04 — دستیارِ نامه: استخراج و ثبتِ داده در پروفایلِ مشتری(ها) (درخواست مالک)

- **[OWNER REQUEST]** گزینه‌ای که وقتی تیک بخورد، داده‌های مفیدِ نامه را در پایگاه‌دادهٔ مشتریِ
  مرتبط ثبت کند — **با دقتِ زیاد**: یک نامه ممکن است چند مشتری را نام ببرد و برخی محتوا به مشتریِ
  اصلی ربط نداشته باشد؛ هر داده به مشتریِ درستش برود، اگر پروفایل نبود ساخته شود، تکراری ثبت نشود،
  و اگر نسخهٔ نامه به‌روزتر بود به‌روزرسانی شود؛ همهٔ تغییرات در لاگِ اصلی و تبِ لاگِ همان مشتری ثبت شود.
- **[DECISION — review-first، ADR-001، فلسفهٔ محافظه‌کارِ de-dup]** چون این مسیر در پایگاه‌دادهٔ
  واقعیِ مشتری می‌نویسد، مدل فقط **پیشنهاد** می‌دهد؛ سرور به‌صورت قطعی هر پیشنهاد را به مشتری تخصیص
  و طبقه‌بندی می‌کند و کاربر پیش از نوشتن آن را تیک می‌زند. هیچ‌چیز خودکار نوشته نمی‌شود و هیچ
  مقدارِ تاریخیِ به‌روزتر با کهنه‌تر جایگزین نمی‌شود.
- **[CHANGE — بک‌اند]**
  - ابزارِ جدید `db_extract`؛ op جدید `db_write` (account_no/customer_name/key/value). `parse_db_writes`
    آن‌ها را از JSONِ مدل جدا می‌کند (نرمال‌سازیِ key، dedup، سقفِ ۴۰).
  - سرویسِ `letter_db_extract.py` با دو فاز: `stage_db_writes` (فقط-خواندنی) هر واقعیت را به یک حساب
    تخصیص می‌دهد — **حسابِ ذکرشده در نامه → مشتریِ اصلی → تطبیقِ بی‌ابهامِ نام؛ در غیرِ این‌صورت
    UNRESOLVED و به‌صورتِ note سطح می‌آید، هرگز حدس زده نمی‌شود** — و add/update/skip را با گاردِ
    کهنگیِ تاریخ تعیین می‌کند. `apply_db_writes` (require_editor) هنگام نوشتن dedup/کهنگی را دوباره چک
    می‌کند، مشتری/پروفایل را در صورت نبود می‌سازد (کشِ per-batch تا چند واقعیت برای یک حسابِ نو روی
    PK تصادم نکنند)، زیرمجموعه‌ای امن از کلیدها را به ستون‌های ساختاری mirror می‌کند، و **هر نوشتن را
    با account_no ثبتِ audit می‌کند** (⇒ هم لاگِ اصلی، هم تبِ Logs همان پروفایل).
  - endpointها: `analyze` وقتی `db_extract` فعال است پیشنهادهای db_write را stage و به changes اضافه
    می‌کند؛ `POST /apply-db` نوشتنِ نهایی را انجام می‌دهد.
- **[CHANGE — فرانت‌اند]** کارت‌های db_write سبز و مجزا: مشتریِ مقصد + شماره‌حساب، نشانِ «ثبتِ نو/
  به‌روزرسانی»، key→value، و برچسبِ «پروفایلِ جدید ساخته می‌شود». هنگام «اعمال»، موارد db_writeِ
  تیک‌خورده به `/apply-db` می‌روند (نه روی نامه)؛ toastِ نتیجه: added/updated/new-profiles/skipped.
- **[VERIFY]** ۱۳ تستِ اختصاصی: parse/stage (چند-مشتری، unresolved، add/update/skip/staleness) و
  apply از مسیرِ HTTP (ساختِ پروفایل، dedup، کهنگی، به‌روزرسانیِ تاریخِ جدیدتر، ثبتِ audit به حساب).
  کلِ suiteِ letter-ai: ۲۶ سبز. type-check + build سبز. **[LIMITATION]** اسکرین‌شاتِ زندهٔ مرورگرِ
  همین ابزار به‌دلیلِ هنگِ متناوبِ بوتِ سنگینِ سرورِ محلی در این محیط (بی‌ربط به کد؛ startup دست‌نخورده)
  گرفته نشد؛ مسیرِ HTTP کاملاً با تست پوشش داده شده و UIِ آن همان ماشینِ apply/review است که سه بار
  زندهٔ مرورگر تأیید شد.

### 2026-07-04 — دو رفعِ باگِ گزارش‌شدهٔ مالک (دستیارِ نامه + رندرِ RTL)

- **[BUG 1 — مدل: `temperature is deprecated for this model` (400)]** مالک گزارش داد در حالتِ
  «خودکار»، بهترین مدلِ فعال (Opus 4.8) هنگام «بررسی» خطای 400 می‌داد و مجبور می‌شد مدلِ ضعیف‌ترِ
  پایین‌تر انتخاب کند. ROOT CAUSE: مدل‌های جدیدِ reasoning پارامترِ `temperature` را رد می‌کنند و
  ما آن را می‌فرستادیم.
  - **[CHANGE — `app/ai/inference.py`, به نفعِ همهٔ قابلیت‌های AI]** اگر پاسخْ 400 بود و متنِ خطا
    شاملِ «temperature»، پارامتر (top-level و nestedِ Gemini) حذف و **یک‌بار** دوباره ارسال می‌شود.
    همچنین `routers/letter_ai.py` دیگر `temperature` صریح نمی‌فرستد تا مدلِ خودکار اصلاً به این 400
    نخورد (retry به‌عنوان backstop می‌ماند). +۲ تست (`test_inference_temperature_retry.py`).
- **[BUG 2 — رندر: کلیپ‌شدنِ حرفِ اولِ کلمه در ابتدای سطرِ RTL]** مالک گزارش داد کلمهٔ «ابطال» (و
  مشابه‌ها) وقتی اولِ سطر می‌آید بخشی از حرفِ اولش «پوشانده/بریده» می‌شود. ROOT CAUSE (با بازتولیدِ
  مرورگری تأیید شد): `.bcell{overflow:hidden}` لبهٔ راستِ (شروعِ RTL) سطرهای wrap‌شده را می‌بُرد،
  به‌ویژه متنِ bold که کمی پهن‌تر است.
  - **[CHANGE — `frontend/src/app/letter/page.tsx`]** `.bcell` از `overflow:hidden` به
    `overflow:clip; overflow-clip-margin:4px` تغییر کرد — همچنان جعبه را کلیپ می‌کند (کنترلِ ارتفاعِ
    صفحه‌بندی حفظ می‌شود) ولی چند px بیرونِ لبه را paint می‌کند تا اولین گلیفِ سطر بریده نشود.
    بدونِ تغییرِ عرض/padding ⇒ صفحه‌بندی دست‌نخورده. هم در ویرایشگر هم در چاپ (کلاسِ مشترکِ `.bcell`).
- **[VERIFY]** بازتولیدِ مرورگری قبل/بعد (اسکرین‌شات به مالک)؛ type-check + build سبز؛ suiteِ کامل
  بک‌اند (نتیجه پایین‌تر). `backend/static` بازساخته و سینک شد.

## 2026-07-06 — پیوست‌های نامه + استخراجِ هوشمند از پیوست‌ها + لینکِ عمومیِ پروفایل‌ها (درخواست مالک)

- **[OWNER REQUEST]** (۱) وقتی «پیوست: دارد» است دکمهٔ آپلود پیوست فعال شود؛ فایل‌ها در Google
  Drive با نام‌گذاری دقیق/پوشهٔ مربوطه ذخیره و ذیلِ پروفایلِ مشتری ثبت شوند. (۲) در دستیارِ هوشمند
  ابزاری فعال شود که پیوست‌ها را مثلِ صفحهٔ Import — با همان راه‌حل‌های اثبات‌شده برای خواندنِ
  فایل، سقفِ حجم، کرش وسطِ کار — کامل و بدونِ خلاصه‌سازی استخراج کند: همهٔ موارد مرتبط با موضوعِ
  نامه و همهٔ حساب‌های نام‌برده؛ ثبت در پایگاه‌دادهٔ مشتریِ نامه و سایر مشتری‌های نام‌برده؛ ساختِ
  پروفایل اگر نبود؛ زیرساختِ ستون/جایگاه اگر نبود. (۳) زیرساختِ لینکِ بین پروفایل‌ها (که برای
  ضامن‌ها بود) برای همهٔ موضوعات با ذکرِ دقیقِ علت تعمیم یابد.
- **[CHANGE — پیوست‌ها]** آپلود از طریقِ همان endpointِ اثبات‌شدهٔ `crm/attachments` (Drive با نامِ
  قابل‌ردیابی در `attachments/cust-<acc>/fac-LTR-<letterId>` + fallbackِ دیسک) با scopeِ
  `facility_id=LTR-<letterId>` ⇒ خودکار ذیلِ پروفایلِ مشتری هم دیده می‌شود. جدید:
  `GET /api/letters/{id}/attachments`. فرانت: دکمهٔ «📎 پیوست‌ها» فقط وقتی «دارد» + پنلِ
  آپلود/فهرست/دانلود/حذف (نیازمندِ ذخیرهٔ نامه).
- **[CHANGE — لینکِ عمومی]** مدلِ جدید `CustomerLink` (account_no, related_account, kind, reason,
  source, source_ref) + `relationships.ensure_link` (idempotent، جهت‌آگنوستیک: زوج+kind تکراری
  نمی‌شود، علتِ متمایزِ جدید append می‌شود) + ادغام در `relationships_for_account` ⇒ لینک روی
  **هر دو** پروفایل (given/received) با علتِ دقیق دیده می‌شود. kinds: guarantor/letter/co_signer/
  family/business_partner/other.
- **[CHANGE — استخراج از پیوست‌ها]** سرویسِ `letter_attachment_extract.py` با استفادهٔ عینی از
  قطعاتِ حل‌شدهٔ Import: `pdf_chunks` استریمی + `resolve_multimodal` یک‌بار + `send_multimodal`
  به‌ازای هر قطعه با retryِ 429 و آزادسازیِ بایت‌ها؛ Excel/CSV با `workbook_to_text`+`chunk_text`؛
  docx با پارسرِ قطعی؛ سقفِ ۱۶ قطعه به‌ازای هر پیوست (فایل‌های غول‌پیکر → صفحهٔ Import).
  **پرامپتِ بازنگری‌شده**: پرامپتِ Import + زمینهٔ نامه (موضوع/مشتری/متن) + الزامِ استخراجِ کامل و
  بدونِ خلاصه‌سازی برای همهٔ حساب‌های نام‌برده + آرایهٔ `relationships` با علتِ دقیق.
  endpointِ `POST /letter-ai/extract-attachment/{id}` (هر پیوست یک درخواست — UI ترتیبی اجرا
  می‌کند؛ بایت‌ها از Drive یا دیسک). خروجی از همان گیتِ `stage_db_writes` می‌گذرد (add/update/skip،
  unresolved حدس زده نمی‌شود) + پیشنهادهای `link` (زوجِ معکوس dedup). **review-first**: هیچ‌چیز
  بدونِ تیکِ کاربر نوشته نمی‌شود. `apply-db` توسعه یافت: `links[]` ⇒ ensure_link + ساختِ stubِ
  دوطرف + auditِ روی **هر دو** پروفایل. «زیرساختِ ستونِ جدید» = کلیدهای پویا در data_json (همان
  سازوکارِ ثبت‌شدهٔ extract-to-DB).
- **[VERIFY]** +۷ تست (`test_letter_attachments_links.py`): idempotency/دوطرفه‌بودنِ لینک، apply
  با links (ساختِ stub + audit دوطرف)، آپلود/فهرستِ پیوستِ نامه، پرامپتِ زمینه‌دار، stage
  (fieldها برای هر دو حساب، dedupِ رابطهٔ معکوس، ضامن→link)، endpointِ استخراج با مدلِ mock.
  type-check + build سبز؛ suiteِ کامل پایین‌تر ثبت می‌شود.

### 2026-07-06 — بازبینیِ کاملِ درخواستیِ مالک روی قابلیتِ پیوست‌ها/استخراج/لینک: ۲ اصلاح

- **[REVIEW]** مرورِ خط‌به‌خطِ هر سه بخش + جست‌وجوی مواردی که تست‌ها نمی‌گیرند. دو باگِ واقعی
  یافت و رفع شد؛ بقیهٔ مسیرها (idempotencyِ لینک، dedupِ رابطهٔ معکوس، گاردِ توهم، upsertِ
  دوطرفهٔ audit، مسیرِ Drive/دیسک) صحیح تأیید شدند.
- **[BUG FIXED — دانلودِ پیوست با auth]** لینکِ دانلودِ پنلِ پیوست‌ها `<a href>` ساده بود که JWT
  را حمل نمی‌کند ⇒ در production (auth فعال) 401. جایگزین با الگوی موجودِ `downloadFile`
  (fetchِ احرازشده → blob).
- **[BUG FIXED — سطلِ general به‌عنوانِ مشتری]** پیوستِ نامهٔ عمومی زیر کلیدِ `general` ذخیره
  می‌شود؛ endpointِ استخراج آن را به‌عنوانِ حسابِ اصلی fallback می‌کرد ⇒ خطرِ ساختِ پروفایلِ
  جعلیِ «general» و نسبت‌دادنِ داده‌ها به آن. گارد: `general` هرگز primary نمی‌شود؛ بدونِ
  primary فقط واقعیت‌هایی که حسابشان صریحاً ذکر/تطبیق شده stage می‌شوند. +۱ تست.
- **[VERIFY]** ۸ تستِ فایلِ feature سبز؛ type-check + build سبز؛ suiteِ کامل پایین‌تر.

### 2026-07-06 — پاسخ به سه پرسشِ مالک: پوششِ فرمت، انتقالِ راه‌حل‌های Import، قوتِ پرامپت

- **[REVIEW]** مقایسهٔ سطر‌به‌سطرِ استخراجِ پیوست‌ها با صفحهٔ Import سه شکاف نشان داد؛ رفع شد:
  1. **پوششِ فرمت** هم‌سطحِ Import شد و فراتر: `image/tiff`، `image/bmp`، `image/jpg` و `.xlsm`
     اضافه شد (قبلاً فقط png/jpeg/webp/gif و xlsx/xls/csv) + پشتیبانیِ جدیدِ **متنِ ساده**
     (`.txt/.text/.log`, `text/plain`) با decodeِ تحمل‌پذیر (utf-8 → cp1256 برای متونِ legacy).
  2. **تشخیصِ فرمت**: قبلاً فقط از پسوندِ نام فایل (`mimetypes.guess_type`) — فایلِ بدونِ پسوند رد
     می‌شد. حالا fallbackِ **magic-byte** (%PDF-، PNG، JPEG، TIFF) — همان درسِ Import که
     «به اسمِ فایل اعتماد نکن».
  3. **پرامپتِ روابط قوی‌تر شد**: فقط روابطِ صریحِ سند (نه استنباطی)، `reason` باید عینِ عبارتِ
     سند (به فارسی، با ذکرِ سند/بخش) باشد چون همین متن در هر دو پروفایل ثبت می‌شود،
     انتخابِ دقیق‌ترین kind، و «در شک، حذف کن نه حدس».
- **[NOTE]** پرامپتِ پایه همان EXTRACTION_PROMPTِ اثبات‌شدهٔ Import است (تفکیکِ چند-مشتری،
  آشتی‌دادنِ تکرار/تعارض با منبعِ موثقِ جدیدتر، sanity-checkِ اعداد، فیلد-به-فیلد) + زمینهٔ نامه.
- **[VERIFY]** +۱ تست (مسیرِ متنی + قواعدِ روابط در پرامپت) → ۹ تستِ feature سبز؛ suiteِ کامل
  پایین‌تر ثبت می‌شود. تغییرات فقط بک‌اند (rebuild فرانت لازم نبود).

### 2026-07-06 — انتخابِ per-attachment برای ابزارِ استخراج (درخواست مالک)

- **[OWNER REQUEST]** بتواند انتخاب کند کدام پیوست‌ها استخراج شوند — شاید همه لازم نباشد.
- **[CHANGE — فقط فرانت]** زیرِ ابزارِ «استخراجِ کامل از پیوست‌ها» فهرستِ پیوست‌ها با چک‌باکس
  (پیش‌فرض: همه)، دکمه‌های «همه/هیچ‌کدام»، و شمارندهٔ «N از M» روی برچسبِ ابزار. `runAi` فقط
  انتخاب‌شده‌ها را می‌فرستد؛ اگر ابزار تیک خورده ولی هیچ پیوستی انتخاب نشده، پیامِ راهنما.
  بک‌اند از قبل per-attachment بود — تغییری نخواست.
- **[VERIFY]** type-check + build سبز؛ static سینک شد؛ suiteِ کامل (بدونِ تغییرِ بک‌اند) پایین‌تر.

### 2026-07-06 — ابزارِ جداول: انتخابِ per-table + تسلطِ کاملِ AI با op جدیدِ table_replace (درخواست مالک)

- **[OWNER REQUEST]** وقتی نامه چند جدول (در هر صفحه) دارد، بتواند انتخاب کند AI روی کدام‌ها کار
  کند؛ اگر «دستورِ اختصاصی» دربارهٔ جدولِ انتخاب‌شده بود، AI بتواند آن را «همه‌جوره» اصلاح/ارتقا
  دهد (نه فقط محتوایی — ساختار، چیدمان، ستون/سطر، …)؛ بخش‌های غیرجدولیِ همان دستور هم پوشش
  داده شود؛ و اگر جدول انتخاب شده ولی دستوری درباره‌اش نیست، همان رفتارِ پیش‌فرضِ قبلی بماند.
- **[CHANGE — op جدیدِ `table_replace`]** مدل می‌تواند HTML کاملِ نسخهٔ بازطراحی‌شدهٔ یکی از
  جدول‌های «انتخاب‌شده» را برگرداند. ایمنی: **sanitizerِ whitelistیِ سمتِ سرور** (فقط تگ‌های جدول +
  تأکیدِ ساده؛ فقط colspan/rowspan و styleهای امنِ محدود؛ script/handler/link/img و محتوایشان حذف؛
  HTML غیرجدولی یا بدونِ سطر ⇒ رد) + اعتبارسنجیِ table_index در محدودهٔ جدول‌های واقعاً ارسال‌شده.
  باگِ حین توسعه: تگِ voidِ ممنوع (img بدونِ تگِ بسته) حالتِ skip را قفل می‌کرد و بقیهٔ سلول را
  می‌بلعید — رفع و با تست قفل شد.
- **[CHANGE — فرانت]** با تیکِ ابزارِ جداول، فهرستِ جدول‌های نامه (شناسایی با uidِ پایدارِ سطرِ اول؛
  برچسبِ «جدول N — سطر×ستون + سرآغاز») با چک‌باکس می‌آید (پیش‌فرض همه + «همه/هیچ‌کدام»).
  فقط انتخاب‌شده‌ها (HTML کامل) به analyze می‌روند؛ نگاشتِ index→uid نگه داشته می‌شود و applyِ
  table_replace دقیقاً همان جدول را در body جایگزین می‌کند (normalizeTables برای data-rهای نو).
  کارتِ نتیجه **پیش‌نمایشِ زندهٔ** جدولِ بازطراحی‌شده را نشان می‌دهد (HTMLِ sanitized).
- **[CHANGE — پرامپت]** قواعدِ صریح: با دستورِ جدولی ⇒ بازطراحیِ همه‌جانبه ولی بدونِ تغییرِ
  داده‌های واقعی مگر صریحاً خواسته شود؛ بخش‌های غیرجدولیِ دستور با opهای معمول پاسخ داده شوند
  («هیچ بخشی بی‌پاسخ نماند»)؛ بدونِ دستورِ جدولی ⇒ table_replace ممنوع و رفتارِ پیش‌فرض (note +
  اصلاحِ جزئیِ text_replace).
- **[VERIFY]** +۴ تست (sanitizer whitelist/رد، اعتبارسنجیِ محدوده و tables_count=0، قواعدِ پرامپت)
  → ۲۳ تستِ letter-ai سبز؛ type-check + build سبز؛ suiteِ کامل پایین‌تر.

### 2026-07-06 — سه‌گانهٔ جداول: رفعِ باگِ صفحه‌بندی + پرکردنِ جدول از پایگاه‌داده + «جدول‌های پیوست» (درخواست مالک)

- **[OWNER REQUEST]** ۱) با ساختِ جدول، بلوکِ پایانی (امضاکننده/رونوشت/اقدام) با وجودِ جای خالیِ
  فراوان به صفحهٔ بعد پرتاب می‌شد؛ ۲) وقتی «دستورِ اختصاصی» پرکردنِ جدولِ انتخاب‌شده را می‌خواهد،
  AI باید با پایگاه‌داده تطبیق دهد (موجود ⇒ دقیق پر کند، ناموجود ⇒ اعلام)؛ ۳) امکانِ ثبتِ جدول
  به‌عنوانِ **پیوستِ نامه**: صفحه(های) مجزا بعد از صفحهٔ آخر، اندازه‌گیریِ هوشمند با چرخشِ خودکار
  به landscape بدونِ بیرون‌زدگی، عنوانِ خوانا و قابل‌ویرایش، عنوانِ اختیاری برای جدول‌های معمولی،
  انتخاب‌پذیری در دستیارِ هوشمند، سلول‌های قابل‌ویرایش بدونِ متنِ چرخیده، و سربرگِ سالم.
- **[ROOT CAUSE — باگِ صفحه‌بندی]** بازتولید با harness واقعی در Chromium: منطقِ `distribute()`
  سالم بود؛ مشکل، **پرتگاهِ چکِ پایانی** بود — عبورِ محتوا از `closingTop-15px` حتی به‌اندازهٔ
  چند پیکسل، و حتی توسطِ **بلوک‌های خالیِ نامرئیِ انتهایی** (`<div><br></div>` که insertTable
  همیشه اضافه می‌کند)، کلِ بلوکِ پایانی را به صفحهٔ نو می‌فرستاد. جدول‌ها محتوا را پله‌ای (~۳۰px)
  بزرگ می‌کنند و دقیقاً همین پرتگاه را فعال می‌کردند.
- **[FIX — سه‌مرحله‌ای، طراحی حفظ شد]** (الف) بلوک‌های خالیِ انتهایی در چکِ برخورد نادیده
  گرفته می‌شوند؛ (ب) در برخوردِ کوچک، بلوکِ پایانی **گروهی به پایین سُر می‌خورد** (سقف: خطِ
  شماره‌صفحه/فوتر؛ ارتفاعِ واقعیِ هر فیلدِ پایانی با عرض/فونتِ خودش اندازه‌گیری می‌شود چون
  رونوشت/اقدام چندخطی می‌شوند)؛ (ج) فقط وقتی هندسه واقعاً جا ندارد صفحهٔ انتهایی ساخته می‌شود.
  در حالتِ «چیدمان» جابه‌جایی اعمال نمی‌شود تا drag گیج‌کننده نشود. هر ۴ سناریو در harness
  تأیید شد (۲ موردِ باگِ گزارش‌شده حل؛ صفحهٔ واقعاً پر همچنان درست push می‌شود).
- **[CHANGE — پرکردنِ جدول DB-aware (بک‌اند)]** قاعدهٔ صریح در قواعدِ جدولِ `build_user_prompt`:
  منبعِ مجازِ پرکردن فقط «حقایقِ پایگاه‌داده» است؛ هر قلم اول در حقایق جست‌وجو شود
  (customer/facilities/guarantors/profile)؛ یافت‌شده با همان عدد/تاریخ/واحد؛ ناموجود ⇒ «—» +
  noteِ فهرستِ اقلامِ غایب؛ نامهٔ بدونِ حساب ⇒ اعلام و هرگز دادهٔ ساختگی. راهنمای ابزارِ
  جداول (TOOLS) هم‌راستا شد. +۱ تستِ قفل‌کننده.
- **[FEATURE — جدول‌های پیوست (فرانت)]** دیالوگِ ساختِ جدول جایگزینِ promptها شد: ردیف/ستون +
  **عنوانِ اختیاری** (برای جدولِ معمولی به‌صورتِ بلوکِ وسط‌چینِ توپُر بالای جدول در متن) +
  چک‌باکسِ «ثبت به‌عنوانِ پیوست». جدول‌های پیوست در `values_json` (کلیدِ `attTables`، جدا از
  فیلدها) ذخیره و به‌ترتیب به‌صورتِ صفحاتِ سربرگ‌دارِ مستقل بعد از صفحهٔ آخرِ نامه رندر می‌شوند
  (نشانِ «پیوست N» + عنوانِ قابل‌ویرایش + شماره‌صفحهٔ سراسری). **اندازه‌گیریِ هوشمند**: عرضِ
  طبیعیِ جدول اندازه‌گیری و در صورتِ نیاز صفحه خودکار **landscape** می‌شود — روی صفحه فقط
  ورقِ عریض‌تر است (هیچ چرخشی؛ ویرایش کاملاً عادی) و در چاپ با `@page` نام‌دار (A4 landscape)
  بدونِ بیرون‌زدگی؛ جدولِ بلند فونتش پله‌ای کوچک می‌شود و اگر باز جا نشد نشانِ هشدار می‌گیرد؛
  کوچک‌شدنِ جدول به‌صورتِ زنده به portrait برمی‌گردد. سربرگ در landscape با لنگرِ درست
  (لوگو چپ‌بالا، نامِ بانک با همان فاصله از راست، فوتر/شماره‌صفحه متوازن و لنگرِ پایین).
  toolbarِ جدول (سطر/ستون/ادغام/…)، تغییرِ عرضِ ستون با درگ، و **table_replace**ِ دستیار همگی
  به جدول‌های پیوست هم مسیر‌یابی شدند (nextBody یافت‌نشد ⇒ جست‌وجو در attTables)؛ در فهرستِ
  انتخابِ جدول‌های دستیار با برچسبِ «جدولِ پیوست N» می‌آیند؛ پنلِ پیوست‌ها آن‌ها را کنارِ
  فایل‌ها فهرست می‌کند (با شمارهٔ صفحه و نشانِ افقی) و ساختنشان پیوست=«دارد» را خودکار می‌کند.
- **[VERIFY]** ۲۸ چکِ E2E در Chromium واقعی (سرو استاتیکِ build + mockِ API): دیالوگ، عنوان،
  صفحهٔ پیوست، portrait/landscape خودکار در هر دو جهت، ویرایشِ بدونِ چرخش، شماره‌صفحه‌ها،
  پنل، فهرستِ دستیار، اعمالِ table_replace روی جدولِ پیوست، برابریِ نمای چاپ، و ذخیره/بارگذاری
  (payload شاملِ attTables؛ بارگذاری صفحات را بازمی‌سازد) — همه سبز. type-check + build سبز؛
  static از ریشهٔ ریپو سینک شد (مارکر reflow-v17 در باندل تأیید)؛ suiteِ کاملِ بک‌اند پایین‌تر.

### 2026-07-06 — پیگیریِ مالک پس از دیپلوی: صفحهٔ شبح + ۴ قابلیتِ جدولِ جدید

- **[OWNER REPORT]** ۱) هنوز با ساختنِ جدولِ کوچک، موارد پایینی به صفحهٔ بعد می‌رفت؛ ۲) درخواستِ
  درگِ کلِ جدول برای جابه‌جایی در صفحه بدونِ به‌هم‌خوردنِ فیلدها؛ ۳) برچسبِ پیوست‌جدول‌ها به
  «جدول N پیوست» تغییر کند و شماره‌گذاری‌شان از جدول‌های متن مستقل بماند؛ ۴) دکمه‌های چینشِ
  متنِ خانه‌ها (راست/وسط/چپ) در منوی شناور؛ ۵) کشیدنِ لبهٔ بیرونیِ جدول برای کوچک/بزرگ‌کردنِ
  کلِ جدول (پیش‌فرض فیت صفحه بماند).
- **[ROOT CAUSE — صفحهٔ شبح]** فیکسِ قبلی فقط جلوی pushِ «چکِ بلوکِ پایانی» را می‌گرفت؛ اما خودِ
  `distribute()` هم وقتی صفحه نزدیکِ سقف بود، همان `<div><br></div>` نامرئیِ بعدِ جدول را به
  صفحهٔ تازه سرریز می‌کرد → صفحهٔ ۲ فقط با یک خطِ خالی + بلوکِ پایانی (بازتولیدِ دقیق در harness:
  heights=[556,29]). **فیکس:** وقتی باقی‌ماندهٔ صف فقط بلوک‌های خالی است، روی همان صفحه (over-cap،
  کلیپ‌شده و نامرئی) می‌مانند و هرگز صفحه نمی‌سازند؛ خط‌خالیِ میانی (Enter عمدی) رفتارش عوض نشده.
  در harness تأیید شد (۸بند+جدول: از ۲ صفحه به ۱).
- **[FEATURE — درگِ جدول در متن]** دکمهٔ ⠿ روی نوارِ شناورِ جدول: کشیدن، نشانگرِ بنفشِ محلِ رها
  بینِ بندها، رها = ادغامِ قطعاتِ صفحه‌شکستهٔ جدول و جابه‌جایی به همان مرز؛ ذخیره از همان مسیرِ
  ویرایشِ دستی (join صفحات → body) پس چیدمانِ فیلدها دست نمی‌خورد. جدولِ پیوست درگ نمی‌شود
  (صفحهٔ خودش را دارد؛ toast راهنما).
- **[FEATURE — چینشِ خانه‌ها]** سه دکمهٔ راست/وسط/چپ‌چین در نوارِ جدول؛ deterministic روی
  `style.textAlign` خانه‌های انتخاب‌شده (الگوی valTop/Middle/Bottom)، مسیر‌یابی body/پیوست مشترک.
- **[FEATURE — تغییرِ اندازهٔ کلِ جدول]** دستگیرهٔ لبهٔ بیرونی (سمتِ چپ در RTL): درصدِ عرضِ جدول
  با کلاسِ `tblw` + متغیرِ `--tw` (در `.measure` و `.psheet` هم اعمال می‌شود تا صفحه‌بندی و چاپ
  همان چیزی را ببینند که کاربر می‌بیند)؛ لبهٔ راست لنگر می‌ماند؛ clamp ۲۰–۱۰۰٪؛ برگشت به ≥۹۹.۵٪
  یعنی حذفِ کلاس (پیش‌فرضِ فیتِ صفحه — نامه‌های قدیمی بدونِ تغییر). محدودیتِ دانسته: بازطراحیِ
  AI (table_replace) کلاس را نگه نمی‌دارد (sanitizer کلاس‌ها را حذف می‌کند) — عرضِ سفارشی بعدِ
  بازطراحی باید دوباره کشیده شود.
- **[CHANGE — برچسب‌ها]** نشان/پنل/فهرستِ دستیار: «جدول N پیوست» (سری‌های شماره‌گذاری مستقل از
  «جدول N» متن)؛ دکمهٔ حذفِ ایموجی 🗑 (روی ویندوزِ مالک tofu می‌شد) → دکمهٔ متنیِ «حذف».
- **[VERIFY]** ۳۵ چکِ E2E در Chromium (همهٔ قبلی‌ها + برچسبِ نو، چینشِ خانه، resize با persist،
  درگ با نشانگر و جابه‌جاییِ واقعی در body و سلامتِ صفحه) سبز؛ type-check + build سبز؛ harness
  صفحهٔ شبح سبز؛ static سینک شد؛ suiteِ کاملِ بک‌اند پایین‌تر.

### 2026-07-06 — پیگیریِ سوم مالک: تشخیص‌پذیریِ دیپلوی + باگِ ریشه‌ایِ render + چهار قابلیتِ جابه‌جایی/اندازه

- **[OWNER REPORT]** ۱) resize فقط از یک لبه بود — باید از هر دو لبه؛ ۲) «سومین بار» ادعای بازگشتِ
  باگِ صفحه؛ ۳) جدولِ پیوست باید در صفحهٔ خودش (بدونِ تعدی به جاهای نامربوط) درگ شود؛
  ۴) هر دو نوع جدول وقتی کوچک‌ترند به چپ/راست هم درگ شوند.
- **[ROOT CAUSE — گزارشِ سوم]** دو عامل: (الف) مارکرِ build در هر دو دیپلویِ قبلی «reflow-v17»
  مانده بود ⇒ نه مالک و نه ما نمی‌توانستیم بفهمیم کدام نسخه در Render زنده است؛ شکلِ دقیقِ
  نامهٔ مالک (متن+جدولِ ۳×۳) روی کدِ فعلی در harness فقط ۱ صفحه است (۳۷۷px < 423) — علائمِ
  اسکرین‌شات همان «صفحهٔ شبحِ» batch قبل بود که به‌احتمالِ قریب هنوز دیپلوی نشده بود.
  (ب) در همین بررسی یک **باگِ ریشه‌ایِ واقعی** پیدا شد: `render()` صفحه‌بندی تگِ `<table>` را
  بدونِ اتربیوت بازمی‌ساخت ⇒ کلاسِ tblw و متغیرهای عرض/آفست بعدِ هر repagination از نمایش
  می‌افتاد و با اولین ویرایشِ join-محور از خودِ body هم پاک می‌شد. فیکس: تگِ بازِ جدول در
  واحدهای trow حفظ و در render بازنشانده می‌شود.
- **[HARDENING — سدِ قطعی]** بعد از distribute یک invariant صریح: «صفحهٔ انتهاییِ تماماً خالی
  از هیچ مسیری زنده نمی‌ماند» (fold به صفحهٔ قبل). مارکر → **reflow-v18** (از این پس هر batch
  مارکر عوض می‌شود). ابزارِ تشخیصِ راهِ دور: `?pdbg=1` ⇒ لاگِ کنسولِ صفحات/ارتفاع‌ها/سقف/شیفت
  و مختصاتِ بلوکِ پایانی.
- **[FEATURE — resize از هر دو لبه + آفستِ افقی]** دستگیرهٔ لبهٔ راست هم اضافه شد (لبهٔ مقابل
  لنگر می‌ماند)؛ مدلِ هندسه: `--tw` (عرض٪) + `--toff` (فاصله از راست٪) با `margin-left:auto` —
  در measure/psheet هم اعمال می‌شود. نوارِ باریکِ بالای جدول = **درگِ افقی** برای جدولِ
  کوچک‌شده (هر دو نوعِ جدول؛ تمام‌عرض پیامِ راهنما می‌دهد).
- **[FEATURE — درگِ جدولِ پیوست در صفحهٔ خودش]** گریپِ ⠿ روی جدولِ پیوست حالا آن را عمودی
  در همان صفحه جابه‌جا می‌کند (`offY` روی AttTable، clampشده به مرزهای صفحه/فوتر، در چاپ هم
  اعمال؛ در values_json ذخیره می‌شود) — به‌جای toastِ «جابه‌جا نمی‌شود».
- **[VERIFY]** ۴۳ چکِ E2E روی خودِ باندلِ دیپلوی‌شده (backend/static) سبز — شاملِ بقای هندسه
  بعد از repagination (قبلاً به ۱۰۰٪ ریست می‌شد: --tw=50.4 حفظ شد)، درگِ افقی (toff از ۲۰.۳ به
  ۳۳.۵)، درگِ عمودیِ پیوست (۱۹۷→۳۳۷ نسبت به صفحه) و persistِ offY، و سناریوی شکلِ نامهٔ
  مالک (۱ صفحه + بلوکِ پایانی روی همان صفحه). type-check/build سبز؛ suiteِ بک‌اند پایین‌تر.

### 2026-07-06 — batch 4: ارتفاعِ ردیف‌ها + تصویر در نامه (درج/اندازه/کراپ/جابه‌جایی) + «پشتِ متن» وردمانند

- **[OWNER REQUEST]** ۱) کشیدنِ لبه‌های افقیِ جدول (داخلی+بیرونی) برای کم/زیادکردنِ ارتفاعِ
  ردیف‌ها؛ ۲) گزینهٔ Behind Text مثل Word برای جدول: با انتخاب، دکمه‌ای بیاید که جدول را از
  جریانِ متن جدا و آزادانه روی صفحه، زیرِ متن‌ها و فیلدها، جابه‌جا کند (و برگرداندنش)؛
  ۳) درجِ عکس در محلِ نشانگر (مثل جدول) + همان «پشتِ متن» برای عکس؛ ۴) کراپِ درجا، بزرگ/
  کوچک‌کردن از چپ/راست/بالا/پایین و جابه‌جاییِ عکس.
- **[FEATURE — ارتفاعِ ردیف]** نوارهای افقیِ درگ در مرزِ هر ردیف + لبهٔ بالاییِ جدول؛ ارتفاع
  روی `<tr>` (نقشِ min-height) با data-r در body/پیوست ذخیره می‌شود؛ measurer/چاپ خودکار
  هم‌راستا (rowHtml همان استایل را حمل می‌کند).
- **[FEATURE — تصویر]** دکمهٔ «تصویر»: فایل → downscale روی canvas (سقف ۱۴۰۰px، JPEG/PNG) →
  data-URL در پوششِ یکنواختِ `.imgcrop[data-im]` دقیقاً در محلِ نشانگر (همان الگوی caretِ
  جدول). با کلیک روی تصویر: نوارِ ابزار + ۸ دستگیره — حالتِ **اندازه** (لبه‌ها کش می‌دهند،
  گوشه‌ها متناسب) و حالتِ **کراپ** (پنجره کوچک می‌شود و marginها محتوا را لنگر می‌کنند؛
  بازنشانیِ کراپ هم هست)، چینشِ راست/وسط/چپ، درگِ ⠿ بینِ بندها (همان نشانگرِ بنفش)، حذف.
  **گاردِ حیاتی:** `reflowBody` سطرهای فقط-تصویر (بدونِ متن) را دور می‌ریخت — نامهٔ
  ذخیره‌شده با عکس هنگامِ لود عکسش را از دست می‌داد؛ در هر دو نقطه (بلوکِ ساختاری +
  ردِ سطرِ خالی) گارد شد. normalizeِ یک‌باره data-im به عکس‌های قدیمی هم می‌دهد.
- **[FEATURE — پشتِ متن (floats)]** لایهٔ `floats` در values_json ({id,kind,html,x,y,w,w0,page})؛
  رندرِ absolute به‌عنوانِ اولین فرزندِ ورق (نقاشی زیرِ همه‌چیز) در ادیتور و چاپ. دکمهٔ
  «پشتِ متن» روی نوارِ جدول (ادغامِ قطعاتِ صفحه‌شکسته و حذف از جریان) و نوارِ تصویر.
  دستگیرهٔ ⠿ همیشه‌رو: کشیدن = جابه‌جاییِ آزاد (حتی به صفحهٔ دیگرِ نامه، clamp به مرزها)؛
  کلیک = انتخاب → outline + دستگیرهٔ اندازه (لبهٔ راست لنگر) + نوار: «بازگشت به متن»
  (درج در نزدیک‌ترین مرزِ بند به موقعیتِ فعلی)، «حذف»، «بستن». جدولِ شناورِ انتخاب‌شده
  بالا می‌آید و سلول‌هایش قابلِ ویرایش است؛ تصویرِ شناور با scale نسبت به w0 رندر می‌شود.
  جمع‌کننده‌های joinِ بدنه (درگِ جدول/تصویر، unfloat) ادیتورهای float را کنار می‌گذارند تا
  محتوای شناور هرگز واردِ body نشود.
- **[LIMITATION — دانسته]** جدولِ شناور در فهرستِ جدول‌های دستیارِ هوشمند نمی‌آید (خارج از
  جریانِ متن است) و ابزارهای ساختاریِ نوارِ جدول رویش no-op هستند — برای اصلاحِ ساختاری،
  اول «بازگشت به متن». کراپِ margin در style به shorthand ادغام می‌شود (رفتارِ مرورگر).
- **[VERIFY]** ۵۸ چکِ E2E روی خودِ باندلِ دیپلوی‌شده — از جمله: persistِ ارتفاعِ ردیف، درجِ
  تصویر با پوشش، اندازه از دستگیرهٔ راست، کراپ با marginِ منفی، شناورشدن/درگِ آزاد/زیرِ
  متن بودن (DOM-first)/بازگشت به متن برای تصویر و جدول، و ذخیرهٔ floats در values_json.
  مارکر → reflow-v19. type-check/build سبز؛ suiteِ کاملِ بک‌اند پایین‌تر.

### 2026-07-06 — دانلودِ PDF نامه با ظاهرِ عیناً یکسان (درخواست مالک)

- **[OWNER REQUEST]** نامه‌ها با همان شکل/فرمتِ تهیه‌شده قابلِ دانلود باشند؛ موقعِ دانلود
  ظاهر و چیدمان به‌هم نریزد.
- **[DESIGN]** بالاترین وفاداری = رندرِ خودِ مرورگرِ کاربر (فونت‌های محلیِ B Nazanin،
  RTL/justify، جدول‌ها/تصاویر/floatها دقیقاً همان که می‌بیند): هر ورقِ نمای چاپ
  (`.psheet` — شاملِ صفحاتِ افقیِ پیوست) با `html-to-image` (SVG foreignObject → layoutِ
  واقعی، نه شبیه‌سازی) در مقیاسِ 2x به PNG و با `jsPDF` تمام‌صفحه روی A4 با جهتِ همان
  ورق (عمودی/افقی per-page) چیده می‌شود. کتابخانه‌ها lazy-load هستند (فقط با کلیک،
  chunkِ جدا — صفحهٔ نامه سنگین نشد). کلونِ ورق در hostِ off-screenِ visible رندر می‌شود
  (print-wrap خودش display:none است و مستقیم rasterize نمی‌شود). نامِ فایل از عنوان/موضوع؛
  دانلود با anchor+blob (الگوی موجودِ downloadFile) چون saveِ jsPDF نامِ غیرلاتین را
  می‌انداخت. ثبت در audit log با action=export.
- **[E2E NOTE]** headless Chromium برای هر anchor+blob نامِ «download» گزارش می‌کند (در
  تستِ ایزوله هم؛ محدودیتِ محیطِ headless) — مرورگرِ واقعی نام را از download attribute
  می‌گیرد، همان الگویی که دانلودِ پیوست‌ها الان در production با آن درست کار می‌کند.
- **[VERIFY]** +۴ چکِ E2E: PDFِ سه‌صفحه‌ای با صفحهٔ **افقیِ** واقعی (MediaBox 841.89pt)،
  ساختارِ سالمِ %PDF، تطابقِ تعدادِ صفحه با ورق‌ها، حجمِ محتوایی — مجموعِ ۶۴ چک روی
  باندلِ دیپلوی‌شده سبز. مارکر → reflow-v20. type-check/build سبز؛ suiteِ بک‌اند پایین‌تر.

### 2026-07-06 — انتخابِ فرمتِ دانلود: PDF یا Word (درخواست مالک)

- **[OWNER REQUEST]** موقعِ دانلود بشود بین PDF و Word انتخاب کرد.
- **[CHANGE]** دکمهٔ «دانلود PDF» شد «دانلود ▾» با منویِ دو گزینه: «PDF — چاپیِ دقیق» و
  «Word (.docx)». رندرِ صفحات به یک تابعِ مشترک (`renderSheetPngs`) refactor شد — همان
  موتورِ وفادارِ foreignObject در 2x. **Word:** فایلِ .docx واقعی با کتابخانهٔ `docx`
  (lazy-load): هر صفحه یک section با حاشیهٔ صفر و جهتِ خودش (صفحهٔ افقیِ پیوست ⇒
  sectionِ landscape) و تصویرِ صفحه با ابعادِ 793×1122 (یک پیکسل زیرِ A4 تا Word هرگز
  به صفحهٔ دوم سرریز نکند). نامِ فایل با anchor+blob (نامِ فارسی سالم).
- **[BUILD ISSUE]** `docx@9` (ESM) با SWCِ Next 14 کامپایل نمی‌شود («super outside a
  method») → پین به `docx@8.5.0` که با webpack سالم است.
- **[VERIFY]** ساختارِ docx با zipfileِ پایتون عمیقاً اعتبارسنجی شد: zip سالم، ۳ section،
  ۱ orient=landscape، ۳ `<w:drawing>`، ۳ فایلِ mediaی PNG (۲۰۷–۲۵۰KB) با ۳ relationship و
  Content-Typeِ درست. +۴ چکِ E2E (منو، امضای PK، document.xml، media per sheet) — مجموع
  ۶۷ چک روی باندلِ دیپلوی‌شده سبز. مارکر → reflow-v21. suiteِ بک‌اند پایین‌تر.

### 2026-07-06 — بازسازیِ خروجیِ Word: سندِ واقعیِ قابلِ ویرایش (اعتراضِ به‌حقِ مالک)

- **[OWNER FEEDBACK]** نسخهٔ قبلیِ Word صفحات را به‌صورتِ عکس می‌گذاشت — برای کاربر بی‌ارزش؛
  فلسفهٔ انتخابِ Word ویرایش‌پذیری است. اشتباهِ طراحی از من بود؛ کامل بازسازی شد.
- **[REBUILD — wordExport.ts]** ماژولِ جدیدِ lazy: **همه‌چیز متن و ساختارِ واقعیِ Word است:**
  بدنه از HTML به پاراگراف‌های RTL/justified با فونتِ B Nazanin (اجرای bold/italic/underline
  حفظ)، جدول‌ها → جدول‌های واقعیِ Word (کادر، شیدینگِ سرستون، colspan/rowspan، ارتفاعِ
  ردیف‌ها از style، چینشِ خانه‌ها، عرضِ سفارشیِ tblw به‌صورت درصد، visuallyRightToLeft)،
  تصاویرِ درون‌متنی با «پختنِ» پنجرهٔ کراپ روی canvas به ImageRunِ درون‌خطی. سربرگ
  (لوگو/نامِ بانک) به‌صورتِ تصاویرِ anchorشده در **headerِ** واقعیِ Word در همان مختصاتِ
  طراحی؛ فوترِ بانک + «صفحه X از Y» با فیلدهای بومیِ PAGE/NUMPAGES در **footer**. بلوکِ
  بالایی (بسمه‌تعالی، شماره/تاریخ/پیوست/طبقه‌بندی + گیرنده در جدولِ دوستونهٔ بی‌کادر،
  موضوع با خطِ جداکننده) و بلوکِ پایانی، همه متن. جدول‌های پیوست: سکشنِ مستقل با جهتِ
  خودش (افقی/عمودی) و عنوانِ «جدول N پیوست». اشیاء «پشتِ متن»: تصویرِ شناورِ
  behindDocument در همان مختصاتِ صفحه.
- **[VERIFY]** اعتبارسنجیِ عمیقِ خروجی با python: متنِ واقعیِ بدنه/سلولِ پیوست/امضا در
  document.xml، ۴ جدولِ w:tbl، ۳ سکشن + ۱ افقی، w:bidi فراوان، فونت ست‌شده، headerها با
  ۶ anchorِ سربرگ، فیلدهای PAGE/NUMPAGES در فوتر، و در نسخهٔ دارای float: `behindDoc="1"`
  با مبدأ صفحه. + سخت‌گیرانه‌ترین چکِ OOXML: خوش‌فرمیِ همهٔ اجزای XML، اجزای الزامی،
  resolveشدنِ همهٔ relationshipها و پوششِ Content-Typeها — هر دو فایل کاملاً معتبر.
  (LibreOfficeِ sandbox حتی txt ساده را هم لود نمی‌کند — برای رندرِ بصری قابلِ استفاده
  نبود؛ صحتِ فایل با اعتبارسنجیِ ساختاری اثبات شد.) ۶۸ چکِ E2E روی باندلِ دیپلوی‌شده
  سبز. مارکر → reflow-v22. توجه: چیدمانِ Word ذاتاً جریانی است — ظاهرِ نامه حفظ می‌شود
  ولی pixel-exact بودن فقط از PDF برمی‌آید (در UI هم همین تفکیک اعلام شده).

### 2026-07-06 — رفعِ آینه‌شدنِ چینش در خروجیِ Word (گزارشِ مالک با اسکرین‌شات)

- **[OWNER REPORT]** فایلِ Word «برعکس» بود: تاریخ 2026/07/06 به‌صورت 06/07/2026، ستون‌های
  بالای نامه آینه‌ای (شماره/تاریخ راست و گیرنده چپ — عکسِ نامه)، فاصلهٔ بعد از «:» گم‌شده.
- **[ROOT CAUSE]** (الف) پرچمِ `rightToLeft` روی تک‌تکِ runها گذاشته شده بود — در OOXML این
  یک **override** جهت است، نه اعلامِ زبان؛ روی متنِ مخلوط، بخش‌های لاتین/عددی را معکوس
  می‌کند (تاریخ برعکس، ترتیبِ سریال به‌هم). راهِ درست همان کارِ مرورگر است: RTL فقط در
  سطحِ پاراگراف (`bidirectional`) + الگوریتمِ bidi استاندارد؛ مقادیرِ صرفاً لاتین (سریال/
  تاریخ/داخلی) در **isolateِ LTR** (U+2066/2069) — آینهٔ همان spanهای dir="ltr" خودِ صفحه.
  (ب) در جدولِ دوستونهٔ بالا، ترتیبِ سلول‌ها ثابت فرض شده بود؛ حالا از چیدمانِ خودِ نامه
  (x گیرنده در برابرِ x شماره) تعیین می‌شود — سلولِ اول در جدولِ RTL سمتِ راست می‌نشیند.
  (ج) plainText فاصلهٔ انتهای برچسب‌ها را trim می‌کرد («پیوست :دارد»).
- **[CHANGE]** کارخانهٔ runِ واحد (mkRun): بدونِ overrideِ جهت + فونتِ cs و szCs/boldCs در
  همهٔ runها (قبلاً فقط sz ست می‌شد و متنِ فارسی می‌توانست اندازهٔ پیش‌فرض بگیرد)؛ مقادیر
  با ltr() ایزوله؛ ستون‌ها layout-driven؛ فاصلهٔ برچسب‌ها حفظ؛ فاصلهٔ خطوطِ بدنه ~1.5.
- **[VERIFY]** روی docxِ بازتولیدشده: صفر `<w:rtl/>`، ۷۳ `<w:bidi/>`، szCs/csFont ست،
  مقدارِ ایزوله با ترتیبِ اصلیِ «182 / 4 / ---- / 2026»، سلولِ گیرنده اول (راست).
  ۶۸ چکِ E2E روی باندلِ دیپلوی‌شده سبز. مارکر → reflow-v23. suiteِ بک‌اند پایین‌تر.

### 2026-07-07 — Word: مربع‌های LRI/PDI (مقایسهٔ کنار-همِ مالک)

- **[OWNER REPORT]** مقایسهٔ Word با نامهٔ اصلی: ستون‌ها و ترتیبِ اعداد درست شده بود، اما
  کاراکترهای ایزولهٔ U+2066/2069 (فیکسِ قبلی) در Word به‌صورتِ مربع‌های مرئیِ «LRI/PDI»
  کنارِ شماره/تاریخ/داخلی رندر می‌شدند — Word برخلافِ مرورگر این کنترل‌ها را مخفی نمی‌کند.
- **[FIX]** ایزوله‌ها → **LRM** (U+200E، نشانهٔ bidi با عرضِ صفر که خودِ Word استفاده
  می‌کند و نامرئی است). چون runها از فیکسِ قبل تمیزند (بدونِ w:rtl)، ترتیب بدونِ ایزوله هم
  درست می‌ماند و LRM فقط محافظِ لبه است. تأیید روی خروجی: U+2066/2069 صفر، LRM حاضر،
  مقدارِ محافظت‌شده «182 / 4 / ---- / 2026» با ترتیبِ اصلی، صفر w:rtl. مارکر → reflow-v24.
- **[VERIFY]** ۶۸ چکِ E2E روی باندلِ دیپلوی‌شده سبز؛ type-check/build سبز؛ suite پایین‌تر.

### 2026-07-07 — Word v25: رقم‌های فارسی در متنِ فارسی + پله‌ی عمودیِ گیرنده (پیگیریِ مالک)

- **[CONTEXT]** فایلی که مالک باز کرد هنوز خروجیِ v23 بود (مربع‌های LRI/PDI = ایزوله‌های
  نسخهٔ قبل؛ v24 هنوز دیپلوی نشده بود — بارِ سوم که تست با دیپلویِ در جریان تصادف کرد؛
  دستورالعمل: قبل از تست حتماً مارکر چک شود). دو عیبِ واقعیِ دیگر از همان اسکرین‌شات
  همین حالا رفع شد تا نسخهٔ بعد کامل باشد:
- **[FIX 1 — رقم‌ها]** Word برای رقم‌های ASCII داخلِ runهای فارسی فونتِ لاتینِ جایگزین
  برمی‌دارد (غربی/کج دیده می‌شدند؛ در نامه با B Nazanin فارسی‌شکل‌اند). در سطحِ «بلوک»
  (پاراگراف/سلول/li) اگر متن فارسی داشته باشد، رقم‌های ASCII به رقمِ واقعیِ فارسی (۰-۹)
  تبدیل می‌شوند — نمایشِ Word در هر پیکربندی عینِ نامه. مقادیرِ LTR (سریال/تاریخ/داخلی)
  دست نمی‌خورند.
- **[FIX 2 — پلهٔ گیرنده]** در نامه بلوکِ گیرنده پایین‌تر از خطِ شماره شروع می‌شود؛ در Word
  هم‌تراز بود. حالا از خودِ چیدمان (recName.y − shomareh.y) با خطوطِ خالی همان پله ساخته
  می‌شود (سقف ۴ خط).
- **[VERIFY]** روی docxِ بازتولیدشده: OOXML معتبر، بدونِ ایزوله، LRM حاضر، صفر w:rtl،
  «ستون بلندبالای شماره ۱» (رقمِ فارسی‌شده) حاضر و هیچ رقم لاتینِ باقی‌مانده در متنِ فارسی.
  ۶۸ چکِ E2E روی باندلِ دیپلوی‌شده سبز. مارکر → reflow-v25. suite پایین‌تر.

### 2026-07-07 — ساختِ پیوست با هوش مصنوعی (فایلِ واقعیِ اکسل/ورد از داده‌های پایگاه‌داده)

- **[FEATURE]** درخواستِ مالک: در پنلِ پیوست‌ها، علاوه بر بارگذاری/تحلیل، دکمه‌ای که با
  دستورِ کاربر و دسترسیِ AI به پایگاه‌داده، خودِ پیوست را بسازد — جدولِ اکسل با ستون‌ها و
  ردیف‌های خواسته‌شده، یا متنِ توضیحیِ وردی «با لحنِ نامه»؛ خروجی یک **فایلِ واقعی** که
  مثلِ پیوست‌های آپلودی ثبت شود (نه جدول-پیوستِ صفحه‌دار). و الزامِ صریح: وقتی ابزارِ
  «استخراج از پیوست‌ها → دیتابیس» تیک می‌خورد، این پیوست‌های ساختهٔ AI (که داده‌شان از
  خودِ DB آمده) **پیش‌فرض شامل نشوند** — جلوگیری از چرخهٔ نوشتنِ دوباره.
- **[DESIGN]** الگوی review-first/سرور-مرجع: مدل فقط یک **specِ سخت‌گیرِ JSON** پیشنهاد
  می‌دهد (`kind/filename/title/warnings/sheets|paragraphs`)؛ سرور با ولیدیشن/کلمپِ
  قطعی (MAX_SHEETS=3، MAX_ROWS=500، MAX_COLS=30، MAX_PARAGRAPHS=150، MAX_CELL=500،
  sanitizeِ نامِ فایل/برگه، None→خالی، align نامعتبر→justify) فایلِ واقعی را خودش
  رندر می‌کند: openpyxl (rightToLeft، عنوانِ merged، هدرِ استایل‌دار B Nazanin، ستون‌های
  content-sized، freeze) و python-docx (w:bidi در سطحِ پاراگراف — طبقِ درسِ v23 هیچ
  overrideِ per-run؛ فونتِ cs/szCs). دادهٔ گمشده هرگز اختراع نمی‌شود → سلولِ خالی +
  warning که در UI نشان داده می‌شود.
- **[CHANGE — backend]** سرویسِ نو `app/services/letter_attachment_generate.py` +
  endpointِ `POST /api/letter-ai/generate-attachment` در `letter_ai.py` (حقایقِ DB با
  همان `_gather_facts`، ذخیره دقیقاً با مسیرِ آپلودِ دستی: Drive-اول/دیسک-فالبک،
  `facility_id=LTR-<id>`، پروفایلِ مشتری). **نشانِ منشأ:** `notes` با `AI_GENERATED:`
  شروع می‌شود و لیستِ `GET /api/letters/{id}/attachments` فیلدِ `ai_generated` می‌دهد.
- **[CHANGE — frontend]** فرمِ «ساختِ پیوست با هوش مصنوعی» در پنلِ پیوست‌ها (دستور +
  قالبِ خودکار/اکسل/ورد + انتخابِ مدل + نمایشِ warningها)، نشانِ «🪄 ساختِ AI» روی
  ردیف‌ها، و در پیکرِ استخراج: `attSelected` حالا `aiSelAtts[id] ?? !ai_generated` —
  یعنی ساخته‌های AI پیش‌فرض بی‌تیک‌اند (تیکِ دستی همچنان آزاد؛ دکمهٔ «همه» صریح همه را
  true می‌کند) + توضیحِ همین قاعده در UI. مسیرهای قبلی (آپلود/استخراج/جدول-پیوست)
  دست‌نخورده‌اند.
- **[VERIFY]** ۷ تستِ نوی بک‌اند (پارس/کلمپِ spec، استنتاجِ kind، ردِ خروجیِ خراب،
  round-tripِ xlsx/docxِ واقعی، endpoint با مدلِ mock + پرچمِ ai_generated در لیست +
  دانلودِ workbookِ واقعی، ردِ bad_spec) سبز؛ suiteِ کامل پایین‌تر. E2E: ۱۰ چکِ نو
  (فرم، ثبتِ فایل، بجِ AI فقط روی ردیفِ ساخته، warningها، «۱ از ۲» در ابزارِ استخراج،
  بی‌تیکیِ پیش‌فرضِ ساختهٔ AI و تیکِ دستی) — مجموع ۷۸ چک روی باندلِ دیپلوی‌شده سبز.
  type-check/build سبز؛ static سینک شد. مارکر → reflow-v26.

### 2026-07-07 — بازبینیِ درخواستیِ مالک روی «ساختِ پیوست» + دو تقویت (v27)

- **[RE-VERIFY]** به درخواستِ مالک همهٔ مسیرها دوباره روی کد چک شد: ذخیرهٔ Drive-اول/
  دیسک-فالبک ✔، ردیفِ Attachment با account_no مشتری → در تبِ Documents پروفایل
  (customers.py `_by_acc(Attachment)`) کنارِ بقیه ✔، دانلود از پنلِ نامه (Drive و
  دیسک هر دو) ✔، انتخابِ خودکارِ مدل = resolve بر اساسِ task با فالبکِ general از
  همان فهرستِ مدل‌های دستیار + دراپ‌داونِ انتخابِ دستی ✔. دو نمونهٔ واقعیِ خروجی
  (اکسلِ دوشیتی + وردِ توضیحی) با خودِ سرویس رندر و ساختاراً تأیید و برای مالک
  ارسال شد (RTL، عنوانِ merged، هدرِ bold/رنگی، freeze، عرضِ content-sized؛ docx
  با w:bidi روی همهٔ پاراگراف‌ها و فونتِ cs روی همهٔ runها).
- **[HARDEN 1 — گاردِ سروریِ چرخهٔ داده]** تا الان جلوگیری از استخراجِ پیوست‌های
  ساختهٔ AI فقط تیکِ پیش‌فرضِ فرانت بود؛ طبق ADR-001 (سرور مرجع است) endpointِ
  extract-attachment حالا پیوستِ AI_GENERATED را رد می‌کند
  (`{ok:false, error:"ai_generated_attachment"}`) مگر `allow_ai_generated:true`
  صریح بیاید — فرانت این پرچم را فقط برای پیوستی می‌فرستد که کاربر خودش تیک زده.
  مسیرِ قبلی حفظ شده (override آگاهانه ممکن است؛ حذفِ قابلیت نیست).
- **[HARDEN 2 — «مشاهده» از پنلِ نامه]** پرسشِ مالک «از همان‌جا قابلِ بازکردن/
  مشاهده/پرینت؟» → دکمهٔ «مشاهده» به ردیف‌های پیوستِ نامه اضافه شد: blobِ احرازشده
  از `/view` (inline) در تبِ جدید — PDF/تصویر همان‌جا دیده و پرینت می‌شود؛
  xlsx/docx در برنامهٔ خودش باز می‌شود. همان الگوی پروفایلِ مشتری.
- **[VERIFY]** تستِ جدیدِ گارد (ردِ پیش‌فرض + عبور با override) — ماژول ۸/۸؛ suiteِ
  کامل پایین‌تر؛ type-check/build سبز؛ ۷۸ چکِ E2E سبز؛ static سینک. مارکر → reflow-v27.

### 2026-07-07 — پیوست‌ساز: دادهٔ سراسری/شعبه‌ای با پروتکلِ need_data (گزارشِ مالک: جدولِ خالی) — v28

- **[OWNER REPORT]** دستورِ «لیستِ املاکِ شعبهٔ شیخ زاید با وضعیت/تاریخ بیمه‌نامه، سندِ
  رهنی، پلاکِ ثبتی و اسامی مدیرانِ حساب» جدولِ خالی داد با warning «هیچ رکوردی در
  حقایقِ پایگاه‌داده ارائه نشده».
- **[ROOT CAUSE]** دو علتِ هم‌زمان: (الف) نامه «عمومی» بود ⇒ `_gather_facts("")`
  هیچ حقیقتی نمی‌داد؛ (ب) بنیادی‌تر: درخواست ذاتاً **بین‌مشتری/شعبه‌ای** بود ولی موتور
  فقط دادهٔ «یک حساب» را می‌فرستاد — مسیرِ دادهٔ سراسری اصلاً وجود نداشت. خودِ داده‌ها
  در DB هستند: `Customer.branch`/`relationship_manager` و در `MortgagedProperty`:
  `plate_no`/`mortgage_deed_no`/`insurance_no`/`insurance_expiry`. انضباطِ «اختراع
  ممنوع» درست عمل کرد (جدولِ خالی + هشدار)، مشکل نبودِ دسترسیِ داده بود.
- **[CHANGE]** پروتکلِ **need_data** (دوفازی، حداکثر یک دورِ اضافه): پرامپتِ اول حالا
  «کاتالوگِ داده‌های سراسری» را دارد — ۶ مجموعهٔ whitelisted (properties/customers/
  facilities/securities/fixed_deposits/guarantors) + مقادیرِ واقعیِ distinct «شعبه» از
  DB (مدل خودش معادلِ فارسی↔لاتینِ نامِ شعبه را از همین فهرست برمی‌دارد). اگر مدل
  به‌جای spec «need_data» بدهد، سرور کوئریِ قطعی و سقف‌دار (۳۰۰ ردیف/مجموعه، فیلترِ
  شعبه از روی مشتری، case-insensitive؛ شعبهٔ بی‌تطبیق ⇒ فهرستِ کامل + ستونِ شعبه +
  warning، هرگز خالیِ خاموش) را اجرا و دورِ دوم spec نهایی را می‌سازد
  (max_tokens=16000؛ need_data تکراری ⇒ خطای typed). فرانت: راهنمای «نامهٔ عمومی»
  اصلاح شد (دیگر «بدونِ داده» نیست) + مثالِ شعبه‌ای در سرصفحهٔ فرم. مسیرِ تک‌حسابیِ
  قبلی دست‌نخورده (بدونِ need_data همان رفتارِ سابق).
- **[VERIFY]** ۳ تستِ نو: parse_need_data (whitelist/ redِ spec)، fetch_datasets
  (فیلترِ شعبه + مدیرِ حساب/پلاک/سند/بیمه در ردیف، fallbackِ شعبهٔ ناموجود، کاتالوگِ
  شعبه‌ها) و بازتولیدِ عینِ سناریوی مالک end-to-end (نامهٔ عمومی + همان دستور ⇒ دو
  فاز ⇒ xlsx با ردیفِ واقعیِ شعبهٔ درست و بدونِ ردیفِ شعبهٔ دیگر) — ماژول ۱۱/۱۱؛
  suiteِ کامل پایین‌تر؛ type-check/build سبز؛ ۷۸ چکِ E2E سبز؛ static سینک.
  مارکر → reflow-v28.

### 2026-07-08 — نامهٔ پیشنهاد (انگلیسی): جای مهر و امضای مدیر (گزارشِ مالک) — v29

- **[OWNER REPORT]** در صفحهٔ ۳ نسخهٔ انگلیسیِ offer letter، خطِ «Manager Signature
  & Stamp» بلافاصله زیر «Credit Facility Department» بود — جایی برای مهر و امضای
  واقعی باز نشده بود؛ شرط: چینشِ بقیه به‌هم نخورد.
- **[FIX]** فقط همان بلوک: `margin-top: 22mm` روی `.ol-sign` مدیر (پیش‌فرضِ کلاس
  9mm است و بقیهٔ امضاها دست‌نخورده ماندند). چون `.ol-fit` ستونِ flex است و بلوکِ
  امضای پایانیِ صفحه `margin-top:auto` دارد، فضای اضافه از فضای خالیِ تهِ صفحه
  جذب می‌شود — نه از محتوا. نسخهٔ دوزبانه (وام شخصی) از قبل درست بود (امضای بانک
  با auto در تهِ صفحه).
- **[VERIFY]** هارنسِ اندازه‌گیریِ playwright روی باندلِ build شده: فاصلهٔ dept→خط
  ۸۳px (~۲۲mm)، نقطه‌چین/بلوکِ پذیرش در جای قبلی، بلوکِ آخر داخلِ صفحهٔ ۳،
  zoom=1 (هیچ shrink رخ نداد) + اسکرین‌شاتِ بصری. ۷۸ چکِ E2E نامه سبز؛
  type-check/build سبز؛ static سینک؛ suiteِ بک‌اند پایین‌تر. مارکر → reflow-v29.

### 2026-07-08 — نامهٔ پیشنهاد (انگلیسی) صفحهٔ ۲: امضای مشتری چپ، بدونِ خطِ اضافه (گزارشِ مالک) — v30

- **[OWNER REPORT]** در صفحهٔ ۲، فرم دو خطِ امضا کشیده بود و برچسبِ «Customer
  Signature with Stamp» سمتِ راست بود؛ در سندِ مرجعِ Word فقط یک برچسبِ بولد در
  سمتِ **چپ** است، بدونِ هیچ خطی.
- **[FIX]** بلوکِ `.ol-sign` دوspanای صفحهٔ ۲ (span خالی + برچسبِ راست) با یک
  `div` سادهٔ بولدِ چپ‌چین با `margin-top:auto` جایگزین شد — عینِ سندِ مرجع.
  صفحهٔ ۱ (دو امضای بانک/مشتری) و صفحهٔ ۳ دست‌نخورده.
- **[VERIFY]** هارنسِ offer حالا ۸ چک دارد: صفحهٔ ۲ فقط یک برچسب، متنِ برچسب در
  نیمهٔ چپ (اندازه‌گیری با Range روی خودِ متن، نه جعبهٔ full-width)، بدونِ خط
  (borderTopWidth=0 — توجه: preflightِ Tailwind به همه‌چیز border-style:solid با
  عرضِ صفر می‌دهد؛ چکِ style به‌تنهایی گمراه‌کننده است) + چهار چکِ قبلیِ صفحهٔ ۳.
  اسکرین‌شاتِ بصری تأیید شد؛ ۷۸ چکِ E2E نامه سبز؛ type-check/build سبز؛ static
  سینک؛ suiteِ بک‌اند پایین‌تر. مارکر → reflow-v30.

### 2026-07-08 — نامهٔ پیشنهاد: فرمِ خالیِ پیش‌فرض + متغیرهای چشمک‌زن + پنلِ چینش با دبل‌کلیک (درخواستِ مالک) — v31

- **[OWNER REQUEST]** سه خواسته برای همهٔ قالب‌های offer letter: (۱) فرم پیش‌فرض هیچ
  داده‌ای نداشته باشد؛ فقط با واردکردن شماره‌حساب از آخرین تسهیلاتِ مشتری خودکار پر
  شود؛ (۲) هر متغیرِ پرنشدهٔ داخل نامه هایلایتِ چشمک‌زن داشته باشد و با پرشدن برود؛
  (۳) مثل نامه‌های رسمی، دبل‌کلیک روی بخش‌های نامه چینش را قابلِ‌تنظیم کند.
- **[CHANGE 1 — خالیِ پیش‌فرض]** `EMPTY` (همهٔ فیلدها '') حالتِ آغازینِ فرم است؛
  `INITIAL` (M/S.، DUBAI، 1000، متنِ وثایق، امروز، سالِ جاری، Overdraft و …) فقط
  به‌عنوانِ پایهٔ «بارگیریِ حساب»/استخراجِ مصوبه باقی ماند — رفتارِ load عوض نشد.
- **[CHANGE 2 — چشمک‌زن]** کمکیِ `V(key, ph)` + `fillN` (نسخهٔ nodeدارِ fill برای
  بندهای شرایط) + `replaceNode` (توکنِ 250,000 در مدارکِ دوزبانه): هر متغیرِ خالی
  به‌صورت `span.olv-e` با انیمیشنِ زرد چشمک می‌زند (title = برچسبِ فارسیِ فیلد)؛
  پرشدن ⇒ متنِ عادی. چاپ همیشه تمیز است (`@media print` انیمیشن/پس‌زمینه صفر).
  placeholderها فقط خط‌چین‌اند (قانونِ bidi: متنِ فارسی داخلِ سندِ LTR درهم می‌ریزد —
  یک نمونه در بازبینیِ بصری گرفته و اصلاح شد). هر دو قالب (English + دوزبانه) پوشش
  داده شدند، شاملِ سربرگ‌ها (REF سریال/سال، DATE، شعبه).
- **[CHANGE 3 — چینش با دبل‌کلیک]** دبل‌کلیک روی هر بلوک/متغیرِ پیش‌نمایش ⇒ پنلِ
  شناورِ «چینش» (فونتِ pt، بولد، تراز، جهت، فاصلهٔ خط/حروف، جابه‌جایی عمودی/افقی،
  عرض٪ + بازنشانیِ عنصر/همه). کلیدِ هر override مسیرِ DOMِ عنصر داخلِ صفحه است؛
  اعمال بعد از هر رندر (پاک‌سازیِ قبلی‌ها ← اعمالِ جدیدها ← fitPages). ذخیره:
  localStorage به تفکیکِ قالب + داخلِ snapshotِ مشتری (olLayoutMap) و بازیابی در
  «بارگیری».
- **[VERIFY]** هارنسِ offer از ۸ به ۱۶ چک رسید: خالی‌بودنِ فرم و پیش‌نمایش (بدونِ
  DUBAI/Overdraft)، ≥۱۰ متغیرِ چشمک‌زن، رفتنِ چشمک با پرشدنِ فیلد، بازشدنِ پنل با
  دبل‌کلیک، اعمالِ تراز، ماندگاری در localStorage، بازنشانیِ عنصر + ۸ چکِ قبلی
  (صفحهٔ ۲/۳ امضاها) — همه سبز. ۷۸ چکِ E2E نامه سبز؛ ۶۶۷ تستِ بک‌اند سبز؛
  type-check/build سبز؛ static سینک. مارکر → reflow-v31.

### 2026-07-08 — نامهٔ پیشنهادِ دوزبانه: ۱۴ بند، چیدمانِ صفحهٔ ۳/۴، ضامن‌های زیرِهم + دوپلیکیتِ ضامن در DB — v32

- **[OWNER REPORT]** چهار مورد: (۱) نامهٔ وامِ شخصی باید هر ۱۴ بند را داشته باشد نه ۱۲؛
  (۲) در بند ۷ اسامیِ ضامن‌ها پشت‌سرِهم و نامنظم می‌آمد، باید زیرِهم و مرتب باشند؛
  (۳) در صفحهٔ آخر جدولِ امضای مقترض چسبیده به تهِ صفحه بود، باید بالاتر/مرتفع‌تر؛
  (۴) ذیلِ یک حساب یک ضامن دو بار با املای کمی متفاوت و دو فرمتِ شمارهٔ حساب
  («131757» و «2624-131757-006») ثبت شده بود — انگار دو نفر.
- **[FIX 1 — ۱۴ بند]** `terms` در `personalLoanContent.ts` از ۱۲ آیتمِ ادغام‌شده به
  **۱۴ آیتمِ مجزا** بازنویسی شد (بندهای ۹+۱۰ و ۱۱+۱۲ که غلط در هم ادغام بودند
  جدا شدند)؛ جملهٔ غیرشماره‌دارِ «Should you require any clarification» به
  `closingNote` منتقل شد (حذف نشد — قانون ۲). رندر: صفحهٔ ۲ بندهای ۱–۷، صفحهٔ ۳
  بندهای ۸–۱۴ + closingNote + امضای بانک.
- **[FIX 2 — ضامن‌های زیرِهم]** به‌جای joinِ تک‌خطیِ ` / `، کامپوننتِ `GuarStack`
  هر ضامن را در یک `div.guar-line` جدا (بولد، line-height ۱.۵) زیرِهم نشان می‌دهد؛
  در هر دو ستونِ انگلیسی و عربی. `guarLine` تک‌خطی برای مصرف‌کننده‌های دیگر ماند.
- **[FIX 3 — امضای صفحهٔ ۴]** `marginTop:'auto'` (چسبیده به ته) → `14mm` و ارتفاعِ
  خانهٔ امضا `18mm`؛ حالا بلوکِ امضا زیرِ اقرارنامه با فضای کافی می‌نشیند نه تهِ صفحه.
- **[FIX 4 — دوپلیکیتِ ضامن، ROOT CAUSE + forward-fix]** رکوردهای ضامن verbatim از
  ایمپورتِ چندسالهٔ Securities List می‌آیند؛ فیلدِ `guarantor_account` فرمتِ استاندارد
  ندارد (گاهی «core»، گاهی «branch-core-suffix») و نامِ ضامن املای متفاوت دارد، پس
  dedupِ قبلی که **نامِ دقیق** را کلید می‌کرد این‌ها را دو نفر می‌دید. de-dupِ مشتری
  (db_cleanup) هم روی customers کار می‌کند نه guarantors — برای همین «نگرفت».
  **forward-fix محافظه‌کار** (رفتارِ قبلی حفظ شد، فقط یک pass اضافه شد): در upsertِ
  ضامن اگر مطابقتِ نامِ دقیق نبود ولی **coreِ ۶رقمیِ حساب یکی** بود و نام‌ها
  **هم‌پوشانیِ توکنیِ قوی** داشتند (≥۲ توکنِ مشترک یا subset؛ honorific/AL حذف)، همان
  رکورد به‌روزرسانی می‌شود نه رکوردِ جدید. `_acct_core`/`_name_tokens`/`_name_similar`
  اضافه شدند. **رکوردهای موجودِ production ادغامِ خودکار نمی‌شوند** (اصلِ review-first)
  — این فقط جلوی دوپلیکیتِ آینده را می‌گیرد؛ پاکسازیِ داشته‌های فعلی باید با بازبینی
  انجام شود.
- **[VERIFY]** ۳ تستِ نوی ضامن (normalizeِ core + تشابهِ نام؛ ادغامِ «SALWA MOHD…»/
  «SALWA MOHAMED… AL MAAZMI» با core یکسان؛ عدم‌ادغامِ ضامنِ واقعاً متفاوت) سبز؛
  ۶۶۹ تستِ بک‌اند سبز. هارنسِ offer از ۱۶ به ۲۲ چک رسید (۱۴ بندِ شماره‌دار، حضورِ
  بندهای ۱۰/۱۲، closingNoteِ غیرشماره‌دار، فاصلهٔ امضای صفحهٔ ۴ از ته) + ۷۸ چکِ E2E
  نامه سبز؛ type-check/build سبز؛ static سینک. مارکر → reflow-v32.

### 2026-07-08 — دستیارِ نامه: عامیانه→رسمی، تکمیلِ «؟»های ناتمام، اجرای دستورهای داخلِ متن (+ خواندن/ثبتِ DB) — v33

- **[OWNER REQUEST]** پنج توانایی برای فیلدِ متنِ نامه: (۱) متنِ تماماً عامیانه با
  زدنِ دستیار رسمی/بانکی و هم‌لحنِ مکاتباتِ مشابه شود؛ (۲) تکه‌های عامیانهٔ وسطِ
  متنِ رسمی با لحنِ بقیهٔ متن هماهنگ شوند؛ (۳) علامتِ سؤالی که به‌جای «نتوانستم
  جمله را ببندم» گذاشته شده تشخیص داده و جمله با فعلِ مناسب و بدونِ تکرارِ
  واژه‌ها کامل شود؛ (۴) جمله‌های «پرامپت‌مانند» داخلِ متن (مثل «میخوام اینجا
  اینطوری نوشته بشه...») به‌عنوانِ دستور اجرا شوند و متنِ خواسته‌شده جایشان
  بنشیند؛ (۵) اگر آن دستور به دادهٔ پایگاه‌داده نیاز داشت بخواند و اگر گفت «ثبت
  بشه»، ثبت هم بشود.
- **[CHANGE]** روی همان معماریِ امنِ review-first (مدل فقط پیشنهاد می‌دهد؛ گاردِ
  find؛ تیکِ کاربر): (الف) راهنمای ابزارِ `professional` صریحاً بازنویسیِ
  عامیانه→رسمی را برای «کلِ متن» و «تکه‌های وسطِ متنِ رسمی» پوشش می‌دهد —
  جمله‌به‌جمله با text_replaceهای جدا تا تک‌تک قابلِ‌تیک باشند. (ب) ابزارِ نو
  **`complete`** («تکمیلِ جمله‌های ناتمام»): «؟»یِ تنها/جملهٔ نیمه‌کاره/«...» را
  به‌عنوانِ جای‌خالی تشخیص می‌دهد و همان جمله را رسمی و روان می‌بندد (فعلِ مناسب،
  بدونِ تکرارِ واژه‌های مجاور؛ ابهام ⇒ دو پیشنهاد یا note؛ ادعای مالیِ نو ممنوع).
  (ج) ابزارِ نو **`inline_prompts`** («اجرای دستورهای نوشته‌شده داخلِ متن»):
  جملهٔ دستوری را با متنِ خواسته‌شده (رسمی، هم‌لحن) جایگزین می‌کند؛ داده فقط از
  «حقایقِ پایگاه‌داده»، قلمِ ناموجود «________» + توضیح؛ اگر دستور «ثبت» بخواهد
  علاوه بر متن op=db_write هم می‌دهد. (د) گیتِ ثبت: staging db_write حالا با
  `db_extract` **یا** `inline_prompts` فعال می‌شود (SYSTEM_PROMPT هم‌راستا شد).
  (هـ) هر دو ابزار به VALID_CATEGORIES و به تیک‌های پیش‌فرضِ UI اضافه شدند +
  برچسبِ دستهٔ فارسی. مسیرهای قبلی دست‌نخورده.
- **[VERIFY]** ۳ تستِ سرویس (ثبتِ ابزارها با راهنماهای الزام‌آور؛ بقای دسته‌های نو
  در ولیدیشن؛ حضورِ راهنماها در پرامپت) + تستِ endpointِ سناریوی کاملِ مالک
  («؟» ناتمام + دستورِ داخلِ متن با «ثبت بشه»، بدونِ تیکِ db_extract ⇒ هر دو
  text_replace از گاردِ find می‌گذرند و db_write هم stage می‌شود) — ماژول‌ها
  ۲۸/۲۸؛ suiteِ کامل پایین‌تر؛ ۷۸+۲۲ چکِ E2E سبز؛ type-check/build سبز؛ static
  سینک. مارکر → reflow-v33.

### 2026-07-10 — افرلتر چندتسهیلاتی + سامانهٔ تعرفهٔ شارژ (Schedule of Charges) با محاسبهٔ خودکار — v34

- **[OWNER REQUEST + منابع]** (۱) در جدولِ صفحهٔ ۱ افرلترِ شرکتی بشود برای تسهیلاتِ
  بیشتر ردیف اضافه کرد (نمونهٔ Word مالک: OD+CD + ردیفِ Total)؛ (۲) کامیشنِ همهٔ
  ردیف‌ها جمع و در همان یک خانه (بند ۲۳) بنشیند؛ (۳) بولتنِ اسکن‌شدهٔ ۸صفحه‌ایِ
  «Schedule of Charges» (Corporate C01-04-2025 + Individual P01-04-2025) دقیق
  خوانده، در جای مناسبِ دیتابیس ثبت و از فرانت قابلِ‌مشاهده/ویرایش باشد (تعرفه‌ها
  دوره‌ای عوض می‌شوند)، و افرلتر شارژ را حسبِ نوع/مبلغ/نوعِ حساب خودکار پر کند —
  با رعایتِ سقف/کف، معافیتِ تسهیلاتِ کارمندی (نه تسهیلاتِ غیرکارمندیِ کارمند)، و
  حالتِ پوششِ ۱۰۰٪ سپردهٔ underlien.
- **[VALIDATION]** مدلِ محاسبه با نامهٔ واقعیِ مالک (حساب 301408) صحت‌سنجی شد:
  CD 2,800,000 × ‰4 = 11,200 + OD 3,500,000 با پوششِ کاملِ سپرده → 0.1% با سقفِ
  1,000 ⇒ جمع 12,200 — عینِ رقمِ بند ۲۳ همان نامه. این کیس در تست قفل شد.
- **[BACKEND]** جدولِ نو `charge_rules` (model `ChargeRule`) + سرویسِ
  `charge_calc.py` (طبقه‌بندیِ متنِ آزادِ نوعِ تسهیلات → قاعده؛ per_mille/percent/
  flat + min/max + آستانهٔ مبلغِ کوچک؛ کارمزدِ خطِ اعتباری «یک‌بار روی جمعِ»
  ردیف‌های غیرسپرده‌ای؛ od_100fd به‌ازای هر ردیف؛ وامِ تجاری 1.5% min500؛ وامِ
  شخصی 1% min500 max2500 و ≤10k→min200؛ موقتِ OD 2%/CD 1% min500 max2500؛
  تسهیلاتِ کارمندی صفر با یادداشت؛ نوعِ ناشناخته صفر+هشدار — هیچ عددی اختراع
  نمی‌شود) + روترِ `/api/charge-tariff` (list با seedِ fill-empty-only از بولتن،
  upsert، quarantineِ نرم، compute) — همه با audit.
- **[FRONTEND]** صفحهٔ نوی «Charge Tariff» در منوی Tools: جدولِ قواعدِ هر دو بخش
  (شرکتی/حقیقی) با نسخهٔ بولتن، ویرایش/افزودن/قرنطینه — منبعِ زندهٔ تعرفه.
  افرلترِ English: بلوکِ «تسهیلاتِ جدول صفحهٔ ۱» با + افزودن ردیف (نوع/سقف/نرخ/
  Remarks + تیک‌های پوششِ سپرده/کارمندی/موقت برای هر ردیف از جمله ردیف ۱)؛ جدولِ
  چاپی ردیف‌های اضافه + ردیفِ بولد Total (جمعِ سقف‌ها، فرمتِ نمونه)؛ جملهٔ مقدمه
  جمعِ اسمی می‌شود («Overdraft and Cheque Discount facilities are…»)؛ دکمهٔ
  «محاسبهٔ کارمزد از تعرفه» + حالتِ خودکارِ debounceشده با هر تغییرِ نوع/مبلغ ⇒
  جمعِ کارمزدها در ProcessingFee (بند ۲۳، +VAT) + نمایشِ ریزِ محاسبه و هشدارها.
  ردیف‌ها/تیک‌ها/autoCharge در snapshot ذخیره و با بارگیری برمی‌گردند.
- **[VERIFY]** ۷ تستِ بک‌اندِ نو (کیسِ مرجعِ 12,200، کف/سقفِ خط، تجمیعِ چند ردیف
  در یک کارمزدِ خط، وام‌ها و آستانهٔ کوچک، معافیتِ کارمندی، موقت‌ها، ناشناخته+هشدار،
  CRUD+seed+ویرایشِ نرخ+قرنطینه) — ۶۸۰ تستِ کل سبز. هارنسِ offer از ۲۲ به ۲۸ چک
  (ردیفِ دوم در جدول، Totalِ 6,300,000، جمعِ intro، ProcessingFee=12,200، بند ۲۳،
  ریزِ محاسبه) + ۷۸ چکِ نامه سبز؛ type-check/build سبز؛ static سینک.
  مارکر → reflow-v34.

### 2026-07-10 — سینکِ «ایمپورتِ مصوبه → افرلترِ چندردیفی» + بازمحاسبه با حذفِ ردیف — v35

- **[OWNER REQUEST]** بعد از آپلود/تحلیلِ مصوبه در صفحهٔ Import (که تسهیلات را در
  DB ثبت/به‌روزرسانی می‌کند)، زدنِ شماره‌حساب در افرلتر باید آخرین جزئیاتِ تسهیلات
  را بیاورد — و اگر مصوبه چند ردیف تسهیلات دارد، همان چند ردیف در جدول ساخته
  شود و شارژ و بقیهٔ فیلدهای وابسته حسبِ آن‌ها به‌روز شوند؛ حذفِ ردیف هم باید
  وابسته‌ها را با ردیف‌های باقی‌مانده بازمحاسبه کند.
- **[BACKEND]** پاسخِ `/api/crm/offer-letter-data/{acc}` حالا آرایهٔ `Facilities`
  دارد: همهٔ تسهیلاتِ غیرحذف‌شدهٔ حساب، بزرگ‌ترین اول (هم‌راستا با رفتارِ
  تک‌تسهیلاتیِ قبلی)، با برچسبِ نوع، مبلغِ فرمت‌شده، نرخِ «x% p.a.»، remarks و
  status. فیلدهای تک‌تسهیلاتیِ قبلی برای سازگاری دست‌نخورده ماندند.
- **[FRONTEND]** قاعدهٔ تازگی در بارگیری: **ردیف‌های تسهیلاتِ DB بر snapshot
  قدیمی می‌چربند** (هدفِ سینک همین است — مصوبهٔ تازه‌ایمپورت‌شده باید دیده شود)؛
  ردیفِ ۱ = اولین تسهیلاتِ DB و بقیه ردیف‌های اضافه می‌شوند. snapshot فقط وقتی
  DB تسهیلاتی ندارد fallback است، و پرچم‌های هر ردیف (پوششِ سپرده/کارمندی/موقت)
  که در snapshot بودند با تطبیقِ «نوع» دوباره وصل می‌شوند تا بارگیریِ مجدد
  آن‌ها را نپراند. حذف/افزودنِ ردیف از قبل (v34) در effectِ خودکارِ شارژ بود ⇒
  ProcessingFee، ردیفِ Total و جملهٔ مقدمه بلافاصله با ردیف‌های باقی‌مانده
  بازمحاسبه می‌شوند.
- **[VERIFY]** تستِ بک‌اندِ نو (دو تسهیلات OD 3.5M + Loan 2.8M ⇒ آرایهٔ مرتب با
  نوع/مبلغ/نرخ) — ۶۸۱ تستِ کل سبز. هارنسِ offer از ۲۸ به ۳۳ چک: بارگیریِ حسابِ
  دوتسهیلاتی ⇒ دو ردیف + Totalِ 6,300,000 + ProcessingFee خودکارِ 12,200؛ حذفِ
  ردیف ⇒ حذفِ Total و بازمحاسبهٔ ProcessingFee (mockِ حساس به تعدادِ آیتم).
  ۷۸ چکِ نامه سبز؛ type-check/build سبز؛ static سینک. مارکر → reflow-v35.

### 2026-07-10 — گزارشِ مالک از ایمپورتِ واقعی: ردیفِ خیالیِ «Credit Facility»، وثایقِ نیامده، برچسبِ خام — v36

- **[OWNER REPORT + ROOT CAUSE]** بعد از ایمپورتِ مصوبهٔ دوتسهیلاتیِ واقعی، افرلتر
  سه ردیف نشان داد و متنِ وثایق از مصوبه نیامد. سه علتِ مستقل:
  (۱) **ردیفِ خیالی:** متنِ سپردهٔ FD underlien («Fixed Deposit 365 days, Ref
  AJMN FD-2025-73») توسط مدل به‌عنوانِ facility برگشته، در `persist_customer`
  چون typeاش در whitelist نبود به **other** نگاشت و به‌عنوانِ Facility ساخته شد؛
  `_FTYPE_LABEL['other'] = "Credit Facility"` هم همان برچسب را چاپ کرد — دوبله‌ثبت
  نبود، misclassification بود (گاردِ per-type dedup درست کار می‌کند: ایمپورتِ
  مجددِ همان سند فقط update می‌کند، ردیف نمی‌سازد — در تست هم قفل شد).
  (۲) **وثایق:** استخراج اصلاً فیلدی برای متنِ REQUIRED SECURITIES نداشت و
  `loadAccount` هم فقط `saved || defaultِ عمومی` را می‌گذاشت.
  (۳) «cheque_discounting» خام چاپ می‌شد چون `_FTYPE_LABEL` ناقص بود.
- **[FIX 1 — دو لایه]** پرامپتِ استخراج: قاعدهٔ صریح «سپرده/FD (حتی underlien)
  هرگز facility نیست — فقط در security؛ سطرِ Total/سرخطِ خطِ اعتباری/کارمزد هم
  facility نیستند». + گاردِ قطعیِ سرور (`_NON_FACILITY_RE`): ورودیِ facility با
  typeِ خالی/other که متنش deposit/FD/سپرده/underlien/total/کارمزد دارد **ساخته
  نمی‌شود** (شمارنده در خروجی: `facilities_skipped_deposits`). typeهای واقعیِ
  whitelisted هرگز حذف نمی‌شوند — محافظه‌کار.
- **[FIX 2 — وثایق از مصوبه]** فیلدِ نو `required_securities` در اسکیمای استخراج
  (متنِ verbatim، هر قلم یک خط) → روی پروفایل (`RequiredSecurities`؛ هر ایمپورتِ
  جدید بازنویسی می‌کند — آخرین مصوبه مرجع است) → در پاسخِ `/offer-letter-data` →
  در `loadAccount` با اولویتِ DB بر snapshot و default.
- **[FIX 3]** `_FTYPE_LABEL` کامل شد (Cheque Discount، Trust Receipt، LC Sight/
  Usance، LoG).
- **[NOTE — دادهٔ موجود]** ردیفِ خیالیِ فعلی در production خودکار پاک نمی‌شود
  (اصلِ review-first): از صفحهٔ Facilities حذفش کن یا در افرلتر ردیفش را حذف کن؛
  ایمپورت‌های بعدی دیگر نمی‌سازندش.
- **[VERIFY]** تستِ end-to-endِ عینِ سناریو: payload با OD+CD+جعلیِ FD ⇒ فقط ۲
  facility، skipped=1؛ ایمپورتِ مجدد ⇒ added=0/updated=2 (بدونِ دوبله)؛
  offer-letter-data ⇒ دقیقاً دو ردیف با برچسبِ درست + متنِ وثایقِ مصوبه.
  ماژول ۲۲/۲۲؛ suiteِ کامل پایین‌تر؛ ۳۳+۷۸ چکِ E2E سبز؛ type-check/build سبز؛
  static سینک. مارکر → reflow-v36.

### 2026-07-10 — قالب‌بندیِ انتخاب‌محور در پیش‌نمایشِ افرلتر + پنهان‌سازیِ ردیف‌های خیالیِ قدیمی — v37

- **[OWNER REPORT]** (۱) انتخابِ بخشی از جمله در پیش‌نمایش و زدنِ «بولد» از پنلِ
  دبل‌کلیک، کلِ بلوک را بولد می‌کرد — همان چالشی که در نامه‌های رسمی با نوارِ
  شناورِ انتخاب حل شده بود؛ (۲) ردیفِ Overdraft/«Credit Facility» هنوز تکراری
  دیده می‌شد — تحلیل: دوبله‌خوانی از دو منبع نیست (v35 همهٔ ردیف‌ها را از یک
  آرایهٔ DB می‌گیرد)؛ ردیفِ خیالیِ pre-v36 هنوز در DBِ production است.
- **[FIX 1 — نوارِ قالب‌بندیِ انتخاب]** الگوی نوارِ شناورِ صفحهٔ نامه‌ها به
  پیش‌نمایشِ افرلتر آمد: انتخابِ متن ⇒ نوارِ `olf-bar` (B/I/U/A−/A＋/پاک‌کردن)
  که فقط روی همان بازه اعمال می‌شود. چون پیش‌نمایش Reactرندر است (نه
  contentEditable)، markها به‌صورت «آفستِ متنی داخلِ کلیدِ مسیرِ عنصر» ذخیره
  می‌شوند (`olMarks`) و در همان effectِ بعدِ-هر-رندر: spanهای قبلی unwrap و
  بازه‌های جاری با TreeWalker+Range دوباره wrap می‌شوند (بازهٔ ناسازگار = skip
  امن). ماندگاری مثل چیدمان: localStorage به تفکیکِ قالب + snapshot
  (`olMarksMap`) + بازیابی در بارگیری؛ «بازنشانیِ همه» هر دو را پاک می‌کند؛ چاپ
  طبیعی است (استایلِ inline). پنلِ دبل‌کلیک برای «کلِ بلوک» سرِ جایش ماند.
- **[FIX 2 — گاردِ نمایشی]** `/offer-letter-data` ردیفِ تسهیلاتِ OTHERتایپی که
  متنش با همان regexِ v36 «سپرده/جمع» است را در آرایهٔ Facilities **نمایش
  نمی‌دهد** — رکوردِ DB دست‌نخورده می‌ماند (review-first؛ حذف از صفحهٔ
  Facilities با خودِ مالک). نامه‌های حسابِ 301408 از همین دیپلوی بدونِ ردیفِ
  خیالی بارگیری می‌شوند حتی قبل از پاکسازیِ دستی.
- **[VERIFY]** تستِ بک‌اندِ نو (OD واقعی + phantomِ other/FDدار ⇒ فقط Overdraft
  در Facilities؛ facilities_count همچنان ۲) — ۶۸۳ تستِ کل سبز. هارنسِ offer از
  ۳۳ به ۳۷ چک: ظهورِ نوار با انتخاب، بولدِ فقط-کلمهٔ «Dear»، بولدنشدنِ بقیهٔ
  بلوک، ماندگاری در localStorage. ۷۸ چکِ نامه سبز؛ type-check/build سبز؛
  static سینک. مارکر → reflow-v37.

### 2026-07-10 — هاتفیکسِ کرشِ removeChild در قالب‌بندیِ انتخابی + جابه‌جایی باکسِ ملاحظات — v38

- **[OWNER REPORT]** انتخابِ یک کلمه و زدنِ «بزرگ‌تر» از نوارِ شناور ⇒ کرشِ کاملِ
  صفحه: «Failed to execute removeChild on Node». + باکسِ ملاحظات باید بالاتر
  بیاید چون ملاحظاتِ ردیفِ ۱ تسهیلات است.
- **[ROOT CAUSE]** همان ریسکِ ثبت‌شده در experience: spanهای markِ ما text-nodeهای
  متعلق به React را جابه‌جا می‌کنند؛ در رندرِ بعدی (هر تغییرِ state) React برای
  عنصری که diff می‌گیرد removeChild روی نودی می‌زند که دیگر فرزندِ مستقیم نیست ⇒
  کرش. unwrapِ effectمحور دیر است چون بعدِ reconciliation اجرا می‌شود.
- **[FIX — remount guard]** تا وقتی markی وجود دارد، کلِ زیر درختِ پیش‌نمایش
  (`#offer-print`) در هر رندر با key تازه **remount** می‌شود — React درختِ قبلی
  را یکجا دور می‌ریزد و هرگز داخلش diff نمی‌گیرد، پس تصادمِ نود ناممکن است؛
  بدونِ mark، key ثابت است و هیچ هزینه‌ای نیست. اعمالِ مجددِ markها/چیدمان/
  fitPages را همان effectِ بعدِ-هر-رندر انجام می‌دهد. تستِ رگرسیونِ عینِ سناریو
  در هارنس: markِ فعال + چند تغییرِ فیلد ⇒ بدونِ pageerror، mark باقی.
- **[FIX 2]** باکسِ «ملاحظات» به داخلِ بلوکِ تسهیلات (زیرِ پرچم‌های ردیف ۱)
  منتقل و برچسبش شفاف شد («ملاحظات ردیف ۱ تسهیلات»)؛ «وثایق و مدارک» تمام‌عرض شد.
- **[VERIFY]** ۶۸۳ تستِ بک‌اند سبز؛ هارنسِ offer از ۳۷ به ۳۹ چک (کرش‌نکردن حینِ
  رندرهای مکرر با markِ فعال + بقای mark)؛ ۷۸ چکِ نامه سبز؛ type-check/build
  سبز؛ static سینک. مارکر → reflow-v38.

### 2026-07-10 — تورِ ایمنیِ خود-ترمیم برای پیش‌نمایشِ افرلتر (پیگیریِ کرشِ removeChild) — v39

- **[OWNER REPORT]** کرشِ removeChild دوباره دیده شد (۱۰:۴۰) — تحلیل: markِ
  ذخیره‌شده در localStorage از تلاشِ قبلی، روی نسخهٔ v37 (قبل از remount guard)
  هنگامِ لودِ صفحه دوباره اعمال و کرش می‌شد؛ دیپلویِ v38 هنوز نرسیده بود.
  v38 همین مسیر را هم می‌بندد (markِ ذخیره‌شده ⇒ remount در هر رندر).
- **[HARDENING]** برای اینکه هیچ خطای رندری هرگز کلِ اپ را سفید نکند، پیش‌نمایش
  در ErrorBoundaryِ خود-ترمیم (`OlSafe`) پیچیده شد: هر خطای render/commit ⇒
  پاک‌شدنِ قالب‌بندی‌های انتخابیِ ذخیره‌شده (state + localStorage هر دو قالب) +
  remountِ پیش‌نمایش + toastِ «قالب‌بندی بازنشانی شد — صفحه ادامه می‌دهد».
- **[VERIFY]** ۶۸۳ تستِ بک‌اند؛ ۳۹+۷۸ چکِ E2E (شاملِ رگرسیونِ کرشِ v38) همه سبز؛
  type-check/build سبز؛ static سینک. مارکر → reflow-v39.

### 2026-07-10 — سربرگ و پانوشتِ افرلتر = عینِ سربرگِ واقعیِ نامه‌ها (آبی، نه سیاه‌وسفید) — v40

- **[OWNER REPORT]** سربرگِ فرمِ افرلتر با سربرگِ نامه‌های واقعی فرق دارد (۳ عکسِ
  نمونه)؛ پانوشت ساختارش درست ولی رنگش باید مثل نمونه **آبی** باشد نه سیاه‌وسفید؛
  «هر جای دیگر هم این اشتباه هست، همه را اصلاح کن».
- **[ROOT CAUSE]** صفحهٔ افرلتر سربرگ را با CSS/متن بازسازی می‌کرد و لوگوی
  کامپوزیتِ سرمه‌ایِ `BANK_LOGO` (voucher) را می‌گذاشت — هم رنگ غلط (سرمه‌ای/مشکی
  به‌جای آبیِ روشنِ برند)، هم نامِ بانک دوبار دیده می‌شد (داخلِ عکسِ کامپوزیت +
  متنِ کنارش). درحالی‌که ground truth از قبل در `letter/letterhead.ts` موجود بود:
  `LH_LOGO` (نشان + المكتب الاقليمي/REGIONAL OFFICE)، `LH_NAME` (نامِ عربی/انگلیسی
  + "Licensed by CBUAE") و `LH_FOOTER` (بنرِ آدرس/SWIFT با خطِ آبی) — استخراجِ
  عینی از قالبِ Wordِ رسمی؛ صفحهٔ نامهٔ رسمی و خروجیِ Word همین‌ها را می‌کشند.
- **[FIX]** offer-letter/page.tsx: سربرگِ انگلیسی = `LH_LOGO` (۲۶mm، چپ) +
  `LH_NAME` (۶۴mm، راست، بالای REF/DATE)؛ سربرگِ دوزبانه = همان دو عکس در بلوکِ
  چپ (جدولِ Date/Branch/Ref دست‌نخورده)؛ پانوشتِ انگلیسی = عکسِ `LH_FOOTER`
  (خطِ روی پانوشت داخلِ خودِ عکس است ⇒ border-top حذف فقط برای --en) + مارکرِ
  «N | Page» سرِ جایش. متنِ سه‌خطیِ آدرس به‌عنوانِ alt حفظ شد (دسترس‌پذیری +
  ثابت‌ماندنِ FOOT_*). کلاس‌های CSS قدیمی (ol-ro/ol-bank/ol-foot-ar/…) قرنطینه
  (بی‌استفاده ولی موجود)، حذفی در کار نیست.
- **[SCOPE CHECK — «کجاهای دیگر»]** جست‌وجوی سراسری: صفحهٔ نامهٔ رسمی و
  wordExport از قبل از همین asset ها استفاده می‌کنند (درست بودند)؛
  credit-file-retail/corporate و sanction سربرگِ متنیِ سادهٔ فرم‌های داخلی
  (اکسل‌بازسازی) دارند نه سربرگِ نامه — دست نخوردند؛ BANK_LOGO سرمه‌ای فقط در
  برندِ UI (سایدبار/voucher) ماند که سندِ چاپی نیست.
- **[VERIFY]** ۶۸۳ تستِ بک‌اند سبز؛ ۳۹ چکِ offer + ۷۸ چکِ letter سبز؛
  type-check/build سبز؛ اسکرین‌شاتِ سربرگ/پانوشت با نمونهٔ مالک مطابقت شد؛
  static سینک. مارکر → reflow-v40.

### 2026-07-10 — چینشِ scoped (قالب در برابر حساب) + دکمهٔ سراسریِ «چیدمان» روی همهٔ فرم‌ها — v41

- **[OWNER REQUEST]** (۱) الگوی نامه‌های رسمی (پنلِ دبل‌کلیک + دکمهٔ سراسریِ
  «چیدمان») روی افرلتر و همهٔ فرم‌های دیگر هم باشد؛ (۲) قانونِ scope: فرمی که
  محتوایش به حسابی تعلق دارد (حتی ذخیره‌نشده) ⇒ تغییرِ چینش فقط برای همان حساب؛
  فرمِ خالی/بدونِ حساب ⇒ تغییرِ چینش یعنی تغییرِ قالبِ اصلی.
- **[DESIGN]** سه لایهٔ موجود حفظ و هم‌قاعده شدند (بدونِ بازنویسیِ سیستمِ
  جنگ‌آزمودهٔ افرلتر):
  - **افرلتر (درجا):** scope = `tpl` یا `tpl::acct` برای olLayout و olMarks؛
    state دو لایه شد (`olBase`/`olMarksBase` = قالب، `olLayout`/`olMarks` =
    scope فعلی) و پیش‌نمایش نقشهٔ ادغام‌شده را اعمال می‌کند (حساب برنده).
    persist خالی ⇒ removeItem (تایپِ رقم‌به‌رقمِ شماره‌حساب کلیدِ زباله نمی‌سازد).
    snapshot مشتری فقط storeهای حساب را حمل می‌کند و restore هم به کلیدهای
    حساب می‌نویسد (snapshotهای قدیمی همان‌جا restore می‌شوند — قالب دست نمی‌خورد).
    دکمهٔ سراسریِ «چیدمان» + hover-highlight + کلیکِ تکی = همان پنل؛ نشانگرِ
    scope در نوارِ ابزار و در پنل. کرش‌گاردها (remount key، OlSafe) حالا نقشهٔ
    ادغام‌شده را می‌بینند و پاک‌سازیِ بحران هر دو لایه را پاک می‌کند.
  - **ماژولِ مشترکِ جدید `lib/docLayout.tsx` (`useDocLayout`):** همان الگوی
    path-keyed افرلتر (پنل، clear-before-apply بعدِ هر رندر، merge پایه+حساب،
    کلیدهای `dl:<docKey>[::acct]`) بدونِ نیاز به تغییرِ markup — به
    credit-file-retail، credit-file-corporate و sanction وصل شد (دکمه +
    راهنمای scope در نوارِ ابزار، پنل + استایل‌ها در انتهای صفحه).
  - **`lib/formDesign.tsx` (voucher از قبل داشت):** پارامترِ `account` گرفت؛
    layoutِ مؤثر = قالب + tweakهای حساب (merge به تفکیکِ property)، ذخیره/
    بازنشانی فقط روی کلیدِ scope فعلی؛ متنِ دکمه‌ها/پیام‌ها scope را می‌گویند.
    voucher حالا `useFormDesign('voucherLayout_v1', acNo)`.
- **[PITFALL]** کامنتِ JSX که با واژهٔ «global» شروع شود directive ESLint تلقی
  می‌شود (`'hover' is not a valid configuration for a global`) و build را
  می‌شکند — بازنویسی شد.
- **[VERIFY]** ۶۸۳ تستِ بک‌اند سبز؛ هارنسِ offer ۳۹→۴۴ چک (ذخیرهٔ حساب‌محور،
  دست‌نخوردنِ قالب، hint scope، کلیکِ تکی در حالتِ چیدمان)؛ ۷۸ چکِ نامه؛ هارنسِ
  جدیدِ docLayout ۱۰ چک (cf-retail قالب/حساب/merge/دکمه، sanction)؛ voucher
  ۵ چک (کلیدِ قالب/کلیدِ حساب/بدونِ خطا)؛ type-check/build سبز؛ static سینک.
  مارکر → reflow-v41.

### 2026-07-10 — حالتِ چیدمان: درگِ واقعی + پنلِ غیرمزاحم + امضاهای پشتِ‌سرهمِ صفحهٔ ۱ — v42

- **[OWNER REPORT]** (۱) در حالتِ «چیدمان» انتظارِ همان رفتارِ نامهٔ رسمی را
  داشت — کشیدن/جابه‌جایی — ولی کلیک فقط انتخاب می‌کرد؛ (۲) پنلِ شناور جلوی
  دست‌وپا باز می‌شد؛ (۳) امضاهای صفحهٔ ۱ (مدیر/مشتری) باید زیرِ هم و سمتِ چپ
  باشند نه یک ردیفِ چپ/راست.
- **[FIX 1 — درگ]** در حالتِ چیدمان، فشردن و کشیدنِ هر بلوک آن را جابه‌جا
  می‌کند — همان فیلدهای mt/mis که پنل ویرایش می‌کند (پس scoped قالب/حساب و
  قابل‌بازنشانی از همان پنل). آستانهٔ ۴px درگ را از کلیک جدا می‌کند و درگِ
  واقعی، کلیکِ بعدش (بازشدنِ پنل) را مصرف می‌کند. جهتِ افقی از computed
  direction عنصر (rtl ⇒ معکوس). هم در افرلتر هم در useDocLayout (هر ۳ فرم).
- **[FIX 2 — پنل]** پنل دیگر روی نقطهٔ کلیک باز نمی‌شود؛ کنارِ راستِ صفحه
  (یا آخرین جایی که کاربر بردش) باز می‌شود و با کشیدنِ هدرش جابه‌جا می‌شود
  (موقعیت در طولِ session می‌ماند). هر دو پیاده‌سازی (olp/dlp).
- **[FIX 3 — امضاهای صفحهٔ ۱ انگلیسی]** ol-sign ردیفی ⇒ ol-sign-stack:
  «Head of Credit Facility Department» بالا و «Customer Signature with Stamp»
  زیرش با ۱۶mm جای مهر، هر دو چپ و زیرخط‌دار — عینِ نامهٔ واقعی. CSS قدیمی
  ol-sign برای صفحاتِ ۲/۳ دست‌نخورده ماند.
- **[TEST PITFALL]** در Playwright، mouse.move/down/up مختصاتِ viewport
  می‌گیرد و عنصرِ خارج از دید را نمی‌زند (boundingBox با y>viewport) —
  قبل از درگِ دستی scrollIntoViewIfNeeded لازم است؛ وگرنه تست به‌غلط
  پیاده‌سازی را مقصر نشان می‌دهد.
- **[VERIFY]** ۶۸۳ تستِ بک‌اند؛ offer ۴۴→۴۹ چک (امضاهای stacked، درگ ⇒
  ذخیرهٔ mt/mis در store حساب، درگ ≠ بازشدنِ پنل، پنل در لبهٔ صفحه)؛
  letter ۷۸؛ docLayout ۱۰→۱۴ (درگ + لبهٔ صفحه در cf-retail)؛ build/type-check
  سبز؛ static سینک. مارکر → reflow-v42.

### 2026-07-10 — چیدمانِ آزادِ Wordمانند: درگِ بی‌قید (transform) + resize از لبه + چرخش + بازنویسیِ متن — v43

- **[OWNER REPORT]** جابه‌جایی «گیر» داشت — چون با margin (mt/mis) بود، فلوی
  سند مقاومت می‌کرد و همسایه‌ها جابه‌جا می‌شدند. خواسته: مثل «Behind Text» در
  Word — حرکتِ آزاد به هر سمت، تغییرِ اندازه با کشیدنِ لبه‌ها، چرخش، و امکانِ
  تغییرِ محتوای فیلد.
- **[FIX — free float]** درگِ حالتِ چیدمان حالا `transform: translate(dx,dy)
  rotate(rot)` می‌نویسد نه margin — هیچ اثری روی همسایه‌ها ندارد؛ عنصر آزادانه
  روی صفحه می‌لغزد و می‌تواند روی بقیه بنشیند (عینِ behind-text). عناصرِ
  inline هنگامِ transform به inline-block ارتقا می‌یابند. mt/mis برای
  overrideهای قدیمی همچنان اعمال می‌شود (backward compat).
- **[FIX — resize]** فشردن در ۸px لبهٔ چپ/راستِ بلوک ⇒ درگ همان لبه عرضِ
  پیکسلی (`wx`) را تغییر می‌دهد (لبهٔ چپ، موقعیت را هم جبران می‌کند تا لبهٔ
  راست ثابت بماند). پنل هم فیلدهای X/Y آزاد، چرخش (درجه) و عرض px گرفت.
- **[FIX — متن]** پنل textarea «متنِ این عنصر (بازنویسی)» گرفت — فقط روی
  بلوک‌های متنِ سادهٔ بدونِ فرزندِ عنصری اعمال می‌شود؛ در افرلتر وجودِ هر txt
  همان remount-guardِ markها را فعال می‌کند تا reconciliation هرگز به
  نودهای بازنویسی‌شده نخورد (درسِ کرشِ removeChild).
- **[TEST PITFALL]** عنصرِ float شده روی همسایه‌اش paint می‌شود (stacking
  context) — تستِ درگِ عنصرِ دوم باید قبل از جابه‌جاییِ عنصرِ اول اجرا شود
  وگرنه pointerdown به عنصرِ شناور می‌خورد.
- **[VERIFY]** ۶۸۳ تستِ بک‌اند سبز (در جریانِ همین گیت)؛ offer ۴۹→۵۳ چک
  (wx از لبه، dx/dy از درگِ آزاد + transform، چرخش از پنل، بازنویسیِ متن)؛
  letter ۷۸؛ docLayout ۱۳؛ build/type-check سبز؛ static سینک. مارکر → reflow-v43.

### 2026-07-10 — حذفِ فیلد (با شبحِ قابلِ‌برگشت) + درگِ مستقیمِ عنصرِ دبل‌کلیک‌شده — v44

- **[OWNER REQUEST]** مثل صفحهٔ نامهٔ رسمی: (۱) فیلدِ ناخواسته قابلِ حذف از
  برگه باشد؛ (۲) بعد از دبل‌کلیک، همان عنصر بدونِ روشن‌کردنِ حالتِ سراسریِ
  «چیدمان» مستقیم قابلِ درگ/کشیدن/تغییرِ اندازه باشد.
- **[FIX — حذفِ فیلد]** `hide` به مدلِ override اضافه شد؛ دکمهٔ «حذف از برگه /
  نمایشِ دوباره» در پنل. خارج از حالتِ چیدمان ⇒ display:none (در چاپ هم نیست)؛
  داخلِ حالتِ چیدمان ⇒ شبحِ کم‌رنگ با کادرِ قرمزِ خط‌چین که کلیک‌پذیر است و
  از همان پنل برمی‌گردد — حذفِ برگشت‌پذیر، هم‌قاعده با scope قالب/حساب و
  «بازنشانیِ همه/این عنصر». (الگوی hidden صفحهٔ نامه.)
- **[FIX — درگِ عنصرِ انتخاب‌شده]** با پنلِ باز، خودِ عنصرِ انتخاب‌شده (کادرِ
  آبی + cursor:move) بدونِ حالتِ چیدمان درگ (dx/dy) و از لبه‌ها resize (wx)
  می‌شود — فقط همان عنصر؛ بقیهٔ صفحه رفتارِ عادی (انتخابِ متن/marks) دارد.
  پاک‌سازیِ after-render حالا کلیدِ انتخاب‌شده را هم پوشش می‌دهد.
- **[SCOPE]** هر دو در افرلتر + useDocLayout (پروندهٔ اعتباری ×۲ و مصوبه).
- **[VERIFY]** ۶۸۳ تستِ بک‌اند (گیتِ همین نسخه)؛ offer ۵۳→۵۸ چک (درگِ بدونِ
  حالتِ چیدمان، hide/restore، شبح در حالتِ چیدمان)؛ letter ۷۸؛ docLayout ۱۳؛
  type-check/build سبز؛ static سینک. مارکر → reflow-v44.

### 2026-07-10 — فاصلهٔ خط از نوارِ انتخاب، لیستِ Wordمانند (شماره/بولت/تیک)، انتخابِ فونت، و فونتِ پیش‌فرضِ دوخطی — v45

- **[OWNER REQUEST]** (۱) برای متنِ انتخاب‌شده فاصلهٔ بینِ خطوط قابل‌تنظیم باشد؛
  (۲) انتخابِ متنِ چندخطی ⇒ شماره‌گذاری مثل Word یا نشانه (تیک/بولت/خط‌تیره)
  ابتدای هر خط؛ (۳) تغییرِ فونت؛ (۴) پیش‌فرضِ جملهٔ مخلوط: فارسی B Nazanin و
  انگلیسی Times New Roman.
- **[FIX]**
  - نوارِ انتخاب (olf-bar): دکمه‌های «خ−/خ＋» (فاصلهٔ خطِ بندِ میزبانِ انتخاب،
    گام 0.1) و «1. / • / ✓ / ↺» (نشانهٔ ابتدای خطوطِ همان بند) — از همان store
    چینشِ scoped می‌نویسند (قابل‌بازنشانی، قالب/حساب).
  - `list` در مدلِ override: در apply، هر خطِ غیرخالیِ بلوکِ برگ با شماره/
    نشانه پیشونددار می‌شود (شماره‌ها خودکار 1..n). چون text-node ها را عوض
    می‌کند، مثلِ txt واردِ شرطِ remount-guard شد.
  - `font` در مدل + منوی فونت در پنل (B Nazanin، Times، Arial، Traditional
    Arabic، Georgia، Tahoma، پیش‌فرض) — در هر دو پیاده‌سازی (افرلتر + docLayout).
  - فونتِ پیش‌فرضِ صفحاتِ افرلتر: `"Times New Roman","B Nazanin","Traditional
    Arabic",Georgia,serif` — fallback نویسه‌به‌نویسهٔ مرورگر یعنی لاتین در Times
    و فارسی/عربی خودکار در B Nazanin می‌نشیند (خواستهٔ ۴، بدونِ نشانه‌گذاری).
- **[BUG FIX]** فلگِ skipClick بعد از درگِ عنصرِ انتخاب‌شده در حالتِ عادی مصرف
  نمی‌شد (onClick فقط در حالتِ چیدمان وصل است) و دبل‌کلیکِ بعدی را می‌خورد —
  فلگ حالا فقط توسطِ click-openِ حالتِ چیدمان مصرف و در شروعِ هر gesture جدید
  صفر می‌شود (هر دو پیاده‌سازی).
- **[VERIFY]** ۶۸۳ تستِ بک‌اند سبز؛ offer ۵۸→۶۲ چک (فونتِ پیش‌فرضِ مخلوط،
  منوی فونت، شماره‌گذاری، تعویضِ نشانه)؛ letter ۷۸؛ docLayout ۱۳؛ type-check/
  build سبز؛ static سینک. مارکر → reflow-v45.

### 2026-07-11 — ایمپورتِ اکسل: «timed out after 60s» — مهلتِ ۱۸۰s + یک retry + chunk کوچک‌تر — v46

- **[OWNER REPORT]** آپلود اکسلِ «وضعیت املاک حساب‌های مطالباتی…» (۳۰KB) در
  صفحهٔ ایمپورت چند بار پشتِ‌سرهم با «خطا: استخراج با مدل ناموفق بود: timed
  out after 60s» شکست خورد.
- **[ROOT CAUSE]** مسیرِ اکسل/CSV در `_process_document` با `inference.complete`
  و مهلتِ سراسریِ ۶۰ثانیه صدا زده می‌شد (بدونِ retry)، درحالی‌که مسیرِ
  PDF/تصویرِ همان صفحه ۱۸۰ثانیه دارد. chunkهای تا ۱۰۰هزار کاراکتر + خروجیِ
  ۸هزار توکن برای مدل‌های کندترراحت از ۶۰ثانیه رد می‌شود ⇒ شکستِ تکرارشونده.
- **[FIX — کوچک و برگشت‌پذیر، مسیرِ قبلی حفظ]**
  - `inference.complete` پارامترِ اختیاریِ `timeout` گرفت — پیش‌فرض همان ۶۰s،
    پس هیچ مصرف‌کنندهٔ دیگری تغییرِ رفتار ندارد؛ پیامِ خطا مهلتِ واقعی را می‌گوید.
  - مسیرِ اکسلِ ایمپورت: مهلت ۱۸۰s (هم‌ترازِ مسیرِ PDF) + **یک retry** بعد از
    ۳ثانیه روی timeout/خطای شبکه/429 (الگوی backoffِ خودِ مسیرِ PDF) + chunk
    از ۱۰۰k به ۶۰k (هر فراخوانی راحت داخلِ مهلت) + پیامِ خطای شفاف‌تر با
    راهنمای انتخابِ مدلِ سریع‌تر.
  - استخراجِ پیوستِ نامه (letter_attachment_extract) هم — همان کلاسِ کار —
    مهلتِ ۱۸۰s گرفت (این مسیر از قبل chunk-error-tolerant بود).
- **[VERIFY]** تستِ رگرسیونِ جدید: xlsx آپلود ⇒ اولین فراخوانی timeout ⇒ retry
  موفق ⇒ job=done و هر دو فراخوانی با deadline=180. کلِ ۶۸۴ تستِ بک‌اند سبز؛
  فرانت‌اند تغییری نکرد (type-check سبز).

### 2026-07-11 — تاریخ‌ها DD/MM/YYYY با اسلش + کامای مبالغ + رفعِ گم‌شدنِ متنِ وثایق بعد از ذخیره — v47

- **[OWNER REQUEST/REPORT]** (۱) تاریخِ انگلیسی در همهٔ نامه‌ها/فرم‌ها با اسلش
  و به ترتیبِ روز/ماه/سال باشد (الان ISO سال-اول چاپ می‌شد)؛ هر تاریخِ
  «-»دار — حتی تایپ‌شده وسطِ متن — به «/» تبدیل شود. (۲) مبلغِ هزینه/شارژ در
  افرلتر حینِ تایپ کامای سه‌رقمیِ انگلیسی بگیرد. (۳) باگ: متنِ «وثایق و مدارک
  موردنیاز» بعد از ذخیره و بارگیریِ مجدد ناقص برمی‌گشت — اضافه‌های کاربر
  نمی‌آمد.
- **[ROOT CAUSE (۳)]** در loadAccount، «RequiredSecurities» تنها فیلدِ متنی با
  اولویتِ DB بود (`d.RequiredSecurities || saved...` — تصمیمِ v36 برای همگامی
  با مصوبه). نتیجه: متنِ استخراجیِ مصوبه بی‌صدا جایگزینِ متنِ ویرایش‌شده و
  ذخیره‌شدهٔ کاربر می‌شد. سایرِ فیلدها saved-first بودند (سالم)؛ سه‌گانهٔ
  ردیف‌های تسهیلات DB-first عمدی (v35) و دست‌نخورده ماند.
- **[FIX]**
  - «snapshot کاربر برنده» + قابلیتِ v36 به شکلِ صریح حفظ شد: نسخهٔ مصوبه در
    state نگه داشته می‌شود؛ اگر با متنِ نامه فرق داشته باشد toast اطلاع می‌دهد
    و دکمهٔ «از آخرین مصوبه» کنارِ فیلد با confirm جایگزین می‌کند.
  - `lib/dates.ts` → `dmySlash`: تبدیلِ render-time هر توکنِ تاریخ —
    `YYYY-MM-DD` ⇒ `DD/MM/YYYY` و `D-M-YYYY` ⇒ اسلش+صفرپَد. در افرلتر روی
    همهٔ مقادیرِ چاپی (Blink/V، fill، ValidUntil، Remarks ها، NotesPersonal)؛
    در sanction (بلور روی همهٔ inputهای برگه)، credit-file ×۲ و voucher
    (بلورِ فیلدِ تاریخ). ذخیره‌سازی تغییری نمی‌کند مگر کاربر خودش تایپ کند.
  - کامای پولی: ProcessingFee/CreditLimit/LoanAmount/LienAmount + ستونِ سقفِ
    ردیف‌های اضافه — فقط مقادیرِ صرفاً عددی (چیزی مثل «3,500,000/-» یا
    «13% p.a.» دست نمی‌خورد).
- **[VERIFY]** offer ۶۲→۶۷ چک (برد متنِ ذخیره‌شدهٔ وثایق + دکمهٔ مصوبه، DATE
  اسلش‌دار، تاریخِ «-»دارِ تایپی، کاما)؛ letter ۷۸؛ docLayout ۱۳؛ pytest کامل
  (بدونِ تغییرِ بک‌اند) سبز؛ build/type-check سبز؛ static سینک. مارکر → reflow-v47.

### 2026-07-11 — هایلایتِ ماندگارِ انتخاب + ویرایشِ متن داخلِ خودِ نامه با دبل‌کلیک — v48

- **[OWNER REPORT]** (۱) هنگامِ انتخابِ کلمه برای بولد و…، هایلایتِ آبیِ انتخاب
  دیده نمی‌شد (نوارِ قالب می‌آمد ولی رنگِ انتخاب نه)؛ (۲) ویرایشِ متن فقط از
  باکسِ کوچکِ پنل ممکن بود — باید با دبل‌کلیک داخلِ خودِ فیلد ویرایش شود.
- **[ROOT CAUSE (۱)]** با وجودِ هر mark/txt، remount-guard پیش‌نمایش را در هر
  رندر بازمی‌سازد؛ خودِ بازشدنِ نوار (setOlFmt) یک رندر است ⇒ DOM نو می‌شود و
  selectionِ مرورگر (وابسته به نودهای قبلی) می‌پرد — نوار می‌ماند، آبی نه.
- **[FIX (۱)]** تا وقتی نوار باز است، خودِ بازهٔ انتخاب با همان ماشینِ
  offset-basedِ markها به‌صورت span[data-olsel] با پس‌زمینهٔ آبیِ selection
  رندر می‌شود (پس از هر remount هم دوباره اعمال می‌شود)؛ بستنِ نوار پاکش
  می‌کند و در چاپ هم بی‌اثر است. olFmt/olEdit هم به شرطِ remount اضافه شدند.
- **[FIX (۲) — ویرایشِ درجا]** دبل‌کلیک روی بلوکِ متنِ ساده (برگِ بدونِ فرزندِ
  ساختاری) علاوه بر پنل، همان بلوک را contentEditable می‌کند (کادرِ سبز +
  فوکوس): Enter یا کلیکِ بیرون ⇒ ثبت به‌عنوانِ همان txt overrideِ scoped
  (فقط اگر متن واقعاً تغییر کرده باشد — دبل‌کلیکِ خالی چیزی ذخیره نمی‌کند)؛
  Escape ⇒ انصراف. حینِ ویرایش، درگِ همان عنصر خاموش است (caret اولویت دارد)
  و بعد از پایانِ ویرایش برمی‌گردد. باکسِ پنل هم مثل قبل هست.
- **[VERIFY]** offer ۶۷→۷۴ چک (هایلایتِ ماندگار، پاک‌شدن با بستنِ نوار،
  contentEditable شدن، ثبتِ متنِ تایپ‌شده در store حساب، خروج از حالتِ
  ویرایش)؛ letter ۷۸؛ pytest ۶۸۴ سبز؛ build/type-check سبز؛ static سینک.
  مارکر → reflow-v48.

### 2026-07-11 — ایمپورتِ اکسل: «هیچ حساب/مشتری استخراج نشد» ⇒ fallback قطعیِ جدول + خطای تشخیصی — v49

- **[OWNER REPORT]** جدولِ «وضعیت املاک حساب‌های مطالباتی در ایران» (ستون‌های
  شماره حساب/نام مشتری/سند/شهر/نوع/وضعیت، ۱۱۶ حساب) با پیامِ «هیچ حساب/مشتری
  از جدول استخراج نشد» رد شد، درحالی‌که شماره‌حساب‌ها واضح‌اند.
- **[ROOT CAUSE]** متنِ تولیدیِ workbook_to_text سالم بود (تأیید روی خودِ فایل)؛
  یعنی مدل پاسخ داده ولی خروجی‌اش JSONِ قابل‌مصرف نبوده (رد/متنِ آزاد/بریده) —
  parse_model_json per-chunk خالی برمی‌گشت و خطای نهایی هم هیچ سرنخی نمی‌داد.
- **[FIX]**
  - `doc_ingest.table_fallback_customers`: مسیرِ **قطعیِ بدونِ مدل** برای
    جدول‌هایی که هدرشان ستونِ حساب دارد — نگاشتِ فقط-براساس-هدر (بدونِ حدس):
    هستهٔ ۶رقمیِ حساب، نام، و ستون‌های ملک (سند/پلاک ⇒ mortgage_deed_no،
    شهر، نوع، وضعیت ⇒ remarks)؛ ردیف‌های هم‌حساب در یک مشتری merge می‌شوند.
    وقتی خروجیِ مدل خالی بود این مسیر به‌کار می‌افتد (نامِ منبع در نتیجه:
    «… + استخراج قطعی جدول»). روی خودِ فایلِ مالک: ۱۱۶ مشتری/۲۶۶ ملک.
  - persist املاک حالا `remarks` (وضعیت رهن/فک رهن) را هم fill-empty می‌نویسد.
  - اگر باز هم هیچ مشتری نبود، خطا حالا نمونهٔ پاسخ مدل را نشان می‌دهد
    («نمونهٔ پاسخ مدل: …») + راهنمای انتخابِ مدلِ دیگر/چکِ ستونِ حساب.
  - مسیرِ قبلی (مدل-اول) دست‌نخورده است؛ fallback فقط بعدِ شکستِ آن.
- **[VERIFY]** تستِ واحدِ نگاشتِ هدرها (سند/وضعیت/گروه‌بندی + شیتِ بدونِ ستونِ
  حساب ⇒ خالی) و تستِ e2e: مدلِ «sorry…» ⇒ job=done از مسیرِ قطعی. کل تست‌های
  doc-import 25/25؛ pytest کامل در گیت. بدونِ تغییرِ فرانت‌اند.

### 2026-07-11 — استخراجِ پیوستِ نامه: همان fallbackِ قطعی + ویراستارِ ارشد در ابزارِ «تنظیم حرفه‌ای» — v50

- **[OWNER REPORT]** (۱) استخراج از پیوستِ اکسلِ «املاکی که سابقه بیمه دارند»
  در دستیارِ نامه با «خطای مدل: داده‌ای استخراج نشد» شکست خورد — همان الگوی
  v49، ولی در مسیرِ پیوستِ نامه که fallback نداشت؛ (۲) از ابزارِ «تنظیم
  حرفه‌ای» انتظارِ بازنویسیِ شیوا داشت (جمله‌بندیِ خام/بچگانه، تکرارِ کلمات)،
  ولی فقط اصلاح‌های نقطه‌ای (املا/نیم‌فاصله/چند مورد محاوره) پیشنهاد می‌شد.
- **[FIX]**
  - `letter_attachment_extract`: شاخهٔ Excel/CSV بعد از شکستِ خروجیِ مدل، به
    `doc_ingest.table_fallback_customers` می‌افتد (همان مسیرِ قطعیِ v49؛ نامِ
    منبع «… + استخراج قطعی جدول»). مسیرِ مدل-اول دست‌نخورده.
  - راهنمای ابزارِ `professional` گسترش یافت: علاوه بر رسمی‌سازی، «ویراستارِ
    ارشد» — بازنویسیِ شیوایِ جمله‌های خام/ناشیانه حتی وقتی غلط نیستند، تنوعِ
    مترادف برای واژه‌ها/فعل‌های تکراریِ مجاور، حذفِ حشو، شکستنِ جمله‌های
    درهم، و پیوندِ منطقیِ اداری — هر مورد یک text_replace جدا، بدونِ تغییرِ
    معنا/اعداد/تعهدها.
- **[VERIFY]** تستِ جدید: اکسل + مدلِ بی‌خروجی ⇒ extract_attachment ok از مسیرِ
  قطعی (۴۴۰۰۲۲ در خروجی، برچسبِ «قطعی»)؛ ۴۸ تستِ letter-assistant/attachments
  و ۲۶ تستِ doc-import سبز؛ pytest کامل در گیت. بدونِ تغییرِ فرانت‌اند.

### 2026-07-11 — اعمالِ پیشنهاد «متن پیدا نشد»، 502ِ استخراجِ پیوست، و بازنویسیِ همهٔ پاراگراف‌ها — v51

- **[OWNER REPORT]** (۱) با زدنِ «اعمال»، «مورد در متنِ فعلی پیدا نشد و رد شد»؛
  (۲) استخراج از پیوستِ اکسل این بار با «Request failed with status code 502»؛
  (۳) بازنویسیِ حرفه‌ای هنوز کم‌تعداد است (یک پیشنهاد برای کلِ متن).
- **[ROOT CAUSE]**
  - (۱) گاردِ سرور تطبیقِ نرمال‌شده (فاصله/نیم‌فاصله) را می‌پذیرفت ولی اعمالِ
    کلاینت فقط indexOf دقیق و فقط داخلِ «یک text node» بود ⇒ پیشنهادی که ي/ك
    عربی داشت یا از مرزِ <b>/<br> عبور می‌کرد (بازنویسیِ کاملِ پاراگراف)
    در اعمال شکست می‌خورد.
  - (۲) استخراجِ پیوست inline در همان درخواستِ HTTP اجرا می‌شود؛ با مهلتِ
    ۱۸۰ثانیه‌ایِ v46، فراخوانی از timeout دروازهٔ Render رد می‌شد ⇒ 502ِ خام.
- **[FIX]**
  - کلاینت: تطبیقِ فازیِ fallback — نمای canonical (ي→ی، ك→ک، ZWNJ/NBSP/فاصله‌ها
    جمع‌شده) از کلِ فیلد با نگاشتِ اندیس به متنِ اصلی؛ یافتنِ snippet در آن نما
    و splice در عرضِ چند text node (جایگزین در اولین node). مسیرِ دقیقِ قبلی
    اول امتحان می‌شود (رفتارِ سابق حفظ).
  - گاردِ سرور (_norm_ws) هم ي/ك و NBSP را canonical می‌کند (آینهٔ کلاینت).
  - استخراجِ پیوستِ Excel/CSV: **اول مسیرِ قطعیِ فوری** (ستونِ حساب ⇒ نگاشتِ
    بدونِ مدل، بدونِ ریسکِ timeout)؛ فقط اگر جدول قابلِ نگاشت نبود مسیرِ مدل
    با مهلتِ ۷۵ثانیه (زیرِ سقفِ دروازه). مسیرِ مدل حذف نشده.
  - ابزارِ professional: «در تعدادِ پیشنهادها محافظه‌کار نباش — هر پاراگرافِ
    قابل‌بهبود بازنویسیِ خودش را بگیرد».
- **[VERIFY]** تستِ گاردِ ي/ك/نیم‌فاصله؛ ۴۹ تستِ letter-assistant/doc-import؛
  letter ۷۸ و offer ۷۴ چکِ E2E؛ pytest کامل در گیت؛ build/type-check سبز؛
  static سینک. مارکر → reflow-v51.

### 2026-07-11 — ویرایشِ کاملِ متن‌های آشفته (عمومی، نه مخصوصِ یک نمونه) — v52

- **[OWNER REQUEST]** «آیا می‌تواند همچین متنی را کامل ویرایش کند؟» — نمونه:
  نامه‌ای با آمیزهٔ متنِ رسمی، نقل‌قول، جمله‌های پراکنده، و خواستهٔ نویسنده
  وسطِ متن («میخوام از اداره خارجه که یه جوری به بیمه سرمد بگه که ...»).
  تأکید: قابلیت عمومی باشد، برای متن‌های بدتر هم، نه tune شده روی این نمونه.
- **[GAP ANALYSIS]** سه شکافِ عمومی: (۱) inline_prompts فقط دستورهای صریح را
  می‌گرفت؛ خواسته‌های غیرصریح/اول‌شخص («میخوام ... یه جوری بگه که ...») که
  «دربارهٔ» محتوای نامه حرف می‌زنند نه خودِ محتوا، پوشش نداشت؛ (۲) ابزارِ
  paragraphs بازچینشِ جمله‌های پراکنده (برداشتن از جای قبلی + نشستن در جای
  منطقی) را صریح اجازه نمی‌داد؛ (۳) هیچ اصلی نمی‌گفت حاصلِ جمعِ پیشنهادها
  باید «یک نامهٔ کاملِ آماده‌ی امضا» شود نه وصله‌های جدا.
- **[FIX — سه تقویتِ عمومی در promptها]**
  - inline_prompts: تشخیصِ نیتِ نویسنده (زبانِ اول‌شخص/محاوره‌ایِ «می‌خواهم
    گفته شود») ⇒ تبدیل به متنِ رسمیِ خطاب‌شده به مخاطبِ درست، بدونِ باقی‌ماندنِ
    زبانِ نیت در نامه.
  - paragraphs: بازچینشِ واقعی — نسخهٔ منسجمِ هر پاراگراف + برداشتنِ جملهٔ
    جابه‌جاشده از جای قبلی (بدونِ حذف/افزودنِ محتوا).
  - SYSTEM_PROMPT قاعدهٔ ۸: «اصلِ نتیجهٔ نهایی» — با تیکِ همهٔ پیشنهادها باید
    نامه‌ای منسجم با ترتیبِ منطقیِ اداری حاصل شود؛ معنا/مبالغ/تعهدها ثابت.
- **[VERIFY]** تستِ سنجهٔ راهنمای inline_prompts به عبارتِ جدید به‌روز شد
  (همان assertion که رسیدنِ راهنما به مدل را ثابت می‌کند)؛ ۲۹ تستِ
  letter-assistant/endpoints سبز؛ pytest کامل در گیت. بدونِ تغییرِ فرانت‌اند.

### 2026-07-11 — نصفِ صفحهٔ اول خالی، بلوکِ پایانی در صفحهٔ ۲ — v53

- **[OWNER REPORT]** (اسکرین‌شات نامهٔ رسمی) «نصف صفحه اول خالیه و متن خیلی هم
  نیست ولی اون موارد که پایین صفحه میاد افتاده تو صفحه دوم!!» — امضاکننده
  («سرپرستی منطقه خلیج فارس») و اقدام‌کننده با متنِ کوتاه به صفحهٔ ۲ تبعید شدند.
- **[ROOT CAUSE]** چیدمانِ ذخیره‌شدهٔ کاربر (letterTemplate_v4) فیلدِ
  «امضاکننده» را بالای شروعِ ناحیهٔ متن کشیده بود (y≈75mm < body.y=118mm).
  قاعدهٔ قدیمی `closingTop = min(sender.y, copyto.y, action.y) − gap` این
  مقدار را کور مصرف می‌کرد ⇒ `regionAvail(last, true)` به کفِ ۲۴px فرو
  می‌ریخت ⇒ هر متنی «سرریز» محسوب می‌شد ⇒ سُردادن هم ممکن نبود (اقدام‌کننده
  پایین بود، maxShift کوچک) ⇒ `pages.push([])` و تبعیدِ کلِ بلوک به صفحهٔ تازه.
- **[FIX]** `frontend/src/app/letter/page.tsx`: ناحیهٔ رزروِ بلوکِ پایانی فقط
  از فیلدهایی ساخته می‌شود که پنهان نیستند **و** `y >= body.y` دارند؛ اگر هیچ
  فیلدی واجد شرایط نبود، رزروی جز کفِ صفحه (شماره‌صفحه) اعمال نمی‌شود. فیلدِ
  بالاکشیده همان‌جا که کاربر گذاشته رندر می‌شود — فقط دیگر ظرفیتِ متن را
  گروگان نمی‌گیرد. تغییرِ تک‌خطی و برگشت‌پذیر؛ رفتار برای چیدمان‌های عادی
  (همهٔ فیلدها زیرِ متن) بیت‌به‌بیت همان قبلی است.
- **[VERIFY]** دو چکِ جدید در harness نامه (چیدمانِ seed شده با sender بالای
  متن ⇒ یک صفحه؛ هر سه فیلدِ پایانی hidden ⇒ یک صفحه): letter ۸۲/۸۲،
  offer ۷۴/۷۴؛ pytest کاملِ بک‌اند سبز؛ type-check/build سبز؛ static سینک.
  مارکر → reflow-v53.
- **[LESSON]** experiences/paginated-doc-fixed-footer-cliff-slide-dont-push.md
  (پیگیریِ سوم: هندسهٔ ورودیِ کاربر را قبل از مصرف عاقل‌سازی کن).

### 2026-07-13 — کیفیتِ نثرِ دستیار: تکرار واژه‌ها، ادبِ مکاتبه با بالادستی، دلنشینی — v54

- **[OWNER REPORT]** (اسکرین‌شات نامهٔ تمدید بیمه‌نامه) سه ضعف در خروجی دستیار:
  (۱) «کلمات تکراری زیاد اومده»؛ (۲) «رعایت اینکه با اداره بالادستی چطور باید
  درخواست رو مطرح کرد ... نتونسته» — نامه خطاب به «رئیس محترم ادارهٔ کل» با
  صیغهٔ دستوری («مقتضی است آن اداره ... معمول گردد»)؛ (۳) «نوع نوشتنش میتونست
  خیلی بهتر و قشنگ‌تر و لذت‌بخش‌تر باشه».
- **[ROOT CAUSE]** پرامپت‌ها کیفیت را جمله‌به‌جمله می‌سنجیدند نه روی متنِ نهاییِ
  سرهم‌شده (تکرارِ بین‌پاراگرافی دیده نمی‌شد)؛ هیچ قاعده‌ای دربارهٔ جایگاهِ
  مخاطب (بالادستی/هم‌تراز/زیرمجموعه) و صیغهٔ درخواستِ متناسب وجود نداشت —
  با اینکه فیلدهای گیرنده و امضاکننده به مدل داده می‌شوند؛ و «شیوایی» فقط
  در حدِ اصلاحِ جملهٔ ناشیانه خواسته شده بود، نه نثرِ پیوستهٔ دلنشین.
- **[FIX — عمومی، نه tune روی نمونه]** `backend/app/services/letter_assistant.py`:
  - راهنمای professional: سنجهٔ کیفیت = متنِ نهاییِ سرهم‌شده؛ رفعِ تکرارِ
    واژه/قالب در جمله‌ها و پاراگراف‌های مجاور (مترادفِ اداری، ضمیر/اشاره:
    آن، مذکور، یادشده)؛ آهنگِ نثرِ دلنشین (طولِ جمله‌ها متنوع، شروعِ
    پاراگراف‌ها غیرتکراری، پایان‌بندی گرم) — «نامه‌نگارِ باسابقهٔ بانکی».
  - SYSTEM_PROMPT قاعدهٔ ۹ «سلسله‌مراتبِ مخاطب»: تشخیصِ جایگاه از فیلدهای
    گیرنده/امضاکننده؛ به بالادستی فقط زبانِ گزارش/استدعا (به استحضار
    می‌رساند، خواهشمند است دستور فرمایید…)، هرگز صیغهٔ تکلیفی (مقتضی است/
    اقدام نمایید)؛ کارِ شخصِ ثالث ⇒ درخواستِ «صدورِ دستور» از بالادستی؛
    هم‌تراز/زیرمجموعه صیغهٔ خودشان. متنِ ناقض ⇒ پیشنهادِ اصلاح.
  - SYSTEM_PROMPT قاعدهٔ ۱۰ «تنوعِ واژگانی در نتیجهٔ نهایی» (سطحِ سیستم،
    برای همهٔ ابزارها).
- **[VERIFY]** تستِ characterization جدید
  (`test_prose_quality_rules_reach_the_model`) + ۳۰ تستِ ماژول‌های
  letter-assistant سبز؛ pytest کامل در گیت؛ type-check/build سبز (فرانت‌اند
  بدونِ تغییر — static همان است). فقط پرامپت؛ بدونِ تغییرِ منطقِ اعمال/گارد.

### 2026-07-13 — «هنرِ درخواستِ اداری»: زنجیرهٔ مجهول، عبارتِ توخالی، درخواستِ دوباره — v55

- **[OWNER REPORT]** (اسکرین‌شات، ادامهٔ v54) «چقدر مسخره و بچه‌گانه — یعنی چی
  به شرکت بیمه سرمد اعلام گردد ... مثل بچه‌ها که انشا می‌نویسن» — سه عیبِ
  مشخص در خروجی: زنجیرهٔ مجهولِ تودرتو («اعلام گردد که ... اقدام نمایند»)،
  عبارتِ توخالی («جهت انجام اقدامات لازم»)، و دو «خواهشمند است دستور
  فرمایید» پشتِ‌سرِهم برای یک مقصد.
- **[ROOT CAUSE]** قاعده‌های v54 انتزاعی بودند («تکرار نکن»، «صیغهٔ درست»)
  ولی «چطور جملهٔ درخواست ساخته می‌شود» را نمی‌گفتند؛ برای الگوهای جمله‌سازی،
  دستورِ انتزاعی ضعیف‌تر از مثالِ ساختاری (few-shot) عمل می‌کند.
- **[FIX — عمومی]** `letter_assistant.py` SYSTEM_PROMPT قاعدهٔ ۱۱ «هنرِ
  درخواستِ اداری» با ۴ قانونِ سخت + ۳ نمونهٔ بد⇐خوب (الگوی ساخت، نه متنِ
  آماده): (الف) خواسته‌های هم‌مقصد در یک جملهٔ درخواستِ منسجم — «خواهشمند
  است» حداکثر یک بار؛ (ب) زنجیرهٔ «اعلام گردد که ... اقدام نمایند» ممنوع ⇒
  «به [مرجع] ابلاغ گردد تا [کارِ مشخص] را انجام و نتیجه را اعلام نماید»؛
  (پ) عبارتِ توخالی («اقدامات لازم/مقتضی») ⇒ خودِ کارِ مشخص از بافتِ نامه؛
  (ت) هر درخواست: چه‌کسی/چه‌کاری/با چه هدفی/نتیجه به کجا.
- **[VERIFY]** سنجه‌های v55 به `test_prose_quality_rules_reach_the_model`
  اضافه شد (وجودِ قانون‌ها و نمونه‌های بد/خوب در SYSTEM_PROMPT)؛ ۳۰ تستِ
  ماژول سبز؛ pytest کامل در گیت؛ type-check/build سبز (فرانت‌اند بدونِ
  تغییر). فقط پرامپت؛ منطقِ اعمال/گارد دست نخورد.

### 2026-07-13 — نقطه‌گذاری/پاراگرافِ دستیار + زیرخطِ فارسی لای حروف — v56

- **[OWNER REPORT]** (دو اسکرین‌شات: نسخهٔ AI در برابر ویرایشِ دستیِ خودش)
  (۱) خروجی AI «بچه‌گانه، بدون رعایت چینش و پاراگراف‌ها و کاماها»: جمله‌های
  کش‌دارِ بدون نقطه («... صورت پذیرد همچنین شایان ذکر است ...»)، حشوِ
  دوتایی («اقدامات لازم مقتضی»)، پاراگراف‌های به‌هم‌چسبیده، ویرگول‌های
  جاافتاده. (۲) خطِ زیرِ (underline) جملهٔ فارسی «قاتیِ حروف» می‌افتد و
  دقیق زیرِ جمله نمی‌نشیند.
- **[ROOT CAUSE]** (۱) پرامپت‌ها دربارهٔ علائم سجاوندی و مرزِ پاراگراف در
  text_replace ساکت بودند — مدل چند خواسته را به یک جملهٔ بی‌نقطه می‌دوخت
  و پاراگراف‌ها را در replace ادغام می‌کرد. (۲) underline پیش‌فرضِ مرورگر
  از baseline لاتین رسم می‌شود که در خطِ عربی/فارسی از میانِ دُم‌ها و
  نقطه‌های زیرِ حروف می‌گذرد.
- **[FIX]**
  - `letter_assistant.py` SYSTEM_PROMPT قاعدهٔ ۱۲: هر جمله یک گزاره و با
    نقطه بسته شود (قبل از «همچنین/ضمناً/شایان ذکر است» نقطه یا «؛»)؛
    ویرگولِ فارسی بعد از قید آغازین و دو طرفِ معترضه؛ حشوِ انباشته ممنوع
    («لازم مقتضی» ⇒ یکی)؛ find/replace داخل مرزِ همان پاراگراف بماند —
    ادغامِ پاراگراف‌ها ممنوع.
  - `letter/page.tsx` CSS: زیرخط برای همهٔ محل‌های رندر نامه (bcell/psheet/
    measure/rich) با `text-underline-position:under` + `offset:1px` +
    `skip-ink:none` + ضخامتِ ۰٫۸px — خط پیوسته زیرِ حروف، نه از لایشان.
- **[VERIFY]** چکِ جدید در harness نامه (کامپیوتدِ زیرخط) + اسکرین‌شاتِ
  بصری (خط زیرِ «اخذ مجوزهای لازم…» تمیز زیرِ حروف)؛ letter ۸۳/۸۳،
  offer ۷۴/۷۴؛ سنجه‌های قاعدهٔ ۱۲ به تستِ characterization اضافه شد؛ ۳۰
  تستِ ماژول سبز؛ pytest کامل در گیت؛ type-check/build سبز؛ static سینک.
  مارکر → reflow-v56.

### 2026-07-13 — رونوشتِ چندتایی با تراز آویخته + مسیرِ برگرداندنِ فیلدِ حذف‌شده — v57

- **[OWNER REQUEST]** (۱) فیلد رونوشت را قبلاً برای نامه‌ای حذف کرده؛ راهِ
  برگرداندن؟ (۲) چند رونوشت: کلمهٔ «رونوشت» یک بار، گیرنده‌ها (ادارات/شعب)
  منظم زیرِ هم — اولی جلوی «رونوشت»، بعدی‌ها زیرِ نامِ اولین اداره.
- **[FINDING]** برگرداندن از قبل وجود داشت: در حالت «چیدمان»، وقتی فیلدِ
  حذف‌شده هست، منوی «بازگرداندنِ فیلدِ حذف‌شده…» در نوارِ ابزار ظاهر می‌شود
  (v44). فقط کشف‌پذیری کم بود — به مالک اعلام شد؛ بدون تغییر کد.
- **[FIX — رونوشت چندخطی]** `letter/page.tsx` + `wordExport.ts`:
  - چیدمانِ آویخته (hanging flex): برچسب «رونوشت :» ستونِ ثابت، گیرنده‌ها
    ستونِ رشدکننده — هر خطِ اضافه (<br>) دقیقاً زیرِ اولین گیرنده تراز
    می‌شود، نه زیرِ برچسب. در ویرایشگر، ورقِ چاپ/PDF و اندازه‌گیرِ بلوکِ
    پایانی (آینهٔ همان flex تا صفحه‌بندی ارتفاعِ چندخطی را درست بسنجد).
  - RichSpan حالتِ multiline گرفت (فقط رونوشت): Enter همیشه <br> درج
    می‌کند (نه <div> تو در تو)؛ placeholder راهنما: «Enter: گیرندهٔ بعدی».
  - خروجی Word: گیرنده‌های بعدی هر کدام پاراگرافِ خودش زیرِ خطِ «رونوشت»
    (helper جدید plainLines — مرزهای <br>/<div> حفظ می‌شود).
- **[VERIFY]** ۴ چکِ جدید در harness نامه (درجِ <br> با Enter، فعال‌بودنِ
  flex، نشستنِ ستونِ گیرنده‌ها بعد از برچسب، رشدِ دوخطی) — letter ۸۷/۸۷،
  offer ۷۴/۷۴؛ ۶۸۹ تستِ بک‌اند سبز؛ type-check/build سبز؛ static سینک.
  مارکر → reflow-v57.

### 2026-07-14 — استخراج و ثبتِ تفکیکیِ هویتِ اشخاص و جزئیاتِ کاملِ ملک — v58

- **[OWNER REQUEST]** «کد ملی مدیرها، شماره پاسپورت‌ها و تاریخ‌های صدور/انقضا،
  شماره بیمه‌نامه و کد رایانهٔ بیمه، اسم مالک ملک و کد ملی مالک/راهن، کد پستی،
  متراژ، آدرس دقیق، مبلغ ارزیابی/ترهین، شماره سند رهنی و پلاک ثبتی — حتماً
  استخراج و به تفکیک در دیتابیس ثبت شود.»
- **[GAP AUDIT]** موجود بود: آدرس/شهر/کشور، پلاک ثبتی، سند رهنی، ارزیابی+ارز،
  ترهین (تاریخ/مبلغ/ارز)، انقضای بیمه، مدارکِ کاملِ صاحبِ حساب. ستون داشت ولی
  استخراج نمی‌شد: مالک، شماره بیمه‌نامه، متراژ زمین/زیربنا، عمر بنا، zone،
  تاریخ آخرین ارزیابی. اصلاً ستون نداشت: کد ملی (مشتری/شریک/مدیر/ضامن/مالک)،
  سمتِ مدیر، پاسپورت و امارات‌آیدیِ شرکا با تاریخ‌ها، کد پستی ملک، کد رایانهٔ
  بیمه، تاریخ صدور بیمه‌نامه.
- **[FIX]**
  - مدل‌ها: `customer_profiles.national_id` (schema-driven ⇒ خودکار وارد
    پرامپت/persist)؛ `partners` + role/national_id/passport_no/issue/expiry/
    emirates_id_no/expiry؛ `guarantors.national_id`؛ `mortgaged_properties` +
    owner_national_id/postal_code/insurance_issue/insurance_computer_code.
    (استارتاپ self-heal ستون‌های جدید را با ADD COLUMN IF NOT EXISTS می‌سازد.)
  - پرامپتِ استخراج: اسکلتِ partners با هویتِ کامل + role (مدیر/شریک/امضادار —
    قاعدهٔ «PERSON IDENTITY NUMBERS MATTER»: کد ملی رقم‌به‌رقم، پاسپورت با
    صدور/انقضا)؛ guarantors + national_id؛ properties با مالک/کد ملی مالک/
    کد پستی/متراژها/عمر بنا/zone/تاریخ ارزیابی/بیمه‌نامه (شماره + کد رایانه +
    صدور/انقضا) + قاعدهٔ جست‌وجو در سند رهنی/گزارش ارزیابی/صفحات بیمه.
  - persist: نگاشتِ fill-empty همهٔ فیلدهای جدید (شریک/ضامن/ملک).
  - API: whitelistها و مدل‌های Pydantic (پارتنر/ملک/ضامن) برای خواندن/نوشتنِ
    دستی همین فیلدها به‌روز شد؛ خروجیِ list ژنریک است و خودکار برمی‌گرداند.
- **[VERIFY]** ۲ تستِ جدید: پرامپت همهٔ کلیدهای جدید را دارد + persist سرتاسری
  (کد ملی مشتری/مدیر/ضامن/مالک، پاسپورت مدیر با تاریخ‌ها، کد پستی، متراژها،
  بیمه‌نامه شماره/کد رایانه/صدور/انقضا) — ۲۸ تستِ ماژول سبز؛ pytest کامل در
  گیت؛ type-check/build سبز (فرانت‌اند بدون تغییر). بدون حذف/تغییرِ رفتارِ
  موجود — فقط ستون و فیلدِ افزوده.

### 2026-07-14 — نمایشِ فیلدهای هویتی/ملکیِ جدید در پروفایل مشتری — v59

- **[OWNER REQUEST]** «مطمئن شو همه‌چیز استخراج و ثبت می‌شود و بعداً از دیتابیس
  قابل فراخوانی است و در پروفایل‌ها در جای مناسب نشان داده می‌شود.»
- **[E2E AUDIT]** زنجیرهٔ استخراج ⇒ ستون (v58) و بازگشت از API تأیید شد
  (سریالایزرهای customer-full و CRM ژنریک‌اند و همهٔ ستون‌ها را برمی‌گردانند).
  حلقهٔ باقی‌مانده UI بود: فرم‌ها/جدول‌های پروفایل فیلدهای جدید را نه نمایش
  می‌دادند نه اجازهٔ ویرایش می‌دادند.
- **[FIX]** `customer-detail/page.tsx` + `routers/crm.py`:
  - پنل KYC: کارتِ جدید «National ID (کد ملی)» برای صاحبِ حساب (الگوی
    KYC_DOCS با expiryKey خالی — کارت‌های بدونِ انقضا حالا پشتیبانی می‌شوند)؛
    `ProfileUpdate.national_id` هم برای ذخیرهٔ دستی اضافه شد.
  - املاک رهنی: فرمِ افزودن + جدول حالا مالک/راهن، کد ملی مالک، کد پستی،
    zone، متراژ زمین/زیربنا، بیمه‌نامه (شماره · کد رایانه · صدور ← انقضا)
    را می‌گیرد و نشان می‌دهد.
  - شرکا/مدیران: فرم و جدول با سمت (role)، کد ملی، پاسپورت (شماره · صدور ←
    انقضا) و امارات‌آیدی (شماره · انقضا).
  - ضامن‌ها: ورودی و ستونِ کد ملی.
- **[VERIFY]** pytest کامل + type-check/build سبز؛ static سینک و هم‌کامیت.
  تغییرِ UI صرفاً افزایشی — هیچ ستون/قابلیتِ قبلی حذف نشد.

### 2026-07-14 — موضوعِ نامه: جا شدن در یک خط، جداکنندهٔ زیرِ موضوع، ارقامِ فارسی — v60

- **[OWNER REPORT]** (اسکرین‌شات) سه مشکلِ فیلد موضوع: (۱) وقتی موضوع دوخطی
  می‌شود، خطِ جداکنندهٔ موضوع/متن روی کلماتِ خطِ دوم می‌افتد؛ (۲) موضوعِ
  کمی‌بلند باید بدونِ ریزشدنِ افراطی خودش را در یک خط جا دهد؛ (۳) ارقامِ
  شماره‌حساب در موضوع پیش‌فرض با رقمِ لاتین نوشته می‌شد (دستی فارسی کرده بود).
- **[FIX]** `letter/page.tsx` (همان effect اندازه‌گیرِ موضوع):
  - fit-to-one-line: عرضِ طبیعیِ موضوع اندازه گرفته می‌شود؛ اگر از عرضِ باکس
    بیشتر بود، فونت به‌نسبت کوچک می‌شود ولی نه کمتر از ۷۵٪ سایزِ تنظیم‌شده
    (کفِ خوانایی) — فقط اگر با همان کف هم جا نشد، می‌شکند به خطِ دوم.
  - جداکننده: ارتفاعِ رندرشدهٔ موضوع (با فونتِ fit) اندازه گرفته می‌شود و اگر
    پایینِ موضوع از y جداکننده رد شود، جداکننده به زیرِ متن سُر می‌خورد
    (فقط رندر؛ چیدمانِ ذخیره‌شده و حالتِ design دست نمی‌خورد — الگوی closingShift).
  - ارقام فارسی: ورودیِ موضوع از میانِ faDigitsHtml می‌گذرد — تبدیل ۰-۹ی
    ASCII و ارقام عربی به فارسی فقط در text nodeها (attrs/تگ‌ها امن)؛ caret
    حفظ می‌شود (طولِ رشته تغییر نمی‌کند). فیلدهای دیگر (تاریخ/شماره‌نامه که
    عمداً لاتین‌اند) دست نمی‌خورند.
- **[VERIFY]** ۴ چکِ جدید در harness نامه (تبدیل ارقام؛ کوچک‌شدنِ فونتِ موضوعِ
  بلند؛ تک‌خط ماندن؛ نشستنِ جداکننده زیرِ موضوعِ ناگزیر-دوخطی) — letter
  ۹۱/۹۱، offer ۷۴/۷۴؛ pytest کامل در گیت؛ type-check/build سبز؛ static سینک.
  مارکر → reflow-v60.

### 2026-07-14 — دستیار v61: امضاکنندهٔ درست، بررسیِ کاملِ نامه+پیوست‌ها، پایگاه دانشِ خودرشد — v61

- **[OWNER REQUEST — ۴ خواسته]** (۱) دستیار درستیِ «امضاکننده» را بسنجد:
  گیرندهٔ بیرون از مجموعهٔ بانک صادرات منطقه خلیج فارس ⇒ «سرپرستی منطقه خلیج
  فارس»؛ مکاتبهٔ داخلی بین شعب/دوایر ⇒ «دایره تسهیلات اعطایی» (استثنا ممکن).
  (۲) وقتی نامه انعکاسِ نامهٔ دیگری است، انطباقِ متن با پیوست‌ها از همه جهت
  بررسی شود (شماره‌نامه‌ها، ارجاع‌ها، مفهوم). (۳) زیرساختِ بخش آموزشی
  (پایگاه دانش) طوری باشد که دستیار محتوای عمومی/آموزشیِ نامه‌ها و پیوست‌ها
  را منظم، گروه‌بندی‌شده ذیل عناوین و با ارجاعِ دقیق ثبت و فهرست را همان
  لحظه به‌روز کند + برچسبِ تیکِ استخراج، «پایگاه دانش» را هم ذکر کند.
  (۴) تیکِ جدیدِ پیش‌فرض‌فعال: بررسیِ کاملِ نامه و همهٔ پیوست‌ها (درون‌صفحه‌ای
  و فایل ضمیمه) با پایگاه‌داده — مستقل از تیکِ «استخراجِ کامل از پیوست‌ها».
- **[INFRA AUDIT]** پایگاه دانش قبلاً فقط یک صفحهٔ استاتیک بود
  (knowledge/content.ts) — هیچ مدل/API/مسیر ثبتی نداشت؛ متنِ پیوست‌ها هم به
  analyze نمی‌رسید (استخراجِ پیوست جدا و صرفاً به مقصدِ DB بود).
- **[BUILD]**
  - قاعدهٔ ۱۳ SYSTEM_PROMPT: تشخیصِ بیرونی/داخلی از فیلدهای گیرنده ⇒ پیشنهادِ
    set_field برای sender با دلیل (به‌خاطر استثناها فقط پیشنهاد، نه اجبار).
  - ابزارِ جدید full_check «بررسیِ کاملِ نامه و پیوست‌ها با پایگاه‌داده»
    (پیش‌فرض فعال): همهٔ فیلدها + متن + جدول‌های پیوستِ درون‌نامه + متنِ
    فایل‌های ضمیمه در برابر حقایق DB و در برابر همدیگر؛ + انطباقِ انعکاس
    (شماره/تاریخ/فرستندهٔ نامهٔ ارجاع‌شده و کاملْ‌رساندنِ مفهوم). endpoint
    جدید `POST /letter-ai/attachment-text/{id}` متنِ هر پیوست را بدونِ هیچ
    ثبتی می‌دهد (Excel/CSV/Word/متن قطعی؛ PDF/تصویر یک فراخوانِ رونویسیِ
    محدود) — فرانت پیوست‌ها را دانه‌دانه می‌خواند و با analyze می‌فرستد.
    مستقل از تیکِ استخراج و بدونِ تداخل (هر دو از همان انتخابگرِ پیوست).
  - پایگاه دانش: مدل‌های kb_topics/kb_entries + سرویسِ kb_store (گروه‌بندی
    با نرمال‌سازیِ عنوان: ي/ك/نیم‌فاصله/اعراب؛ dedupِ محتوا؛ ارجاعِ دقیقِ
    هر entry) + روتر `/api/knowledge` (فهرست/دسته‌ها زنده از خودِ داده) +
    opِ جدید kb_write در دستیار (staged و قابلِ تیک؛ apply-db ثبت می‌کند) +
    برچسبِ db_extract: «... و پایگاه دانش». صفحهٔ دانش‌نامه بخشِ پویای
    «مطالبِ برداشت‌شده از نامه‌ها» را با فهرستِ خودکار زیرِ بخشِ استاتیک
    نشان می‌دهد (استاتیک حذف نشد).
- **[VERIFY]** ۶ تستِ KB (گروه‌بندی/dedup/فهرستِ زنده/apply-db) + سنجه‌های
  v61 در پرامپت + تستِ attachment_text قطعی — ۳۶+۲۹ تستِ ماژول‌ها سبز؛
  letter ۹۵/۹۵ (۴ چکِ جدید: تیکِ full_check پیش‌فرض‌فعال، برچسبِ KB)،
  offer ۷۴/۷۴؛ pytest کامل در گیت؛ type-check/build سبز؛ static سینک.
  مارکر → reflow-v61.

### 2026-07-14 — بازرسیِ کاملِ v61 به‌درخواستِ مالک + سه اصلاح — v61.1

- **[RE-VERIFY]** کلِ v61 دوباره جزءبه‌جزء با کد و تستِ اجرایی بازرسی شد
  (بدونِ حدس): اعتبارسنجیِ set_field برای sender (فیلد در SCALAR_FIELDS ✓)؛
  فهرستِ ابزارها مستقیم از TOOLS (چیزی جا نمی‌ماند ✓)؛ استقلالِ تیک‌ها ✓؛
  ثبت/گروه‌بندی/ضدتکرارِ KB با تستِ endpoint ✓؛ رندرِ صفحهٔ دانش‌نامه با
  چکِ مرورگریِ جدید (۷/۷: موضوعِ پویا، محتوا، ارجاعِ دقیق، فهرستِ زنده،
  دسته، حفظِ بخشِ استاتیک، دکمهٔ حذف برای admin).
- **[سه یافته و اصلاح]**
  1. متنِ پیوست‌ها فقط با تیکِ full_check به مدل می‌رسید؛ خواستهٔ مالک
     برداشتِ آموزشی «از دلِ پیوست‌ها» با تیکِ استخراج بود ⇒ حالا با
     db_extract هم می‌رود (بک‌اند + فرانت + انتخابگرِ پیوست).
  2. ردیفِ kb_write در فهرستِ بازبینی فقط عنوان داشت — متنِ واقعی که ثبت
     می‌شود دیده نمی‌شد ⇒ حالا موضوع/دسته/متنِ کامل/منبع در همان ردیف.
  3. نقشِ ناموجودِ 'manager' در شرطِ دکمهٔ حذفِ KB ⇒ admin/editor.
- **[VERIFY]** تستِ سرتاسریِ جدیدِ analyze با مدلِ شبیه‌سازی‌شده
  (test_analyze_full_pipeline_v61): رسیدنِ متنِ پیوست با db_extractِ تنها،
  بقای set_fieldِ sender از اعتبارسنجی، stage شدنِ kb_write — سبز؛ ۷ تستِ
  KB؛ letter ۹۵/۹۵؛ offer ۷۴/۷۴؛ چکِ مرورگریِ دانش‌نامه ۷/۷؛ pytest کامل
  در گیت؛ type-check/build سبز؛ static سینک.

### 2026-07-16 — ادغامِ دو کارتِ نامه در صفحهٔ Forms (بدونِ حذفِ هیچ قابلیتی) — v62

- **[OWNER REQUEST]** دو کارتِ «Official Letter» و «General Letters» مبهم‌اند؛
  ذیل یک کارت دربیایند — با احتیاطِ کامل و بدونِ حذفِ قابلیت.
- **[CHANGE]** `forms/page.tsx`: الگوی جدیدِ «کارتِ چند-ورودی» (فیلد اختیاری
  `links` در FormDef): یک کارتِ «Official Letter» با توضیحِ ادغامیِ هر دو و
  دو دکمهٔ ورود — «نامهٔ حساب/مشتری» (`/letter`) و «نامهٔ عمومی (بدونِ حساب)»
  (`/letter?general=1`). هر دو مسیر **عیناً** همان مسیرهای دو کارتِ قبلی‌اند؛
  هیچ صفحه/رفتاری حذف یا تغییر نکرد — فقط ارائهٔ صفحهٔ Forms.
- **[VERIFY]** چکِ مرورگریِ جدید (forms_check) ۱۱/۱۱: تک‌کارت‌شدن، هر دو
  دکمه، href دقیقِ هر دو مسیر (با نرمال‌سازیِ اسلشِ خودِ Next مثل قبل)،
  سالم‌ماندنِ ۴ کارتِ دیگر، بازشدنِ واقعیِ ویرایشگرِ نامه از ورودیِ عمومی و
  ماندنِ `general=1` در URL. (خطای میانی از سرورِ سادهٔ خودِ چک بود —
  ریدایرکتِ اسلش را نداشت؛ production با StaticFiles(html=True) همان
  ریدایرکت را با حفظِ query دارد، مثل قبل از این تغییر.) pytest کامل +
  type-check/build سبز؛ static سینک.

### 2026-07-16 — سازندهٔ پیوست: پر کردنِ دقیقِ «قالب/نمونهٔ» داده‌شده در هر فرمتی — v63

- **[OWNER REQUEST]** بتوانم فایلِ نمونه/قالبی که مرجعِ دیگری داده (مثلاً جدولِ
  خالی با عنوان‌ها و ستون‌های مشخص، در هر نوع فایلی) به سازندهٔ پیوست بدهم؛
  خروجی دقیقاً به همان شکل از دیتابیس پر شود. سه حالت: فقط قالب ⇒ از خودِ
  قالب بفهمد؛ فقط شرح ⇒ مثل قبل؛ قالب + شرح ⇒ شرح روی همان قالب اعمال شود
  (خواسته‌های اضافه هم اجرا شوند).
- **[BUILD]**
  - endpoint جدید `POST /letter-ai/template-text` (multipart): متنِ فایلِ
    قالبِ انتخابی از سیستمِ کاربر — قطعی برای Excel/CSV/Word/متن، یک
    رونویسیِ محدود برای PDF/تصویر (بازاستفاده از attachment_text v61)؛
    هیچ‌چیز ذخیره نمی‌شود؛ سقف ۱۵MB.
  - `generate-attachment`: فیلدهای اختیاریِ template_text/template_name؛
    instruction اختیاری شد ولی گاردِ سرور «حداقل یکی از شرح/قالب» (422)؛
    مسیرِ بدونِ قالب بیت‌به‌بیت مثل قبل.
  - پرامپت: بخشِ «قالب/نمونهٔ داده‌شده توسط کاربر» + قاعدهٔ ۹ سیستم:
    بازتولیدِ عینیِ ساختار/سرستون‌ها/ترتیب/عنوان‌ها، پرکردنِ داده فقط از
    حقایقِ DB، سطرهای نمونهٔ داخل قالب داده نیستند، خواسته‌های اضافهٔ شرح
    روی همان قالب، حالتِ بدونِ شرح صریح هندل شد؛ در فازِ need_data هم قالب
    حفظ می‌شود.
  - UI: دکمهٔ «قالب/نمونه (اختیاری)» + نمایشِ نامِ فایل + حذفِ قالب +
    placeholder وفقی؛ اعتبارسنجی «شرح یا قالب».
- **[VERIFY]** ۳ تستِ جدید (بخشِ قالب و قواعدش در پرامپت + حالتِ فقط-قالب
  end-to-end با مدلِ شبیه‌سازی‌شده + گاردِ 422 + خواندنِ xlsx واقعی در
  template-text) — ۱۴ تستِ ماژول سبز؛ letter ۱۰۱/۱۰۱ (۶ چکِ جدیدِ UI)،
  offer ۷۴/۷۴؛ pytest کامل در گیت؛ type-check/build سبز؛ static سینک.
  مارکر → reflow-v63.

### 2026-07-16 — تاریخچهٔ کاملِ رویدادهای ملک (چند ارزیابی، ترهین/مجدد/مازاد، فک رهن) — v64

- **[OWNER QUESTION → GAP]** «آیا چند تاریخ ارزیابی برای یک ملک، تاریخ‌های
  ترهین/ترهین مجدد و مازاد و فک رهن کامل استخراج و ثبت و نمایش و در
  فراخوانی‌های AI پوشش داده می‌شود؟» — پاسخِ صادقانه: نه. هر ملک فقط «یک»
  valuation/last_valuation_date و «یک» mortgage_date داشت (fill-empty یعنی
  ارزیابی‌های بعدی گم می‌شدند)، فک رهن اصلاً جایی نداشت، و املاک/سپرده‌ها/
  شرکا در facts دستیارِ نامه و full_check و ژنراتور اصلاً نبودند.
- **[BUILD]**
  - مدلِ جدید `property_events`: property_id/account_no، event_type
    (valuation/mortgage/remortgage/additional_mortgage/release/insurance/
    other)، event_date (متن — جلالی یا میلادی همان‌طور که چاپ شده)، amount/
    currency/remarks/منبع/سازنده. ستون‌های تکِ قبلی «آخرین» مقدار را نگه
    می‌دارند؛ تایم‌لاین کامل این‌جاست. (create_all/self-heal می‌سازد؛
    مستقل از ایران/امارات/هرجا — کشور روی خودِ ملک است.)
  - استخراج: اسکلتِ properties فیلدِ «events» گرفت + قاعدهٔ صریح «PROPERTY
    EVENT HISTORY»: هر ارزیابی با تاریخ و مبلغِ خودش (همه، نه فقط آخری)،
    ترهین/مجدد/مازاد، فک رهن، صدور/تمدید بیمه — و آخرین ارزیابی همچنان در
    فیلدهای بالایی.
  - persist: append-with-dedup به کلید (type, date, amount) در هر ملک —
    ایمپورتِ دوباره هیچ ردیفی را دوبله نمی‌کند؛ نوعِ ناشناخته با تاریخ/مبلغ
    ⇒ other؛ رویدادِ بی‌تاریخ-بی‌مبلغ حذف.
  - فراخوانیِ AI (بند ۴ سؤال): `_gather_facts` حالا املاک (+تایم‌لاین)،
    سپرده‌های ثابت و شرکا را هم می‌دهد — سریال‌سازی «schema-driven» با
    `_row_facts` (همهٔ ستون‌های غیرخالی) پس هر ستونِ فعلی و «آینده» خودکار
    در دسترسِ دستیار نامه، full_check و ژنراتورِ پیوست است.
  - API/UI: endpointهای افزودن/حذفِ رویداد (crm) + بخشِ «تاریخچهٔ رویدادهای
    املاک» در پروفایل مشتری (جدولِ رویدادها با نوعِ فارسی/تاریخ/مبلغ/منبع +
    فرمِ افزودنِ دستی)؛ property_events در نمای کاملِ مشتری.
- **[VERIFY]** ۳ تستِ جدید: قاعده‌ها در پرامپت؛ persist سرتاسری (۳ ارزیابی +
  ترهین + مجدد + فک رهن ⇒ ردیف‌های جدا؛ ایمپورتِ مجدد ⇒ صفر دوبله)؛
  build_facts با تاریخچه و ستون‌های schema-driven — ماژول ۳۲/۳۲ سبز؛
  letter ۱۰۱/۱۰۱، offer ۷۴/۷۴؛ pytest کامل در گیت؛ type-check/build سبز؛
  static سینک. مارکر همان reflow-v63 (صفحهٔ نامه تغییری نکرد).
- **[FOLLOW-UP همان تسک]** حین گیت، build فرانت‌اند بی‌صدا شکست
  (`>/dev/null` خطای ESLint را بلعید و static از خروجیِ کهنه سینک شد؛
  کامیتِ اول بدونِ خروجیِ build رفت): useState تایم‌لاین بعد از early
  returnِ صفحه بود (نقضِ rules-of-hooks). state به بالای کامپوننت منتقل و
  build واقعی سبز شد؛ درس: در گیت، خروجیِ build را هرگز به /dev/null نده —
  «BUILD_OK» باید دیده شود، و staticِ سینک‌شده باید diff داشته باشد وگرنه
  از خروجیِ کهنه کپی شده.

## 2026-07-16 — v65: «فایل‌های منبعِ داده» برای ژنراتورِ پیوست (کنارِ قالبِ نمونه)

- **[OWNER REQUEST]** کنارِ دکمهٔ «قالب/نمونه» در «ساختِ پیوست با هوش مصنوعی»
  گزینه‌ای برای پیوست‌کردنِ «انواع فایل‌ها» باشد — هر فرمت، هر تعداد، قابلِ
  افزودن در هر زمان — که هوش مصنوعی «از محتوای آن‌ها» + دستورِ باکس پیوست را
  بسازد؛ اگر قالبِ نمونه هم بود، داده‌های فایل‌های منبع باید دقیقاً در همان
  قالب بنشینند و هر خواستهٔ اضافیِ دستور هم حتماً اجرا شود؛ بدونِ قالب،
  فقط بر اساسِ فایل‌های منبع + دستور.
- **[BUILD]**
  - بک‌اند (`letter_attachment_generate.py`): قاعدهٔ ۱ اصلاح شد (دادهٔ واقعی از
    DB **یا فایل‌های منبعِ کاربر یا خودِ دستور**) + قاعدهٔ جدیدِ ۱۰: «قالب فقط
    شکل، فایل‌های منبع فقط داده؛ تعارضِ منبع↔DB ⇒ مقدارِ منبع + اعلام در
    warnings». `build_prompt` بخشِ «فایل‌های منبعِ داده» گرفت (سقف ۸ فایل ×
    ۲۰هزار نویسه، قبل از بخشِ قالب؛ فایل‌های بی‌متن حذف).
  - روتر (`letter_ai.py`): `GenerateAttachmentRequest.source_files`
    (لیستِ {name,text}) — به هر دو فراخوانیِ build_prompt (دورِ اول و دورِ
    need_data) پاس می‌شود. گاردِ «حداقل شرح یا قالب» سرِ جایش ماند (منبعِ
    تنها ⇒ 422 با پیامِ روشن).
  - فرانت‌اند (`letter/page.tsx` + `api.ts`): دکمهٔ «🗂 فایل‌های منبعِ داده
    (اختیاری)» با multiple + append (dedup به name|size)، ردیفِ chip با ✕
    تکی و «پاک‌کردنِ همه»، شمارندهٔ فارسی؛ متنِ هر فایل با همان endpointِ
    بدون-ذخیرهٔ `/letter-ai/template-text` (فایل‌به‌فایل، با پیشرفت) گرفته
    می‌شود؛ فایلِ ناخوانا ⇒ قطعِ کامل با toast («فایلِ منبع «X» خوانده
    نشد»)، نه ادامهٔ خاموش. hint بازنویسی شد (قالب=شکل، منبع=داده). موفقیت ⇒
    ریستِ قالب+منبع‌ها. مارکر ⇒ `build: reflow-v65`.
- **[VERIFY]** ۲ تستِ جدید (بخشِ منبع‌ها در پرامپت + مسیرِ سرتاسری تا مدلِ
  stub شده) — ماژول ۱۶/۱۶؛ pytest کامل **۷۰۷ passed** (۷۰۵+۲)؛ harness نامه
  **۱۰۷/۱۰۷** (۶ چکِ جدیدِ §38: وجودِ picker، دو chip، افزودنِ سومی با حفظِ
  قبلی‌ها، شمارندهٔ ۳، حذفِ تکی، پاک‌کردنِ همه) — یک انتظارِ harness برای
  hintِ v63 به متنِ جدید به‌روز شد (تغییرِ عمدی)؛ offer ۷۴/۷۴؛
  type-check/build سبز با خروجیِ visible؛ static سینک از ریشهٔ repo با diff.
- **[LESSON]** experiences/ai-generated-artifacts…: «منبع=داده، قالب=شکل» +
  استفادهٔ مجدد از endpointِ فقط-متن + قطعِ صریح روی فایلِ ناخوانا.

## 2026-07-16 — v66: دسترسیِ AI به همهٔ لاگ‌ها (لاگِ کلی، لاگِ حساب، لاگِ کارها/روزانه)

- **[OWNER QUESTION → GAP]** «هرجا هوش مصنوعی به دیتابیس دسترسی دارد باید به
  همهٔ لاگ‌ها هم دسترسی داشته باشد تا اگر در دستور چیزی بود که باید از
  لاگ‌ها استخراج شود بتواند» — پاسخِ صادقانه: نبود. هیچ‌کدام از مسیرهای AI
  (دستیار نامه با همهٔ ابزارها، بررسیِ کامل، پرامپت‌های درون‌متنی، ژنراتورِ
  پیوست) نه `AuditLog` را می‌دیدند نه `JournalEntry` را.
- **[BUILD]** هر دو سطح، از همان گلوگاهِ واحد:
  - **لاگِ حسابِ نامه در facts** — `_gather_facts` (تنها نقطهٔ ساختِ facts؛
    analyze + generate هر دو از همین می‌گذرند) حالا ۴۰ ردیفِ آخرِ audit
    (`account_activity_log`: زمان/کاربر/عملیات/موجودیت/شرحِ ≤300 نویسه،
    جدیدترین اول) و ۴۰ ردیفِ آخرِ `journal_entries` (`journal_log`،
    schema-driven با `_row_facts`) را هم بار می‌کند — best-effort با
    try/except تا جدولِ غایب analyze را نکشد. سرصفحهٔ بخشِ حقایق در پرامپت
    معنی این دو کلید را صریح می‌گوید.
  - **لاگِ کلی/سراسری در need_data ژنراتور** — دو datasetِ جدید در
    `DATASETS`: «audit_logs» و «journal_entries» (کوئریِ محدودِ ۲۰۰۰ ردیفِ
    جدیدترین قبل از فیلتر + سقفِ معمولِ ۳۰۰ با warning؛ فیلترِ شعبه از
    طریق حسابِ ردیف، با همان قراردادِ «بی‌تطبیق ⇒ فهرستِ کامل + هشدار»؛
    نامِ مشتری از حساب join می‌شود). کاتالوگ و parse_need_data خودکار از
    همین دیکشنری تغذیه می‌شوند.
- **[VERIFY]** ۳ تستِ جدید: facts با لاگ‌ها (سقفِ ۴۰، نرمال‌سازیِ فاصله،
  برشِ ۳۰۰، غیابِ کلید وقتی لاگی نیست)، توضیحِ کلیدها در پرامپت، و
  fetch_datasets لاگ‌ها (کاتالوگ + parser + فیلترِ شعبه + join نامِ مشتری) —
  ماژول‌های letter ۵۶/۵۶؛ pytest کامل در گیت؛ type-check/build سبز (تغییرِ
  فرانت‌اند نبود؛ خروجیِ rebuild فقط hash بود و کنار گذاشته شد — مارکر
  همان reflow-v65).

## 2026-07-16 — v67: جستجوی نامحدودِ لاگ‌ها برای AI (به‌جای فقط ۴۰ تای آخر)

- **[OWNER REQUEST]** «لاگ‌ها نباید محدود به ۴۰ تای آخر باشد و باید بتواند
  نامحدود جستجو کند.»
- **[BUILD]** سرویسِ جدید `services/log_search.py`: جستجوی فیلترپذیر
  (متن/کاربر/عملیات/حساب/بازهٔ تاریخِ شمول‌دار) روی «کلِ» جدول‌های
  audit_logs و journal_entries — بدونِ هیچ پیش‌محدودیتِ newest-N در اسکن؛
  فقط ردیف‌های «برگردانده‌شده» سقفِ ایمنیِ توکن دارند (۵۰۰) و «تعدادِ
  واقعیِ» یافته‌ها همیشه کنارش گزارش می‌شود + warning «فیلتر را تنگ‌تر کن» —
  برش هرگز خاموش نیست. ورودیِ مدل با `sanitize_query` مهار می‌شود (کلیدهای
  ناشناس حذف، طول‌ها بریده، همه bound-parameter).
  - **دستیارِ نامه — پروتکلِ need_logs (قاعدهٔ ۱۵):** برشِ ۴۰تاییِ facts
    فقط زمینهٔ آماده است؛ مدل هر وقت لازم داشت به‌جای خروجی، یک‌بار
    `{"need_logs": {...}}` برمی‌گرداند، سرور روی کلِ لاگ‌ها جستجو می‌کند و
    با «نتایجِ جستجوی لاگ‌ها» دورِ دوم اجرا می‌شود (الگوی need_data ژنراتور؛
    فقط یک دور). parse با همان `_loose_json` مقاوم.
  - **ژنراتورِ پیوست:** need_data حالا `logs_filter` اختیاری می‌گیرد
    (sanitize‌شده در parse_need_data) و واکشیِ datasetهای لاگ به‌جای
    «۲۰۰۰ ردیفِ جدیدترِ» v66 از همین جستجوی کلِ جدول می‌گذرد — ردیفِ چند
    سالِ قبل هم با فیلترِ درست قابلِ دسترسی است. فیلترِ شعبه به همان
    قراردادِ قبلی پس از جستجو اعمال می‌شود و غنی‌سازیِ نامِ مشتری حفظ شد.
- **[VERIFY]** ۶ تستِ جدید: sanitize (کلیدِ ناشناس/برش/scope پیش‌فرض)،
  فیلترها + شمارِ واقعی + ترتیبِ جدیدترین-اول، سقفِ ارسال با اعلامِ total،
  parse_need_logs + قاعدهٔ ۱۵ + آگهیِ پروتکل در پرامپت، دورِ دومِ analyze
  سرتاسری (ردیفِ قدیمیِ ۲۰۲۵ به پرامپتِ دورِ دوم می‌رسد)، و logs_filter
  ژنراتور (ردیفِ ۲۰۲۴ با فیلترِ متن برمی‌گردد). pytest کامل در گیت؛
  type-check/build سبز (تغییرِ فرانت‌اند نبود — مارکر همان reflow-v65).

## 2026-07-16 — v68: جدول‌سازی با دستورِ دستیار (table_insert) + پیوست‌ها منبعِ پرکردنِ جدول

- **[OWNER QUESTION → وضعِ موجود]** چهار خواسته دربارهٔ جدول‌ها در باکسِ دستیار:
  (۱) ساختِ جدولِ جدید فقط با دستور — **نبود**؛ (۲) پرکردنِ جدول‌های موجودِ متن
  و پیوست — **بود** (v22–v25: انتخاب‌گرِ per-table شاملِ جدول‌های پیوستی،
  table_replace به مقصدِ درست)؛ (۳) تغییرِ ساختارِ جدولِ موجود — **بود** از
  طریقِ table_replace («ساختاری و محتوایی هر دو»)، فقط راهنمای تیکِ جداول
  هنوز می‌گفت «ساختاری صرفاً note» که ابهام داشت؛ (۴) استفاده از محتوای
  پیوست‌ها برای پرکردنِ جدول وقتی full_check فعال است — **ممنوع بود**
  (تنها منبعِ مجاز: حقایقِ پایگاه‌داده).
- **[BUILD]**
  - **op جدید `table_insert`** (بک‌اند + فرانت‌اند): ساختِ جدولِ کاملاً نو
    بدونِ نیاز به جدولِ ازپیش‌موجود/انتخاب‌شده — کلیدها: html (از همان
    sanitizer سخت‌گیرِ table_replace می‌گذرد؛ تنها مسیرِ ورودِ HTML مدل به
    نامه)، table_title (اختیاری، برشِ ۱۲۰)، placement: body (داخلِ متن، با
    همان مارکاپِ دکمهٔ دستیِ «جدول») یا attachment (صفحهٔ پیوستِ جدا بعد از
    صفحهٔ آخر — AttTable جدید + پیوست=دارد؛ صفحهٔ پیوست خودش landscape/فونت
    را مدیریت می‌کند). قواعدِ ساخت با هر دستورِ غیرخالی به مدل می‌رسد
    (نیازی به جدول/تیکِ خاص نیست)؛ اعتبارسنجی placement ناشناس ⇒ body؛
    پیش‌نمایشِ زندهٔ همان مودالِ بازبینی برای insert هم فعال شد.
  - **پیوست‌ها منبعِ مجازِ داده برای جدول‌ها:** بخشِ «محتوای پیوست‌های نامه»
    (که با full_check/db_extract ارسال می‌شود — full_check پیش‌فرض‌فعال) حالا
    صریحاً منبعِ مجازِ پرکردن/ساختنِ جدول است: مقدارِ دقیق + ذکرِ منبع در
    note؛ ترتیبِ جستجو: حقایقِ DB → محتوای پیوست‌ها → خودِ دستور؛ ناموجود
    ⇒ «—» + note؛ ساختنِ داده همچنان ممنوع.
  - **رفعِ ابهامِ تیکِ جداول:** برچسب ⇒ «بررسی و اصلاح جداول (ساخت/پر کردن/
    بازطراحی با دستور)» و راهنما بازنویسی شد: پیش‌فرض (بی‌دستور) = گزارشِ
    ناهماهنگی + اصلاحِ جزئی؛ با جدولِ انتخاب‌شده = بازطراحیِ کامل با
    table_replace؛ جدولِ نو = table_insert.
- **[FOUND en route]** تستِ smoke ریشه (tests/test_all_existing_features.py)
  از ۱۰ ژوئن (حذفِ عمدیِ صفحهٔ فهرستِ offer-letters، قبل از این چرخه) کهنه
  مانده بود و چون گیت/CI فقط بک‌اند را اجرا می‌کنند دیده نشده بود — انتظار
  به مسیرِ زندهٔ offer-letter/page.tsx به‌روز شد؛ ریشه ۱۹/۱۹ سبز.
- **[VERIFY]** ۲ تستِ جدید (اعتبارسنجی/sanitizer با tables_count=0،
  placement پیش‌فرض و ناشناس، برشِ عنوان، حذفِ غیرجدول؛ سیم‌کشیِ قواعد در
  SYSTEM_PROMPT/راهنمای ابزار/پرامپت با و بدونِ دستور) + ۲ تستِ قدیمی به
  قراردادِ جدیدِ منابعِ داده به‌روز شدند (تغییرِ عمدی) — ماژول ۲۹/۲۹؛
  harness نامه ۱۰۷/۱۰۷ و offer ۷۴/۷۴ روی buildِ جدید؛ pytest کامل در گیت؛
  type-check/build سبز؛ static سینک. **مارکر ⇒ reflow-v68.**

## 2026-07-16 — v68.1: بازبینیِ مو-به-موی مالک روی v62…v68 + یک فیکسِ پیشگیرانه

- **[OWNER REQUEST]** بازبینیِ کاملِ همهٔ کارهای امروز، بدونِ حدس.
- **[VERIFY — همه با شواهد]** تاریخچهٔ main (۷ مرجِ امروز) و درختِ تمیز؛
  مارکرِ reflow-v68 هم‌زمان در سورس/out/static و chunkِ دیپلوی‌شده (شامل
  table_insert و picker منبع‌ها)؛ دیفِ کاملِ ef3fa5ad..HEAD خط‌به‌خط
  بازخوانی شد (روتر letter_ai، ژنراتور، دستیار، log_search، صفحهٔ نامه،
  api.ts)؛ v62 (forms ۱۱/۱۱)، v63/v65 (قواعد ۹/۱۰ + گاردها + هر دو دورِ
  build_prompt)، v64 (دست‌نخورده از گیتِ سبزش)، v66/v67/v68 — تست‌ها:
  ماژول‌های letter+log_search ۹۶/۹۶، suite کامل ۷۱۸، ریشه ۱۹/۱۹، letter
  ۱۰۷/۱۰۷، offer ۷۴/۷۴.
- **[FIX (پیشگیرانه، از بازخوانیِ دیف)]** table_insert با placement=body
  جدولِ نو را «مستقیم» به انتهای body می‌چسباند؛ اگر انتهای متن خودش
  جدولی با همان سرستون بود، mergeAdjacentTables (ترمیمِ جدول‌های شکسته
  بینِ صفحات) دو جدول را یکی می‌کرد و جدولِ جدید «بلعیده» می‌شد. یک خطِ
  خالیِ جداکننده قبل از جدولِ درج‌شده اضافه شد — مجاورتِ مستقیم دیگر رخ
  نمی‌دهد. (بک‌اند بدونِ تغییر؛ فقط صفحهٔ نامه + rebuild/sync استاتیک.)

## 2026-07-16 — v69: «داده‌ای استخراج نشد» ریشه‌یابی شد + دوختنِ پاراگراف‌های پراکنده (paragraph_merge)

- **[OWNER REPORT ۱]** استخراج از سه پیوست (گزارشِ تجمیعیِ تسهیلاتِ شعب،
  631IN.pdf، Yearly lending report.xlsx) با «خطای مدل: داده‌ای استخراج نشد»
  شکست خورد. **ریشه:** مسیرِ استخراجِ پیوست «حساب‌محور» است — فقط واقعیت‌های
  متصل به شمارهٔ حساب را ثبت می‌کند؛ آن فایل‌ها (گزارشِ سطحِ شعبه/نامهٔ مرجع)
  اصلاً ردیفِ حساب‌محور ندارند؛ همهٔ فراخوان‌های مدل موفق بود (chunk_errors
  خالی) ولی خروجیِ قابلِ ثبت صفر و پیامش گمراه‌کننده («خطای مدل»).
- **[BUILD ۱]** دو لایه:
  - **برداشتِ دانشِ عمومی از همان فایل‌ها:** پرامپتِ استخراجِ عمیق حالا
    «kb_items» هم می‌خواهد (بخشنامه با شماره/تاریخِ دقیق، رویه، تعرفه،
    آمارِ تجمیعی/سطحِ شعبه، مطلبِ مرجع — بدونِ دادهٔ خصوصیِ مشتری) و صریح
    می‌گوید «نبودِ دادهٔ حساب‌محور خطا نیست». stage_extraction این‌ها را
    به‌صورت kb_write قابلِ‌بازبینی (dedup، سقف ۲۰، source_note = خودِ فایل)
    کنارِ db_write/link می‌گذارد — apply همان مسیرِ موجودِ پایگاه دانش.
  - **پیامِ صادقانه برای حالتِ واقعاً-خالی:** کدِ خطای جدید
    `no_account_data` (فقط وقتی همهٔ قطعات موفق ولی هر سه خروجی خالی)؛
    فرانت‌اند آن را به‌جای «ناموفقِ» قرمز، به‌صورت یادداشتِ آرام (severity
    low) با توضیحِ روشن نشان می‌دهد + یادآوریِ این‌که محتوای فایل در
    «بررسیِ کامل» و پرکردنِ جدول‌ها همچنان استفاده می‌شود.
- **[OWNER REPORT ۲]** با تیک‌های فعال، دستیار متنِ پراکنده را مرتب نکرد:
  ادامهٔ موضوع در پاراگرافِ جدا ماند، جمله‌های ابتر تشخیص داده نشد — کارِ
  «جزیره‌ای». **ریشه:** text_replace فیزیکاً نمی‌تواند از مرزِ پاراگراف
  بگذرد (متنِ بلوک‌ها در جست‌وجوی fuzzy بدونِ جداکننده به هم می‌چسبند و
  عبارتِ فاصله‌دارِ مدل بی‌تطبیق می‌ماند؛ گاردِ سرور هم find بینِ‌بلوکی را
  رد می‌کند) — یعنی مدل «ابزارِ» ادغام/جابه‌جاییِ بینِ پاراگراف‌ها را نداشت.
- **[BUILD ۲]** op جدیدِ `paragraph_merge`: parts (۲ تا ۶ عبارتِ عیناً موجود،
  هر یک داخلِ یک پاراگراف؛ همان گاردِ ضدتوهمِ text_replace با نرمال‌سازیِ
  ي/ك/فاصله) + replace (متنِ یکپارچهٔ نهایی). اعمالِ کلاینت اتمیک است:
  تکهٔ ۱ ⇒ متنِ ادغام‌شده، تکه‌های بعدی حذف؛ اگر حتی یک تکه پیدا نشود،
  هیچ‌چیز دست نمی‌خورد. پرامپت‌ها: راهنمای ابزارِ پاراگراف‌ها بازنویسی شد
  («کل‌نگر، نه جزیره‌ای» + تشخیصِ صریحِ جملهٔ ابتر/ناتمام + کاربردِ merge)،
  قاعدهٔ ۳ (تعریفِ op) و قاعدهٔ ۸ (بازخوانیِ نهاییِ سرتاسری) تقویت شد.
- **[VERIFY]** ۳ تستِ جدید (اعتبارسنجیِ merge: تکهٔ توهمی/تک‌تکه حذف،
  تطبیقِ نرمال‌شده؛ رسیدنِ قواعد به پرامپت؛ staging کبِ kb_items با dedup +
  خطِ «NOT an error» در پرامپتِ استخراج) — ماژول‌ها ۴۱/۴۱؛ pytest کامل در
  گیت؛ type-check/build سبز؛ static سینک؛ harness نامه ۱۰۷/۱۰۷، offer
  ۷۴/۷۴. **مارکر ⇒ reflow-v69.**

## 2026-07-16 — v70: گپِ «هٔ» (ه + همزهٔ ترکیبی) در فونتِ نامه — نرمال‌سازی به «ۀ»

- **[OWNER REPORT]** در متنِ نامه، «ه چسبانِ همزه‌دار» (مثل «ماههٔ») به حرفِ
  قبلی نمی‌چسبد و گپ می‌افتد.
- **[ROOT CAUSE]** فونتِ نامه (B Nazanin) گلیف/لنگرِ «همزهٔ ترکیبیِ»
  U+0654 را ندارد؛ مرورگر برای همان یک نشان به فونتِ جایگزین می‌پرد،
  زنجیرهٔ shaping عربی وسطِ کلمه می‌شکند و حرفِ قبلی به شکلِ پایانی/مجزا
  رندر می‌شود ⇒ گپ. تک‌نویسهٔ پیش‌ساختهٔ «ۀ» (U+06C0) در فونت‌های سری B
  کامل پشتیبانی می‌شود و «هٔ/ۀ» در فارسی فقط پایانِ واژه می‌آید (کسرهٔ
  اضافه) — پس تبدیلِ سراسری بی‌خطر است.
- **[BUILD]**
  - `fixHehHamza` (ه+U+0654 ⇒ ۀ) در «همهٔ» مسیرهای ورودِ متنِ نامه:
    تایپ در بدنه (onBody) و همهٔ فیلدهای RichSpan (موضوع/گیرنده/رونوشت/
    عنوانِ جدولِ پیوست)، paste (cleanPaste ⇒ هر دو مسیر)، بازکردنِ
    نامه‌های قدیمی (فیلدها + جدول‌های پیوستی)، و خروجیِ اعمال‌شدهٔ AI
    (متن‌ها و جدول‌های ساخته‌شده).
  - **رشته‌های ثابتِ UI هم پاک شد:** ۱۶۹ موردِ «ه+همزه» در ۱۶ فایلِ
    فرانت‌اند (برچسب‌های چاپیِ نامه/فرم‌ها مثل «شمارهٔ صفحه»، صفحهٔ
    offer-letter و…) یک‌جا به «ۀ» تبدیل شد — الگویِ regex خودِ
    fixHehHamza محافظت شد.
  - **گاردها به هر دو شکل تحمل‌پذیر شدند:** بک‌اند `_fold_heh` فقط-برای-تطبیق
    (نمایش دست‌نخورده — درسِ همین گیت: fold داخلِ `_norm_ws` متنِ نمایشیِ
    facts را خراب می‌کرد و تستِ v66 گرفت) در گاردِ text_replace و
    paragraph_merge؛ فرانت‌اند canon جست‌وجوی fuzzy: «ۀ»⇒«ه» و حذفِ همزهٔ
    ترکیبی با حفظِ نگاشتِ ایندکس.
- **[VERIFY]** تستِ جدیدِ گاردِ دوطرفه (نامه ۀ/مدل هٔ و برعکس) — ماژول
  ۳۲/۳۲؛ pytest کامل ۷۲۲؛ §39 هارنس: تایپِ زندهٔ ه+همزه در بدنه ⇒ ذخیره
  به‌صورت U+06C0 و بدونِ U+0654 (letter ۱۰۹/۱۰۹)؛ offer ۷۴/۷۴ (دو انتظارِ
  متنیِ خودِ هارنس هم به «ۀ» به‌روز شد — برچسبِ دکمهٔ تعرفه تغییرِ عمدی)؛
  type-check/build سبز؛ static سینک. **مارکر ⇒ reflow-v70.**

## 2026-07-17 — v71: بلوکِ پایانی با جدولِ درحال‌رشد + دکمهٔ «برگشت» (undo)

- **[OWNER REPORT ۱]** با بزرگ‌شدنِ جدولِ داخلِ متن، «اقدام‌کننده/واحد
  ارسال‌کننده» با وجودِ جای خالیِ فراوان به صفحهٔ بعد می‌افتاد.
- **[ROOT CAUSE — با بازتولیدِ عددی (repro_close.js، pdbg)]** هر سه فیلدِ
  پایانی «با یک عدد» سُر می‌خوردند؛ ظرفیتِ سُرخوردن = فاصلهٔ «پایین‌ترین»
  فیلد تا کفِ صفحه (~۳۵px). سرریزِ ~۶۰px ⇒ کلِ گروه به صفحهٔ «خالی» تبعید
  می‌شد، در حالی که ~۱۰۰px جای دیدنی زیرِ جدول بود.
- **[BUILD ۱]** دو لایه (هر دو در همان useEffect صفحه‌بندی):
  - **سُرخوردنِ per-field:** closingShift از عددِ واحد به Record تبدیل شد؛
    بسته‌بندیِ از-پایین-به-بالا: هر فیلد تا کف یا تا فیلدِ زیری (حداقل ۴px،
    حفظِ ترتیب) پایین می‌رود — گروه فضای داخلی‌اش را فشرده می‌کند؛ موفقیت
    = بالاترین فیلد به‌اندازهٔ کلِ سرریز پایین رفته باشد. مصرف در ویو و
    پرینت per-field شد.
  - **رفتارِ Word وقتی فشرده هم جا نشد:** به‌جای pages.push([]) (بلوکِ
    پایانیِ تنها در صفحهٔ خالی)، «دُمِ سرریزِ» صفحهٔ آخر (ردیف‌های اضافهٔ
    جدول) به صفحهٔ تازه می‌رود و بلوکِ پایانی زیرِ همان ادامه می‌نشیند؛
    حلقه تا جاگیریِ کامل (سقف ۸ دور) + fallback قدیمی اگر هیچ‌چیز بالای
    ناحیهٔ پایانی جا نشود. بازتولید: ۳/۵ ردیف ⇒ یک صفحه (اسلاید)؛ ۷/۹/۱۱
    ⇒ صفحهٔ آخر همیشه ردیف + بلوکِ پایانی، هرگز صفحهٔ خالی.
- **[OWNER REPORT ۲]** اصلاحاتِ toolbar روی جدولِ AI ساخته خرابش کرد و
  «راهِ برگشتی نبود» — عملیاتِ ساختاری/resize خارج از undo مرورگرند.
- **[BUILD ۲]** دکمهٔ «↩ برگشت» + تاریخچهٔ غلطانِ ~۱ثانیه‌ای از {فیلدها +
  جدول‌های پیوستی} (۴۰ مرحله، بدونِ ابزارگذاریِ تک‌تکِ مسیرها؛ هر فشردن
  یک مرحله عقب می‌رود)؛ با بازکردنِ نامهٔ دیگر/نامهٔ نو تاریخچه پاک
  می‌شود (برگشتِ بینِ دو نامه = چسباندنِ متنِ نامهٔ قبلی).
- **[FOUND en route]** تایپ در سلولِ «جدولِ پیوستی» از نرمال‌سازیِ v70
  (ه+همزه⇒ۀ) نمی‌گذشت — وصل شد.
- **[VERIFY]** repro عددی قبل/بعد؛ §40 (بلوکِ پایانی همیشه کنارِ محتوا،
  نه صفحهٔ خالی) + §41 (برگشتِ بدنهٔ خراب‌شده) + پایدارسازیِ §39 (تایپ با
  delay انسانی — flake تستی بود نه باگ) — letter ۱۱۲/۱۱۲ در ۳ اجرای
  پیاپی؛ offer ۷۴/۷۴؛ pytest کامل ۷۲۲؛ type-check/build سبز؛ static
  سینک. **مارکر ⇒ reflow-v71.**

## 2026-07-17 — v72: دستورِ داخلِ متن ⇒ عملیاتِ جدول + قاعدهٔ تجمیعِ صریح

- **[OWNER QUESTION]** «اگر این دستور را داخلِ متنِ نامه بنویسم (بازطراحیِ
  سرستون‌های جدول، پرکردن از پیوستِ اکسل به‌صورت جمعِ همهٔ شعب، هم‌صفحگیِ
  بلوکِ پایانی) همه‌اش را درست انجام می‌دهد؟» — بررسیِ کد سه شکاف نشان داد.
- **[GAPS → BUILD]** (۱) راهنمای «اجرای دستورهای داخلِ متن» فقط به
  text_replace/db_write وصل بود — حالا دستورِ داخلِ متنیِ مربوط به جدول به
  table_replace/table_insert مسیر داده می‌شود (با حذفِ جملهٔ دستور از متن)؛
  (۲) منبعِ داده‌اش «فقط DB» بود — حالا همان منابعِ مجازِ جدول‌ها (DB +
  محتوای پیوست‌ها + خودِ دستور)؛ (۳) «جمعِ همهٔ شعب» = محاسبه، که قواعد
  اجازه نمی‌دادند — قاعدهٔ تجمیعِ صریح اضافه شد: فقط به‌درخواستِ صریح،
  محاسبه دو بارِ مستقل و فقط در صورتِ تطابق، ذکرِ دقیقِ مبنای جمع در note
  (قابلِ راستی‌آزمایی)، و امتناع اگر منبعِ سقف‌خورده/ناقص است؛ (۴) دستورهای
  «هم‌صفحگی/جاگیری» عملِ مدل نیستند (صفحه‌بندیِ v71 خودکار است) — مدل جمله
  را حذف و با note توضیح می‌دهد.
- **[VERIFY]** تستِ سیم‌کشیِ جدید (راهنما + قواعدِ تجمیع در پرامپت) — ماژول
  ۳۳/۳۳؛ pytest کامل در گیت؛ تغییرِ فرانت‌اند نبود (مارکر همان reflow-v71).
