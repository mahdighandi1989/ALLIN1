# Removal Candidates — قرنطینه به‌جای حذف

هیچ قابلیتی (endpoint، صفحه، دکمه، سرویس، fallback) بدون تأیید صریح مالک حذف
نمی‌شود (قانون ۲ در `CLAUDE.md`). هر چیزی که مرده/تکراری/مشکوک به‌نظر می‌رسد
این‌جا ثبت و در صورت لزوم قرنطینه می‌شود تا مالک تصمیم بگیرد.

| تاریخ | مورد | محل | چرا کاندید حذف است | وضعیت |
|-------|------|-----|--------------------|-------|
| 2026-07-02 | `cleanup_unused_files.py` (ریشه) | `archive/quarantine/cleanup_unused_files.py.placeholder` | از PR #226 فقط متن placeholder («محتوای اسکریپت (کد بالا)») داشت — هیچ کد واقعی، هیچ ارجاعی در ریپو | قرنطینه شد |
| 2026-07-02 | `PR_DESCRIPTION.md` و `pull_request_description.md` (ریشه) | ریشه‌ی ریپو | آرتیفکت‌های PRهای قدیمی؛ محتوای تاریخی دارند | فعلاً دست‌نخورده — تصمیم با مالک |
| 2026-08-07 | کامپوننت `VoucherIRR` (قالب جعبه‌ای سند چک ریالی v99) | `frontend/src/app/voucher/page.tsx` | مالک قالب جعبه‌ایِ کپی‌شده از اکسل را رد کرد؛ از v100 اسلیپ IRR با همان کامپوننت `Voucher` استاندارد رندر می‌شود و این تابع بدون استفاده ماند | قرنطینه شد (تابع حذف نشده) |

## 2026-09-05 — «صندوق بیمه‌نامه‌های درایو» (v117) — UI حذف‌شده، بک‌اند قرنطینه

- **چه چیزی:** کارتِ «☁️ بیمه‌نامه‌های درایو» در صفحهٔ ایمپورت (v118 حذف شد —
  دستور صریح مالک: «اون گزینه رو بردار چون اصلا کار هم نکرد و فقط پوشه ساخت»)
  + روترِ بک‌اند `/api/policy-inbox` (ensure/scan/apply/import-file در
  `backend/app/routers/policy_inbox.py`)، سرویسِ `services/policy_inbox.py`،
  primitivesِ درایو (`list_folder`/`rename_file`/`file_link`) و
  `policyInboxApi` در `frontend/src/lib/api.ts`.
- **چرا کار نکرد:** درایوِ production در حالتِ OAuth (drive.file) است و
  فایل‌هایی که کاربر دستی در پوشه می‌گذارد برای برنامه نامرئی‌اند؛ کارِ
  یک‌بارمصرفِ ۹۴ بیمه‌نامه نهایتاً بیرون از برنامه (کانکتور درایوِ Claude)
  انجام شد.
- **وضعیت:** UI حذف؛ بک‌اند + کلاینتِ API **قرنطینه** (بدونِ نقطهٔ ورودِ UI،
  پشتِ auth، بی‌ضرر). primitivesِ درایو عمومی‌اند و ممکن است بعداً به کار
  بیایند. تست‌های `test_policy_inbox.py` سبز نگه داشته می‌شوند.
- **حذفِ کامل:** فقط با تأیید صریحِ بعدیِ مالک (روتر + سرویس + تست‌ها + api).
