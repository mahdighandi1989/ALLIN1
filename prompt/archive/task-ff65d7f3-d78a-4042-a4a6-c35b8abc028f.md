---
task_id: ff65d7f3-d78a-4042-a4a6-c35b8abc028f
title: جایگزینی وابستگی Excel در پایپلاین داده
type: logic_audit
priority: medium
execution_priority: 3000
status: done
external_status: pending
verification_status: done
watched_id: b2586b68-22f8-4e8e-a7a8-9b513c5f70fe
project: mahdighandi1989/ALLIN1
created_at: '2026-05-17T16:08:44.056026+00:00'
updated_at: '2026-06-02T18:00:06.438160+00:00'
archived: true
archived_at: '2026-05-29T21:52:38.539059+00:00'
target_files:
- backend/tests/test_coherence.py
- backend/app/models/facility.py
---

# جایگزینی وابستگی Excel در پایپلاین داده

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

کامپوننت‌ها با 'Excel application' تعامل دارند، اما مشخص نیست که آیا pipeline به نصب Excel روی سرور نیاز دارد یا از کتابخانه‌های جایگزین (مثل openpyxl یا pandas) استفاده می‌کند. این یک وابستگی خارجی است که باید مستند شود.

## 💥 پیامد (impact)
اگر pipeline در محیطی بدون Excel اجرا شود (مثلاً Docker container لینوکسی)، با خطا مواجه می‌شود. همچنین استفاده از Excel automation ممکن است باعث مشکلات performance و stability شود.

## 🛠 پیشنهاد رفع اولیه
تصمیم بگیرید که از کتابخانه‌های Python (مثل openpyxl برای .xlsm و xlrd برای .xls) استفاده کنید تا وابستگی به Excel حذف شود. اگر Excel ضروری است (مثلاً برای اجرای macroها)، این وابستگی را به وضوح در مستندات و requirements ذکر کنید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

پس از پیاده‌سازی این تسک، تشخیص بده آیا بخشی از این درخواست نیاز به
**اقدام دستی کاربر** دارد که در حوزهٔ دسترسی‌ها و قابلیت‌های فعلی شما
(به‌عنوان یک agent با هر مدل و هر سطح دسترسی) امکان‌پذیر نیست. مثال‌ها
(غیر-جامع):

  - ثبت‌نام در سرویس خارجی و تهیهٔ API key یا OAuth credential
  - ایجاد bucket/database/project در cloud provider با account شخصی کاربر
  - آپلود فایل/تصویر/asset/داده‌ی خصوصی که فقط کاربر دارد
  - تأیید/پرداخت برای سرویس پولی
  - تصمیم کسب‌وکار/حقوقی/سلیقه‌ای (انتخاب رنگ برند، متن قانونی…)
  - اقدام در پنل ادمین خارجی که شما credential ندارید
  - هر اقدامی که محدودیت سیاست/امنیت/فنی فعلی شما اجازه نمی‌دهد

**این لیست همیشه قابل استفاده است:** اگر در آینده قابلیت‌های شما
گسترش پیدا کرد و توانستید یکی از این موارد را خودکار انجام دهید،
انجام دهید و TO-DO نسازید. این یادداشت ایستا نیست؛ شما در هر زمان
تشخیص می‌دهید چه چیزی واقعاً «خارج از توان فعلی» است.

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

کامپوننت‌ها با 'Excel application' تعامل دارند، اما مشخص نیست که آیا pipeline به نصب Excel روی سرور نیاز دارد یا از کتابخانه‌های جایگزین (مثل openpyxl یا pandas) استفاده می‌کند. این یک وابستگی خارجی است که باید مستند شود.

## 💥 پیامد (impact)
اگر pipeline در محیطی بدون Excel اجرا شود (مثلاً Docker container لینوکسی)، با خطا مواجه می‌شود. همچنین استفاده از Excel automation ممکن است باعث مشکلات performance و stability شود.

## 🛠 پیشنهاد رفع اولیه
تصمیم بگیرید که از کتابخانه‌های Python (مثل openpyxl برای .xlsm و xlrd برای .xls) استفاده کنید تا وابستگی به Excel حذف شود. اگر Excel ضروری است (مثلاً برای اجرای macroها)، این وابستگی را به وضوح در مستندات و requirements ذکر کنید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
```

## 📋 چک‌لیست مراحل (4 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [x] **مرحله 1: تحلیل و مستندسازی وابستگی به Excel در pipeline داده** — این مرحله شامل تحلیل کامل pipeline داده برای شناسایی تمام نقاط تعامل با Excel application است. باید مشخص شود که آیا pipeline از Excel automation (مثلاً win32com) استفاده می‌کند یا از کتابخانه‌های جایگزین (openpyxl, pandas, xlrd). همچنین باید مستندسازی شود که کدام کامپوننت‌ها به Excel وابسته هستند و 
- [x] **مرحله 2: تصمیم‌گیری و پیاده‌سازی جایگزینی Excel automation با کتابخانه‌های Python** — این مرحله شامل تصمیم‌گیری نهایی درباره جایگزینی Excel automation با کتابخانه‌های Python (openpyxl برای .xlsm و xlrd برای .xls) است. اگر Excel ضروری باشد (مثلاً برای اجرای macroها)، این مرحله شامل مستندسازی صریح این وابستگی در requirements.txt و مستندات می‌شود. اگر Excel ضروری نباشد، این مرحله شامل ب
- [x] **مرحله 3: به‌روزرسانی مستندات pipeline با وابستگی‌های Excel و راه‌حل‌های جایگزین** — این مرحله شامل به‌روزرسانی مستندات pipeline (README, docs, یا فایل‌های جداگانه) با اطلاعات دقیق درباره وابستگی به Excel است. باید مشخص شود که کدام کامپوننت‌ها به Excel نیاز دارند (اگر ضروری است) و کدام یک از کتابخانه‌های Python استفاده می‌کنند. همچنین باید دستورالعمل‌های نصب و راه‌اندازی برای محیط‌ه
- [x] **مرحله 4: افزودن تست‌های coherence برای تشخیص وابستگی‌های ناسازگار در pipeline** — این مرحله شامل نوشتن تست‌های واحد یا integration است که بررسی می‌کنند آیا pipeline داده وابستگی‌های ناسازگار دارد یا خیر. این تست‌ها باید تشخیص دهند که آیا دو کامپوننت فرض‌های متفاوتی درباره محیط اجرا دارند (مثلاً یکی فرض می‌کند Excel نصب است و دیگری فرض می‌کند نیست). خارج از این مرحله: تست‌های unit

---

# 🔹 مرحله 1: تحلیل و مستندسازی وابستگی به Excel در pipeline داده

**Scope:** این مرحله شامل تحلیل کامل pipeline داده برای شناسایی تمام نقاط تعامل با Excel application است. باید مشخص شود که آیا pipeline از Excel automation (مثلاً win32com) استفاده می‌کند یا از کتابخانه‌های جایگزین (openpyxl, pandas, xlrd). همچنین باید مستندسازی شود که کدام کامپوننت‌ها به Excel وابسته هستند و کدام یک از کتابخانه‌های Python استفاده می‌کنند. خارج از این مرحله: تغییر کد یا پیاده‌سازی راه‌حل. این مرحله صرفاً تحلیل و مستندسازی است.
**Key terms:** pipeline data, Excel application, openpyxl, pandas, xlrd, win32com

**بخش مربوط از متن کاربر:**
```
در pipeline `data` یک ناسازگاری منطقی پیدا شد: کامپوننت‌ها با 'Excel application' تعامل دارند، اما مشخص نیست که آیا pipeline به نصب Excel روی سرور نیاز دارد یا از کتابخانه‌های جایگزین (مثل openpyxl یا pandas) استفاده می‌کند. این یک وابستگی خارجی است که باید مستند شود.
```

## 🎯 هدف (خلاصه ساختاریافته)
تحلیل و مستندسازی وابستگی به Excel در pipeline داده

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
تحلیل و مستندسازی وابستگی به Excel در pipeline داده. این مرحله شامل تحلیل کامل pipeline داده برای شناسایی تمام نقاط تعامل با Excel application است. باید مشخص شود که آیا pipeline از Excel automation (مثلاً win32com) استفاده می‌کند یا از کتابخانه‌های جایگزین (openpyxl, pandas, xlrd). همچنین باید مستندسازی شود که کدام کامپوننت‌ها به Excel وابسته هستند و کدام یک از کتابخانه‌های Python استفاده می‌کنند. خارج از این مرحله: تغییر کد یا پیاده‌سازی راه‌حل. این مرحله صرفاً تحلیل و مستندسازی است. در pipeline `data` یک ناسازگاری منطقی پیدا شد: کامپوننت‌ها با 'Excel application' تعامل دارند، اما مشخص نیست که آیا pipeline به نصب Excel روی سرور نیاز دارد یا از کتابخانه‌های جایگزین (مثل openpyxl یا pandas) استفاده می‌کند. این یک وابستگی خارجی است که باید مستند شود. کلیدواژه‌ها: pipeline data, Excel application, openpyxl, pandas, xlrd, win32com. شواهد در کد واقعی پروژه: فایل‌های Excel در مسیرهای `archive/original-excel-files/Backend_Database.xlsm`, `archive/original-excel-files/Backend_Journal.xlsx`, `archive/original-excel-files/CREDIT  FILE  SUMMARY (Corporate).xls`, `archive/original-excel-files/CREDIT  FILE  SUMMARY (Retail).xls`, `archive/original-excel-files/DEPOSITS_LIST.xlsx`, `archive/original-excel-files/Frontend_Final.xlsm`, `archive/original-excel-files/OFFER_LETTER_CONTROLLER.xlsm`, `archive/original-excel-files/OFFER_LETTER_TEMPLATE.docx`, `archive/original-excel-files/PROPERTIES - IRAN.xlsx`, `archive/original-excel-files/PROPERTIES - UAE.xlsx`, `data-import/Backend_Database.xlsm`, `data-import/PROPERTIES - IRAN.xlsx`, `data-import/PROPERTIES - UAE.xlsx`, `data-import/Securities List 2022.xlsx`, `data-import/Securities List 2023.xlsx`, `data-import/Securities List 2024.xlsx`, `data-import/Securities List 2025.xlsx`, `data-import/Securities List 2026.xlsx` نشان‌دهنده وابستگی شدید به فایل‌های Excel هستند. فایل `data-import/README.md` شامل بخش 'Security & Validation' و 'Data Sanitization Process' است که به پردازش فایل‌های Excel اشاره دارد اما مشخص نمی‌کند از کدام کتابخانه Python استفاده می‌کند. فایل `docs/DATABASE_SCHEMA.md` بر اساس تحلیل فایل‌های Excel نوشته شده است. فایل `backend/requirements.txt` شامل کتابخانه‌های `pandas` نیست اما `openpyxl` و `xlrd` نیز در آن دیده نمی‌شود. فایل `pyproject.toml` نیز شامل `pandas` یا `openpyxl` نیست. این نشان می‌دهد که pipeline داده یا از کتابخانه‌ای استفاده می‌کند که در فایل‌های dependency ذکر نشده، یا از Excel automation (win32com) استفاده می‌کند که نیاز به نصب Excel روی سرور دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. مرحله ۱: اسکن کامل کدبیس برای یافتن importهای مرتبط با Excel. در فایل‌های `backend/app/` و `data-import/` به دنبال importهایی مانند `import openpyxl`, `import pandas`, `import xlrd`, `import win32com`, `from openpyxl`, `from pandas`, `from xlrd`, `from win32com` بگرد. از دستور `grep -r "import openpyxl\|import pandas\|import xlrd\|import win32com" backend/app/ data-import/` استفاده کن. مرحله ۲: اسکن فایل‌های Python در `backend/app/` و `data-import/` برای یافتن توابعی که فایل‌های Excel را باز می‌

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: تصمیم‌گیری و پیاده‌سازی جایگزینی Excel automation با کتابخانه‌های Python

**Scope:** این مرحله شامل تصمیم‌گیری نهایی درباره جایگزینی Excel automation با کتابخانه‌های Python (openpyxl برای .xlsm و xlrd برای .xls) است. اگر Excel ضروری باشد (مثلاً برای اجرای macroها)، این مرحله شامل مستندسازی صریح این وابستگی در requirements.txt و مستندات می‌شود. اگر Excel ضروری نباشد، این مرحله شامل بازنویسی کدهای مرتبط با Excel automation با openpyxl/xlrd و به‌روزرسانی requirements.txt می‌شود. خارج از این مرحله: تغییرات در سایر بخش‌های pipeline که به Excel مرتبط نیستند.
**Key terms:** openpyxl, xlrd, .xlsm, .xls, requirements.txt, Excel automation, macro

**بخش مربوط از متن کاربر:**
```
تصمیم بگیرید که از کتابخانه‌های Python (مثل openpyxl برای .xlsm و xlrd برای .xls) استفاده کنید تا وابستگی به Excel حذف شود. اگر Excel ضروری است (مثلاً برای اجرای macroها)، این وابستگی را به وضوح در مستندات و requirements ذکر کنید.
```

## 🎯 هدف (خلاصه ساختاریافته)
جایگزینی Excel automation با openpyxl/xlrd

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
تصمیم‌گیری و پیاده‌سازی جایگزینی Excel automation با کتابخانه‌های Python. این مرحله شامل تصمیم‌گیری نهایی درباره جایگزینی Excel automation با کتابخانه‌های Python (openpyxl برای .xlsm و xlrd برای .xls) است. اگر Excel ضروری باشد (مثلاً برای اجرای macroها)، این مرحله شامل مستندسازی صریح این وابستگی در requirements.txt و مستندات می‌شود. اگر Excel ضروری نباشد، این مرحله شامل بازنویسی کدهای مرتبط با Excel automation با openpyxl/xlrd و به‌روزرسانی requirements.txt می‌شود. خارج از این مرحله: تغییرات در سایر بخش‌های pipeline که به Excel مرتبط نیستند. کلیدواژه‌ها: openpyxl, xlrd, .xlsm, .xls, requirements.txt, Excel automation, macro. در پروژه فعلی، فایل‌های Excel در مسیر data-import/ قرار دارند (مانند Backend_Database.xlsm, PROPERTIES - IRAN.xlsx, PROPERTIES - UAE.xlsx, Securities List 2022-2026.xlsx). همچنین فایل‌های archive/original-excel-files/ شامل فایل‌های اصلی مانند Backend_Database.xlsm, Backend_Journal.xlsx, CREDIT FILE SUMMARY (Corporate).xls, CREDIT FILE SUMMARY (Retail).xls, DEPOSITS_LIST.xlsx, Frontend_Final.xlsm, OFFER_LETTER_CONTROLLER.xlsm, OFFER_LETTER_TEMPLATE.docx, PROPERTIES - IRAN.xlsx, PROPERTIES - UAE.xlsx هستند. فایل data-import/README.md مستندات مربوط به import داده را شامل می‌شود و به وضوح بیان می‌کند که فرمت‌های پشتیبانی شده شامل Excel (.xlsx, .xls, .xlsm) هستند. در حال حاضر، requirements.txt شامل کتابخانه‌های openpyxl یا xlrd نیست. وابستگی‌های فعلی در requirements.txt عبارتند از: fastapi, uvicorn, sqlalchemy, alembic, asyncpg, pydantic, pydantic-settings, email-validator, python-multipart, python-jose, PyJWT, passlib, python-dateutil, psycopg2-binary. همچنین در pyproject.toml وابستگی‌های بیشتری مانند redis, celery, httpx, pytest ذکر شده است. فایل‌های backend/app/routers/customers.py و backend/app/routers/facilities.py و backend/app/routers/stats.py ممکن است از داده‌های Excel استفاده کنند. فایل‌های backend/app/models/customer.py و backend/app/models/facility.py مدل‌های دیتابیس را تعریف می‌کنند که با داده‌های Excel مرتبط هستند. فایل docs/DATABASE_SCHEMA.md ساختار دیتابیس را بر اساس تحلیل فایل‌های Excel شرح می‌دهد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. بررسی وابستگی به macroها: فایل‌های .xlsm موجود (مانند Backend_Database.xlsm, Frontend_Final.xlsm, OFFER_LETTER_CONTROLLER.xlsm) را بررسی کنید که آیا حاوی macroهای VBA هستند که برای پردازش داده ضروری باشند. اگر macro ضروری است، وابستگی به Excel را در requirements.txt و مستندات ذکر کنید. 2. اگر Excel ضروری نیست: الف) کتابخانه‌های openpyxl و xlrd را به requirements.txt اضافه کنید. ب) کدهای موجود در backend/app/routers/customers.py و backend/app/routers/facilities.py و backend/app/routers/stats.py که از Excel automation استفاده می‌کنند را با openpyxl/xlrd بازنویسی کنید. ج) فایل data-import/README.md را به‌روزرسانی کنید تا روش جدید پردازش Excel را توضیح دهد. 3. به‌روزرسانی requirements.txt: خطوط openpyxl>=3.1.2 و xlrd>=2.0.1 را اضافه کنید. 4. ایجاد یک ماژول جدید backend/app/utils/excel_handler.py برای کپسوله‌سازی منطق خواندن/نوشتن Excel با openpyxl/xlrd

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: به‌روزرسانی مستندات pipeline با وابستگی‌های Excel و راه‌حل‌های جایگزین

**Scope:** این مرحله شامل به‌روزرسانی مستندات pipeline (README, docs, یا فایل‌های جداگانه) با اطلاعات دقیق درباره وابستگی به Excel است. باید مشخص شود که کدام کامپوننت‌ها به Excel نیاز دارند (اگر ضروری است) و کدام یک از کتابخانه‌های Python استفاده می‌کنند. همچنین باید دستورالعمل‌های نصب و راه‌اندازی برای محیط‌های مختلف (Windows با Excel, Linux بدون Excel) اضافه شود. خارج از این مرحله: تغییر کد یا مستندات غیرمرتبط با Excel.
**Key terms:** README, docs, requirements.txt, Excel, macro, Docker, Linux

**بخش مربوط از متن کاربر:**
```
اگر Excel ضروری است (مثلاً برای اجرای macroها)، این وابستگی را به وضوح در مستندات و requirements ذکر کنید.
```

## 🎯 هدف (خلاصه ساختاریافته)
به‌روزرسانی مستندات pipeline با وابستگی‌های Excel و راه‌حل‌های جایگزین

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست به‌روزرسانی مستندات pipeline (README, docs, یا فایل‌های جداگانه) با اطلاعات دقیق درباره وابستگی به Excel را دارد. باید مشخص شود که کدام کامپوننت‌ها به Excel نیاز دارند (اگر ضروری است) و کدام یک از کتابخانه‌های Python استفاده می‌کنند. همچنین باید دستورالعمل‌های نصب و راه‌اندازی برای محیط‌های مختلف (Windows با Excel, Linux بدون Excel) اضافه شود. خارج از این مرحله: تغییر کد یا مستندات غیرمرتبط با Excel. بخش مربوط از درخواست اصلی کاربر: «اگر Excel ضروری است (مثلاً برای اجرای macroها)، این وابستگی را به وضوح در مستندات و requirements ذکر کنید.» کلیدواژه‌ها: README, docs, requirements.txt, Excel, macro, Docker, Linux. شواهد در کد واقعی پروژه: فایل `backend/requirements.txt` شامل کتابخانه‌های `fastapi`, `sqlalchemy`, `alembic`, `psycopg2-binary` است اما هیچ کتابخانه‌ای برای خواندن Excel (مثل `openpyxl`, `pandas`, `xlrd`) در آن وجود ندارد. فایل `data-import/README.md` شامل بخش «Data Import Folder» با توضیح فرمت‌های پشتیبانی‌شده (Excel: `.xlsx`, `.xls`, `.xlsm`) و validation rules است اما وابستگی به Excel را در مستندات اصلی (README ریشه) ذکر نکرده. فایل `docker-compose.yml` سرویس‌های `db`, `backend`, `frontend` را تعریف کرده اما هیچ اشاره‌ای به نصب کتابخانه‌های Excel در Dockerfile ندارد. فایل `backend/Dockerfile` از `requirements.txt` برای نصب وابستگی‌ها استفاده می‌کند. فایل `docs/DATABASE_SCHEMA.md` به فایل‌های Excel (Backend_Database.xlsm, PROPERTIES - IRAN.xlsx, PROPERTIES - UAE.xlsx, Securities List 2022-2026.xlsx) به عنوان منبع تحلیل دیتابیس اشاره کرده اما وابستگی runtime را مشخص نکرده. فایل `Makefile` (در ساختار پروژه موجود است) ممکن است شامل دستورات build/import باشد که نیاز به Excel دارند. فایل‌های `frontend/out/dashboard/index.html`, `frontend/out/facilities/index.html`, `frontend/out/index.html` حاوی اسکریپت Inspector Bridge هستند که به `wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_allin1` متصل می‌شوند و ربطی به Excel ندارند. فایل `backend/app/utils/security.py` (deep-read شده) شامل توابع امنیتی است و به Excel مرتبط نیست. فایل `backend/app/main.py` (deep-read شده) نقطه ورود FastAPI است و وابستگی Excel در آن دیده نمی‌شود. فایل‌های `backend/static/404/index.html`, `backend/static/customers/index.html`, `backend/static/dashboard/index.html`, `backend/static/facilities/index.html`, `backend/static/index.html`, `backend/static/login/index.html` صفحات استاتیک هستند. فایل‌های `frontend/out/404/index.html`, `frontend/out/customers/index.html`, `frontend/out/login/index.html` نیز صفحات استاتیک خروجی Next.js هستند. فایل‌های `frontend/src/app/customers/page.tsx`, `frontend/src/app/dashboard/page.tsx`, `frontend/src/app/facilities/page.tsx`, `frontend/src/app/layout.tsx`, `frontend/src/app/login/page.tsx`, `frontend/src/app/page.tsx` کامپوننت‌های React هستند و به Excel مرتبط نیستند. فایل‌های `backend/static/_next/static/chunks/pages/_app-75f6107b0260711c.js` و `frontend/out/_next/static/chunks/pages/_app-75f6107b0260711c.js` bundleهای جاوااسکریپت هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
_(مجری بر اساس Context و معیارهای پذیرش، مراحل را تعیین کند)_

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: افزودن تست‌های coherence برای تشخیص وابستگی‌های ناسازگار در pipeline

**Scope:** این مرحله شامل نوشتن تست‌های واحد یا integration است که بررسی می‌کنند آیا pipeline داده وابستگی‌های ناسازگار دارد یا خیر. این تست‌ها باید تشخیص دهند که آیا دو کامپوننت فرض‌های متفاوتی درباره محیط اجرا دارند (مثلاً یکی فرض می‌کند Excel نصب است و دیگری فرض می‌کند نیست). خارج از این مرحله: تست‌های unit معمولی که در silo اجرا می‌شوند. این تست‌ها باید cross-component باشند.
**Key terms:** coherence issue, unit test, integration test, pipeline data, Excel, feature flag rot, refactor

**بخش مربوط از متن کاربر:**
```
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن تست‌های coherence برای تشخیص وابستگی‌های ناسازگار در pipeline

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/tests/test_coherence.py:1-100` — `test_coherence_pipeline_data` — فایل تست جدید برای coherence issues — باید cross-component باشد
  ```python
  فایل جدید — هنوز وجود ندارد
  ```
- `backend/app/models/facility.py:1-50` — `Facility model` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```python
  مدل Facility با فیلد amount — باید بررسی شود که آیا frontend نیز این فیلد را expects می‌کند
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/routers/customers.py` — این روتر داده‌های مشتریان را از API سرو می‌دهد و باید با frontend در ساختار

## 🔍 Context و وضعیت فعلی
کاربر درخواست افزودن تست‌های coherence برای تشخیص وابستگی‌های ناسازگار در pipeline داده است. این تست‌ها باید بررسی کنند که آیا pipeline داده وابستگی‌های ناسازگار دارد یا خیر. به‌طور خاص، این تست‌ها باید تشخیص دهند که آیا دو کامپوننت فرض‌های متفاوتی درباره محیط اجرا دارند (مثلاً یکی فرض می‌کند Excel نصب است و دیگری فرض می‌کند نیست). خارج از این مرحله: تست‌های unit معمولی که در silo اجرا می‌شوند. این تست‌ها باید cross-component باشند. coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند. کلیدواژه‌ها: coherence issue, unit test, integration test, pipeline data, Excel, feature flag rot, refactor. در پروژه Banking Operations System، pipeline داده شامل import از فایل‌های Excel (مانند Backend_Database.xlsm, PROPERTIES - IRAN.xlsx, PROPERTIES - UAE.xlsx) و پردازش آن‌ها در backend (backend/app/routers/customers.py, backend/app/routers/facilities.py) است. همچنین frontend (frontend/src/app/customers/page.tsx, frontend/src/app/facilities/page.tsx) داده‌ها را از API دریافت می‌کند. coherence issues می‌توانند بین backend و frontend در فرض‌های مربوط به ساختار داده (مثلاً وجود فیلد amount در facilities) یا بین ماژول‌های backend (مثلاً فرض وجود Excel در backend/app/utils/id_generator.py در مقابل backend/app/core/database_manager.py) رخ دهند. feature flag rot می‌تواند در frontend/src/components/Layout.tsx یا frontend/src/app/layout.tsx وجود داشته باشد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/tests/test_coherence.py برای تست‌های coherence cross-component. 2. در این فایل، تست‌هایی بنویس که بررسی کنند آیا backend/app/routers/customers.py و backend/app/routers/facilities.py فرض‌های یکسانی درباره وجود فیلد amount در مدل facilities (backend/app/models/facility.py) دارند. 3. تستی بنویس که بررسی کند آیا frontend/src/app/customers/page.tsx و backend/app/routers/customers.py فرض‌های یکسانی درباره ساختار پاسخ API (مثلاً وجود فیلد name) دارند. 4. تستی برای تشخیص feature flag rot در frontend/src/components/Layout.tsx با بررسی اینکه آیا flag های غیرفعال هنوز در کد وجود دارند. 5. تستی برای بررسی coherence بین backend/app/utils/id_generator.py و backend/app/core/database_manager.py در فرض‌های مربوط به محیط اجرا (مثلاً وجود Excel). 6. تستی برای بررسی coherence بین data-import/README.md و backend/app/routers/customers.py در validation rules.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: medium
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `data` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: static)_

## Task Steps

### Step 1: تحلیل و مستندسازی وابستگی به Excel در pipeline داده
**Status:** `done` (100%)
**Scope:** این مرحله شامل تحلیل کامل pipeline داده برای شناسایی تمام نقاط تعامل با Excel application است. باید مشخص شود که آیا pipeline از Excel automation (مثلاً win32com) استفاده می‌کند یا از کتابخانه‌های جایگزین (openpyxl, pandas, xlrd). همچنین باید مستندسازی شود که کدام کامپوننت‌ها به Excel وابسته هستند و کدام یک از کتابخانه‌های Python استفاده می‌کنند. خارج از این مرحله: تغییر کد یا پیاده‌سازی راه‌حل. این مرحله صرفاً تحلیل و مستندسازی است.
**Excerpt:**
```
در pipeline `data` یک ناسازگاری منطقی پیدا شد: کامپوننت‌ها با 'Excel application' تعامل دارند، اما مشخص نیست که آیا pipeline به نصب Excel روی سرور نیاز دارد یا از کتابخانه‌های جایگزین (مثل openpyxl یا pandas) استفاده می‌کند. این یک وابستگی خارجی است که باید مستند شود.
```

### Step 2: تصمیم‌گیری و پیاده‌سازی جایگزینی Excel automation با کتابخانه‌های Python
**Status:** `done` (100%)
**Scope:** این مرحله شامل تصمیم‌گیری نهایی درباره جایگزینی Excel automation با کتابخانه‌های Python (openpyxl برای .xlsm و xlrd برای .xls) است. اگر Excel ضروری باشد (مثلاً برای اجرای macroها)، این مرحله شامل مستندسازی صریح این وابستگی در requirements.txt و مستندات می‌شود. اگر Excel ضروری نباشد، این مرحله شامل بازنویسی کدهای مرتبط با Excel automation با openpyxl/xlrd و به‌روزرسانی requirements.txt می‌شود. خارج از این مرحله: تغییرات در سایر بخش‌های pipeline که به Excel مرتبط نیستند.
**Excerpt:**
```
تصمیم بگیرید که از کتابخانه‌های Python (مثل openpyxl برای .xlsm و xlrd برای .xls) استفاده کنید تا وابستگی به Excel حذف شود. اگر Excel ضروری است (مثلاً برای اجرای macroها)، این وابستگی را به وضوح در مستندات و requirements ذکر کنید.
```

### Step 3: به‌روزرسانی مستندات pipeline با وابستگی‌های Excel و راه‌حل‌های جایگزین
**Status:** `done` (100%)
**Scope:** این مرحله شامل به‌روزرسانی مستندات pipeline (README, docs, یا فایل‌های جداگانه) با اطلاعات دقیق درباره وابستگی به Excel است. باید مشخص شود که کدام کامپوننت‌ها به Excel نیاز دارند (اگر ضروری است) و کدام یک از کتابخانه‌های Python استفاده می‌کنند. همچنین باید دستورالعمل‌های نصب و راه‌اندازی برای محیط‌های مختلف (Windows با Excel, Linux بدون Excel) اضافه شود. خارج از این مرحله: تغییر کد یا مستندات غیرمرتبط با Excel.
**Excerpt:**
```
اگر Excel ضروری است (مثلاً برای اجرای macroها)، این وابستگی را به وضوح در مستندات و requirements ذکر کنید.
```

### Step 4: افزودن تست‌های coherence برای تشخیص وابستگی‌های ناسازگار در pipeline
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد یا integration است که بررسی می‌کنند آیا pipeline داده وابستگی‌های ناسازگار دارد یا خیر. این تست‌ها باید تشخیص دهند که آیا دو کامپوننت فرض‌های متفاوتی درباره محیط اجرا دارند (مثلاً یکی فرض می‌کند Excel نصب است و دیگری فرض می‌کند نیست). خارج از این مرحله: تست‌های unit معمولی که در silo اجرا می‌شوند. این تست‌ها باید cross-component باشند.
**Excerpt:**
```
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
```
