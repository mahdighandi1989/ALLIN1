---
task_id: ae87a6ca-2f66-469b-b263-79b1785240b3
title: پیاده‌سازی permission check در auth pipeline
type: logic_audit
priority: critical
execution_priority: 1300
status: awaiting_review
external_status: pending
verification_status: partial
watched_id: b2586b68-22f8-4e8e-a7a8-9b513c5f70fe
project: mahdighandi1989/ALLIN1
created_at: '2026-05-17T16:03:27.500337+00:00'
updated_at: '2026-06-03T17:31:46.016936+00:00'
archived: true
archived_at: '2026-05-29T22:04:39.127583+00:00'
tags:
- merged
---

# پیاده‌سازی permission check در auth pipeline

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `auth` یک ناسازگاری منطقی پیدا شد:

در مستندات ارائه شده، هیچ اشاره‌ای به مکانیزم permission یا authorization در pipeline احراز هویت نشده است. تمام مسیرهای mutation (مانند تغییر رمز عبور، ثبت‌نام، لاگین) بدون بررسی سطح دسترسی (role-based یا resource-based) اجرا می‌شوند. این یک شکاف امنیتی جدی است.

## 💥 پیامد (impact)
هر کاربر احراز هویت شده می‌تواند به عملیات‌های حساس مانند تغییر رمز عبور سایر کاربران یا دسترسی به منابع غیرمجاز دست یابد. این نقض اصل least privilege است.

## 🛠 پیشنهاد رفع اولیه
یک middleware یا decorator برای بررسی permission در endpoints اضافه کنید. مثلاً در backend/tests/test_auth.py و backend/app/database.py، قبل از هر mutation، سطح دسترسی کاربر را با token یا role چک کنید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
---
[scan #2 at 2026-05-24T15:57:07.034161+00:00]
## 📋 شرح ناسازگاری
در pipeline `auth` یک ناسازگاری منطقی پیدا شد:

در مستندات هیچ اشاره‌ای به middleware یا decorator برای بررسی مجوزها (permission) در مسیرهای mutation (مانند ثبت‌نام، ورود، به‌روزرسانی پروفایل، تغییر رمز عبور) نشده است. تمام مسیرها مستقیماً به handler متصل هستند و از permission gat

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم وجود permission check در auth pipeline

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline auth است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `auth` یک ناسازگاری منطقی پیدا شد:

در مستندات ارائه شده، هیچ اشاره‌ای به مکانیزم permission یا authorization در pipeline احراز هویت نشده است. تمام مسیرهای mutation (مانند تغییر رمز عبور، ثبت‌نام، لاگین) بدون بررسی سطح دسترسی (role-based یا resource-based) اجرا می‌شوند. این یک شکاف امنیتی جدی است.

## 💥 پیامد (impact)
هر کاربر احراز هویت شده می‌تواند به عملیات‌های حساس مانند تغییر رمز عبور سایر کاربران یا دسترسی به منابع غیرمجاز دست یابد. این نقض اصل least privilege است.

## 🛠 پیشنهاد رفع اولیه
یک middleware یا decorator برای بررسی permission در endpoints اضافه کنید. مثلاً در backend/tests/test_auth.py و backend/app/database.py، قبل از هر mutation، سطح دسترسی کاربر را با token یا role چک کنید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `auth` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: critical
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `auth` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: static)_

## Task Steps

### Step 1: بررسی و مستندسازی وضعیت فعلی permission در auth pipeline
**Status:** `partial` (40%)
**Scope:** بررسی کامل کدهای موجود در backend/app/auth/ و backend/app/database.py و backend/tests/test_auth.py برای شناسایی هرگونه مکانیزم permission موجود (role-based یا resource-based). این مرحله شامل جستجوی grep برای decoratorها، middlewareها، و بررسی token validation فعلی است. خروجی این مرحله یک مستند کوتاه (در فایل docs/security-audit.md) است که وضعیت فعلی را شرح می‌دهد. خارج از این مرحله: پیاده‌سازی هرگونه تغییر.
**Excerpt:**
```
در pipeline `auth` یک ناسازگاری منطقی پیدا شد: در مستندات ارائه شده، هیچ اشاره‌ای به مکانیزم permission یا authorization در pipeline احراز هویت نشده است. تمام مسیرهای mutation (مانند تغییر رمز عبور، ثبت‌نام، لاگین) بدون بررسی سطح دسترسی (role-based یا resource-based) اجرا می‌شوند. این یک شکاف امنیتی جدی است.
```

### Step 2: طراحی مدل داده‌ای Role و Permission در database
**Status:** `not_done` (0%)
**Scope:** اضافه کردن مدل‌های Role و Permission به backend/app/database.py. این شامل ایجاد جدول roles (با فیلدهای id, name, description) و جدول permissions (با فیلدهای id, name, description) و جدول role_permissions (many-to-many) و user_roles (many-to-many) است. همچنین migration اسکریپت برای ایجاد این جداول. خارج از این مرحله: پیاده‌سازی logic مربوط به permission check.
**Excerpt:**
```
یک middleware یا decorator برای بررسی permission در endpoints اضافه کنید. مثلاً در backend/tests/test_auth.py و backend/app/database.py، قبل از هر mutation، سطح دسترسی کاربر را با token یا role چک کنید.
```

### Step 3: پیاده‌سازی decorator require_permission در backend
**Status:** `not_done` (0%)
**Scope:** ایجاد یک decorator به نام `require_permission` در فایل جدید backend/app/auth/permissions.py. این decorator باید یک permission name (string) دریافت کند و قبل از اجرای endpoint، بررسی کند که کاربر احراز هویت شده (از طریق token) دارای آن permission باشد. اگر کاربر permission نداشته باشد، خطای 403 Forbidden برگرداند. خارج از این مرحله: پیاده‌سازی role-based logic (فقط permission مستقیم).
**Excerpt:**
```
یک middleware یا decorator برای بررسی permission در endpoints اضافه کنید. مثلاً در backend/tests/test_auth.py و backend/app/database.py، قبل از هر mutation، سطح دسترسی کاربر را با token یا role چک کنید.
```

### Step 4: پیاده‌سازی تابع get_user_permissions از token
**Status:** `not_done` (0%)
**Scope:** اضافه کردن تابع `get_user_permissions(token: str) -> list[str]` در backend/app/auth/permissions.py. این تابع باید token را decode کند، کاربر را پیدا کند، نقش‌های کاربر را از جدول user_roles بیاورد، و سپس تمام permissions مرتبط با آن نقش‌ها را از role_permissions جمع‌آوری کند. خروجی لیستی از نام permissionها است. خارج از این مرحله: caching یا optimization.
**Excerpt:**
```
قبل از هر mutation، سطح دسترسی کاربر را با token یا role چک کنید.
```

### Step 5: اعمال decorator require_permission بر روی endpoint تغییر رمز عبور
**Status:** `not_done` (0%)
**Scope:** اضافه کردن decorator `@require_permission('change_password')` به endpoint تغییر رمز عبور (احتمالاً در backend/app/auth/routes.py). همچنین اطمینان از اینکه endpoint کاربر جاری را تغییر می‌دهد (self-service) یا اگر admin است، می‌تواند دیگران را تغییر دهد. خارج از این مرحله: سایر endpoints.
**Excerpt:**
```
تمام مسیرهای mutation (مانند تغییر رمز عبور، ثبت‌نام، لاگین) بدون بررسی سطح دسترسی (role-based یا resource-based) اجرا می‌شوند. این یک شکاف امنیتی جدی است.
```

### Step 6: اعمال decorator require_permission بر روی endpoint ثبت‌نام کاربر جدید
**Status:** `not_done` (0%)
**Scope:** اضافه کردن decorator `@require_permission('create_user')` به endpoint ثبت‌نام (register). این اطمینان می‌دهد که فقط کاربران با مجوز مناسب می‌توانند کاربر جدید ایجاد کنند. خارج از این مرحله: ثبت‌نام عمومی (self-registration) که نیاز به permission ندارد.
**Excerpt:**
```
تمام مسیرهای mutation (مانند تغییر رمز عبور، ثبت‌نام، لاگین) بدون بررسی سطح دسترسی (role-based یا resource-based) اجرا می‌شوند.
```

### Step 7: بررسی endpoint لاگین و اعمال permission در صورت نیاز
**Status:** `done` (100%)
**Scope:** بررسی endpoint لاگین (login) در backend/app/auth/routes.py. لاگین معمولاً نیاز به permission ندارد (چون کاربر هنوز احراز هویت نشده). اما اگر endpoint لاگین شامل عملیات‌های حساس (مانند لاگین به عنوان کاربر دیگر) است، decorator اضافه شود. خارج از این مرحله: تغییرات غیرضروری.
**Excerpt:**
```
تمام مسیرهای mutation (مانند تغییر رمز عبور، ثبت‌نام، لاگین) بدون بررسی سطح دسترسی (role-based یا resource-based) اجرا می‌شوند.
```

### Step 8: اضافه کردن seed data برای roles و permissions در migration
**Status:** `not_done` (0%)
**Scope:** ایجاد یک migration اسکریپت که roles پیش‌فرض (مثلاً 'admin', 'user') و permissions پایه (مثلاً 'change_password', 'create_user', 'view_users') را در دیتابیس seed کند. همچنین تخصیص permissions به roles. خارج از این مرحله: API برای مدیریت roles.
**Excerpt:**
```
یک middleware یا decorator برای بررسی permission در endpoints اضافه کنید. مثلاً در backend/tests/test_auth.py و backend/app/database.py، قبل از هر mutation، سطح دسترسی کاربر را با token یا role چک کنید.
```

### Step 9: نوشتن unit tests برای decorator require_permission
**Status:** `not_done` (0%)
**Scope:** اضافه کردن تست‌ها به backend/tests/test_auth.py برای decorator require_permission. تست‌ها باید شامل: 1) کاربر با permission مجاز می‌تواند endpoint را فراخوانی کند، 2) کاربر بدون permission 403 دریافت می‌کند، 3) کاربر بدون توکن 401 دریافت می‌کند. خارج از این مرحله: integration tests.
**Excerpt:**
```
یک middleware یا decorator برای بررسی permission در endpoints اضافه کنید. مثلاً در backend/tests/test_auth.py و backend/app/database.py، قبل از هر mutation، سطح دسترسی کاربر را با token یا role چک کنید.
```

### Step 10: نوشتن integration tests برای permission در endpoints auth
**Status:** `not_done` (0%)
**Scope:** اضافه کردن تست‌های integration در backend/tests/test_auth.py که کل pipeline را تست می‌کنند: 1) ثبت‌نام کاربر جدید با توکن admin، 2) تغییر رمز عبور توسط کاربر بدون permission (باید 403 بگیرد)، 3) تغییر رمز عبور توسط کاربر با permission. خارج از این مرحله: تست‌های UI.
**Excerpt:**
```
تمام مسیرهای mutation (مانند تغییر رمز عبور، ثبت‌نام، لاگین) بدون بررسی سطح دسترسی (role-based یا resource-based) اجرا می‌شوند. این یک شکاف امنیتی جدی است.
```

### Step 11: به‌روزرسانی مستندات API با permission requirements
**Status:** `not_done` (0%)
**Scope:** اضافه کردن بخش permission requirements به مستندات API (احتمالاً در docs/api.md یا فایل OpenAPI). برای هر endpoint، مشخص کنید که چه permission‌هایی لازم است. خارج از این مرحله: تغییر مستندات frontend.
**Excerpt:**
```
در مستندات ارائه شده، هیچ اشاره‌ای به مکانیزم permission یا authorization در pipeline احراز هویت نشده است.
```

### Step 12: بررسی و رفع coherence issues در pipeline auth
**Status:** `done` (100%)
**Scope:** بررسی کامل pipeline auth برای coherence issues (ناسازگاری‌های منطقی) که ممکن است در نتیجه refactor ناتمام یا feature flag rot ایجاد شده باشد. این شامل بررسی اینکه آیا همه endpoints از یک مکانیزم permission استفاده می‌کنند یا خیر. خارج از این مرحله: تغییرات در سایر pipelines.
**Excerpt:**
```
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
```
