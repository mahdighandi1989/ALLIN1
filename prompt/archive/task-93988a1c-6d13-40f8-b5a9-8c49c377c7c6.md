---
task_id: 93988a1c-6d13-40f8-b5a9-8c49c377c7c6
title: پیاده‌سازی مدیریت خطای فایل‌های خراب یا خالی
type: logic_audit
priority: high
execution_priority: 2000
status: pending
external_status: pending
verification_status: pending
watched_id: b2586b68-22f8-4e8e-a7a8-9b513c5f70fe
project: mahdighandi1989/ALLIN1
created_at: '2026-05-17T16:07:03.161872+00:00'
updated_at: '2026-06-03T17:32:41.879687+00:00'
archived: true
archived_at: '2026-05-29T22:07:59.953149+00:00'
tags:
- merged
---

# پیاده‌سازی مدیریت خطای فایل‌های خراب یا خالی

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

هیچ اشاره‌ای به handling حالت‌های خطا مانند فایل خراب (corrupt)، خالی (empty)، یا فرمت نادرست نشده است. اگر فایل‌های Excel باز نشوند یا داده‌ای نداشته باشند، pipeline ممکن است crash کند یا نتایج نادرست بدهد.

## 💥 پیامد (impact)
در صورت وجود فایل خراب یا خالی، pipeline متوقف می‌شود (یا داده‌های ناقص تولید می‌کند) و نیاز به مداخله دستی دارد. این باعث کاهش reliability و افزایش زمان debug می‌شود.

## 🛠 پیشنهاد رفع اولیه
یک مرحله بررسی اولیه اضافه کنید: بررسی وجود فایل، فرمت معتبر، و وجود حداقل یک شیت با داده. در صورت خطا، یک log مناسب ثبت کرده و pipeline را با خطای مشخص متوقف کنید یا از یک fallback استفاده کنید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم مدیریت خطا برای فایل‌های خراب یا خالی

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline data است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

هیچ اشاره‌ای به handling حالت‌های خطا مانند فایل خراب (corrupt)، خالی (empty)، یا فرمت نادرست نشده است. اگر فایل‌های Excel باز نشوند یا داده‌ای نداشته باشند، pipeline ممکن است crash کند یا نتایج نادرست بدهد.

## 💥 پیامد (impact)
در صورت وجود فایل خراب یا خالی، pipeline متوقف می‌شود (یا داده‌های ناقص تولید می‌کند) و نیاز به مداخله دستی دارد. این باعث کاهش reliability و افزایش زمان debug می‌شود.

## 🛠 پیشنهاد رفع اولیه
یک مرحله بررسی اولیه اضافه کنید: بررسی وجود فایل، فرمت معتبر، و وجود حداقل یک شیت با داده. در صورت خطا، یک log مناسب ثبت کرده و pipeline را با خطای مشخص متوقف کنید یا از یک fallback استفاده کنید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `data` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `data` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: static)_

## Task Steps

### Step 1: بررسی و شناسایی فایل‌های مرتبط با pipeline داده و وضعیت فعلی مدیریت خطا
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجو و شناسایی تمام فایل‌های مرتبط با pipeline داده در پروژه (با توجه به stack FastAPI و Next.js) است. باید مشخص شود که کدام فایل‌ها مسئول خواندن، پردازش و اعتبارسنجی فایل‌های Excel هستند. همچنین باید بررسی شود که آیا هیچ‌گونه مدیریت خطا برای فایل‌های خراب، خالی یا با فرمت نادرست از قبل وجود دارد یا خیر. این مرحله صرفاً یک مرحله کشف و مستندسازی است و هیچ تغییری در کد ایجاد نمی‌کند. خروجی این مرحله یک لیست دقیق از فایل‌ها، توابع و مسیرهای مرتبط با pipeline داده و وضعیت فعلی مدیریت خطا در آن‌ها خواهد بود.
**Excerpt:**
```
در pipeline `data` یک ناسازگاری منطقی پیدا شد: هیچ اشاره‌ای به handling حالت‌های خطا مانند فایل خراب (corrupt)، خالی (empty)، یا فرمت نادرست نشده است. اگر فایل‌های Excel باز نشوند یا داده‌ای نداشته باشند، pipeline ممکن است crash کند یا نتایج نادرست بدهد. ... موقعیت دقیق در پروژه: (فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد) ... این مورد در pipeline data است — همه فایل‌های این pipeline مرتبط هستند.
```

### Step 2: اضافه کردن تابع بررسی وجود فایل و فرمت معتبر در ابتدای pipeline داده
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل ایجاد یک تابع جدید (مثلاً `validate_file_input`) در فایل مناسب از pipeline داده است. این تابع باید قبل از هرگونه پردازش، وجود فایل در مسیر مشخص شده را بررسی کند. همچنین باید فرمت فایل را با استفاده از کتابخانه‌ای مانند `openpyxl` یا `pandas` اعتبارسنجی کند تا مطمئن شود فایل Excel واقعاً یک فایل Excel معتبر است و خراب نیست. در صورت خراب بودن یا وجود نداشتن فایل، تابع باید یک استثنا (exception) مناسب پرتاب کند یا یک مقدار خطا برگرداند. این مرحله فقط بر روی بررسی اولیه فایل تمرکز دارد و شامل بررسی محتوای شیت‌ها یا داده‌ها نمی‌شود.
**Excerpt:**
```
یک مرحله بررسی اولیه اضافه کنید: بررسی وجود فایل، فرمت معتبر، و وجود حداقل یک شیت با داده. در صورت خطا، یک log مناسب ثبت کرده و pipeline را با خطای مشخص متوقف کنید یا از یک fallback استفاده کنید.
```

### Step 3: اضافه کردن بررسی وجود حداقل یک شیت با داده در فایل Excel
**Status:** `not_done` (0%)
**Scope:** این مرحله بر روی تابع ایجاد شده در مرحله قبل (`validate_file_input`) یا یک تابع جدید (`validate_sheet_content`) بنا می‌شود. پس از تأیید معتبر بودن فرمت فایل، این تابع باید فایل Excel را باز کرده و بررسی کند که آیا حداقل یک شیت (worksheet) در آن وجود دارد یا خیر. سپس باید بررسی کند که آن شیت (یا شیت‌ها) حاوی حداقل یک ردیف داده (غیر از هدر) هستند. اگر فایل خالی باشد (بدون شیت یا بدون داده)، تابع باید یک استثنا پرتاب کند. این مرحله شامل لاگ کردن خطا یا fallback نیست.
**Excerpt:**
```
یک مرحله بررسی اولیه اضافه کنید: بررسی وجود فایل، فرمت معتبر، و وجود حداقل یک شیت با داده. در صورت خطا، یک log مناسب ثبت کرده و pipeline را با خطای مشخص متوقف کنید یا از یک fallback استفاده کنید.
```

### Step 4: اضافه کردن لاگینگ مناسب برای خطاهای مربوط به فایل در pipeline داده
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل افزودن دستورات لاگینگ (با استفاده از کتابخانه لاگینگ موجود در پروژه، مانند `logging` یا `structlog`) به توابع ایجاد شده در مراحل 2 و 3 است. برای هر نوع خطا (فایل وجود ندارد، فرمت نامعتبر، فایل خالی)، یک پیام لاگ با سطح مناسب (مثلاً `logging.ERROR`) و جزئیات کافی (مانند مسیر فایل، نوع خطا) ثبت شود. این مرحله فقط بر روی لاگینگ تمرکز دارد و شامل تغییر رفتار pipeline (متوقف کردن یا ادامه دادن) نمی‌شود.
**Excerpt:**
```
در صورت خطا، یک log مناسب ثبت کرده و pipeline را با خطای مشخص متوقف کنید یا از یک fallback استفاده کنید.
```

### Step 5: توقف pipeline با خطای مشخص در صورت بروز خطای فایل (بدون fallback)
**Status:** `not_done` (0%)
**Scope:** این مرحله رفتار پیش‌فرض pipeline را در صورت بروز خطاهای فایل (مراحل 2 و 3) تعیین می‌کند. در این مرحله، تصمیم گرفته می‌شود که pipeline باید با یک خطای مشخص (مثلاً `FileValidationError` سفارشی) متوقف شود و به هیچ وجه به پردازش ادامه ندهد. این کار با پرتاب کردن یک استثنا (exception) از توابع اعتبارسنجی انجام می‌شود که توسط فراخوان بالادست (caller) pipeline گرفته می‌شود. این مرحله شامل پیاده‌سازی fallback نیست.
**Excerpt:**
```
در صورت خطا، یک log مناسب ثبت کرده و pipeline را با خطای مشخص متوقف کنید یا از یک fallback استفاده کنید.
```

### Step 6: نوشتن تست‌های واحد (unit tests) برای توابع اعتبارسنجی فایل
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای توابع ایجاد شده در مراحل 2 و 3 (`validate_file_input` و `validate_sheet_content`) است. تست‌ها باید سناریوهای زیر را پوشش دهند: فایل وجود ندارد، فایل با فرمت نامعتبر، فایل Excel معتبر اما خالی (بدون شیت)، فایل Excel با شیت خالی، و فایل Excel معتبر با داده. تست‌ها باید بررسی کنند که توابع در سناریوهای خطا، استثناهای مناسب پرتاب می‌کنند و در سناریوی موفق، بدون خطا عبور می‌کنند. این تست‌ها باید با استفاده از چارچوب تست موجود در پروژه (مانند pytest) نوشته شوند.
**Excerpt:**
```
این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند. ... ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
```

### Step 7: نوشتن تست‌های یکپارچه‌سازی (integration tests) برای pipeline داده با فایل‌های خراب/خالی
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های یکپارچه‌سازی است که کل pipeline داده را با فایل‌های خراب، خالی و نامعتبر فراخوانی می‌کنند. هدف این تست‌ها بررسی این است که pipeline به عنوان یک کل، در مواجهه با این خطاها، به درستی متوقف می‌شود و خطای مناسب را ثبت می‌کند. این تست‌ها باید از توابع سطح بالای pipeline استفاده کنند و نه توابع اعتبارسنجی به صورت مجزا. این تست‌ها باید بررسی کنند که pipeline با خطا مواجه می‌شود و لاگ‌های مناسب تولید می‌شوند.
**Excerpt:**
```
در صورت وجود فایل خراب یا خالی، pipeline متوقف می‌شود (یا داده‌های ناقص تولید می‌کند) و نیاز به مداخله دستی دارد. ... ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
```

### Step 8: بررسی نهایی و مستندسازی تغییرات در مستندات پروژه
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل یک بررسی نهایی (code review) از تمام تغییرات ایجاد شده در مراحل قبل است. اطمینان حاصل می‌شود که کد تمیز، خوانا و مطابق با استانداردهای پروژه است. همچنین، مستندات مرتبط با pipeline داده (مانند README یا فایل‌های مستندات داخلی) به‌روزرسانی می‌شوند تا رفتار جدید pipeline در مواجهه با خطاهای فایل را توضیح دهند. این مرحله شامل ایجاد یک Pull Request (PR) با توضیحات کامل از تغییرات و لیست کامیت‌ها است.
**Excerpt:**
```
در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.
```
