---
task_id: 4e7f00bc-6e88-4874-8050-e61460ea0450
title: صفحه Dashboard در build استاتیک به دلیل خطای 500 در endpoint /api/stats/dashboard به طور کامل fail می‌شود
type: bug
priority: critical
execution_priority: 100
status: done
external_status: pending
verification_status: done
watched_id: b2586b68-22f8-4e8e-a7a8-9b513c5f70fe
project: mahdighandi1989/ALLIN1
created_at: '2026-05-17T16:02:07.310144+00:00'
updated_at: '2026-06-03T17:31:15.601016+00:00'
archived: true
archived_at: '2026-05-18T09:57:26.854318+00:00'
target_files:
- frontend/src/app/dashboard/page.tsx
---

# صفحه Dashboard در build استاتیک به دلیل خطای 500 در endpoint /api/stats/dashboard به طور کامل fail می‌شود

## Raw Idea

صفحه Dashboard (frontend/src/app/dashboard/page.tsx) در حالت build استاتیک (frontend/out/dashboard/index.html) فقط یک اسپینر بی‌نهایت نشان می‌دهد و هرگز داده‌ها را load نمی‌کند. دلیل: کامپوننت از fetch('/api/stats/dashboard') استفاده می‌کند که در زمان build استاتیک (next export) اجرا نمی‌شود. همچنین endpoint بک‌اند /api/stats/dashboard در فایل‌های ارائه شده وجود ندارد و احتمالاً ستون amount در جدول facilities در دیتابیس موجود نیست (طبق docs/DATABASE_SCHEMA.md). نتیجه: کاربر هرگز dashboard را نمی‌بیند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
صفحه Dashboard در build استاتیک به دلیل خطای 500 در endpoint /api/stats/dashboard به طور کامل fail می‌شود

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/dashboard/page.tsx:30-61` — `fetchDashboardData` — این fetch در build استاتیک کار نمی‌کند و endpoint بک‌اند موجود نیست
  ```tsx
  const fetchDashboardData = async () => {
      setLoading(true);
      setError(null);
      try {
        const response = await fetch('/api/stats/dashboard');
        if (!response.ok) {
          throw new Error(`HTTP error! status: ${response.status}`);
        }
        const data = await response.json();
        setStats(data);
      } catch (err: any) { ... }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Next.js 14 App Router (static export) + FastAPI + SQLAlchemy

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/routers/stats.py` (سطر 1) — این فایل باید endpoint /api/stats/dashboard را پیاده‌سازی کند
- `backend/app/models/facility.py` (سطر 1) — مدل Facility باید ستون amount داشته باشد
- `docs/DATABASE_SCHEMA.md` (سطر 1) — مستندات به وجود ستون amount اشاره دارد

## 🌐 نقشهٔ وابستگی‌ها
صفحه Dashboard به endpoint بک‌اند و مدل Facility وابسته است. عدم وجود هر کدام باعث خطای 500 می‌شود.

## 🔍 Context و وضعیت فعلی
صفحه Dashboard (frontend/src/app/dashboard/page.tsx) در حالت build استاتیک (frontend/out/dashboard/index.html) فقط یک اسپینر بی‌نهایت نشان می‌دهد و هرگز داده‌ها را load نمی‌کند. دلیل: کامپوننت از fetch('/api/stats/dashboard') استفاده می‌کند که در زمان build استاتیک (next export) اجرا نمی‌شود. همچنین endpoint بک‌اند /api/stats/dashboard در فایل‌های ارائه شده وجود ندارد و احتمالاً ستون amount در جدول facilities در دیتابیس موجود نیست (طبق docs/DATABASE_SCHEMA.md). نتیجه: کاربر هرگز dashboard را نمی‌بیند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] صفحه Dashboard داده‌ها را نمایش دهد (نه اسپینر بی‌نهایت)
- [ ] endpoint /api/stats/dashboard در بک‌اند موجود و کار کند
- [ ] ستون amount در جدول facilities وجود داشته باشد
- [ ] build استاتیک بدون خطا انجام شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. اضافه کردن getServerSideProps یا getStaticProps برای fetch داده در build time. 2. یا تغییر به client-side fetching با fallback UI. 3. اطمینان از وجود endpoint /api/stats/dashboard در backend/app/routers/stats.py. 4. اضافه کردن ستون amount به مدل Facility در backend/app/models/facility.py و اجرای migration.

## 💡 نمونه‌های قبل/بعد
**رفع fetch در build استاتیک**

_قبل:_
```
const response = await fetch('/api/stats/dashboard');
```

_بعد:_
```
// استفاده از ISR یا client-side fetching با fallback
// یا تغییر به dynamic rendering با 'use client' و useEffect
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `curl http://localhost:8000/api/stats/dashboard`
- `npm run build && npm run start`
- `pytest backend/tests/test_facilities.py -k amount`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر در مدل Facility نیاز به migration دیتابیس دارد

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: medium

## Acceptance Criteria

1. صفحه Dashboard داده‌ها را نمایش دهد (نه اسپینر بی‌نهایت) _(verify: ui_interaction)_
2. endpoint /api/stats/dashboard در بک‌اند موجود و کار کند _(verify: api_response)_
3. ستون amount در جدول facilities وجود داشته باشد _(verify: static)_
4. build استاتیک بدون خطا انجام شود _(verify: backend_test)_

## Task Steps

### Step 1: اضافه کردن ستون amount به مدل Facility در دیتابیس
**Status:** `done` (100%)
**Scope:** این مرحله شامل اضافه کردن فیلد amount (از نوع DECIMAL یا FLOAT) به مدل SQLAlchemy Facility در فایل backend/app/models/facility.py است. همچنین شامل ایجاد migration برای اعمال تغییر در دیتابیس واقعی می‌شود. خارج از این مرحله: تغییر endpointها، تغییر frontend، یا تغییر سایر مدل‌ها. نکته حیاتی: طبق docs/DATABASE_SCHEMA.md این ستون باید وجود داشته باشد اما ظاهراً وجود ندارد — باید دقیقاً نوع داده و محدودیت‌ها (مثلاً nullable=False, default=0) مطابق مستندات باشد.
**Excerpt:**
```
همچنین ستون amount در جدول facilities در دیتابیس موجود نیست (طبق docs/DATABASE_SCHEMA.md)
```

### Step 2: ایجاد endpoint /api/stats/dashboard در بک‌اند FastAPI
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد یک endpoint GET جدید در backend/app/routers/stats.py (یا فایل مناسب دیگر) با مسیر /api/stats/dashboard است. endpoint باید داده‌های آماری dashboard را از دیتابیس (با استفاده از مدل Facility و ستون amount) برگرداند. خروجی باید یک JSON شامل آمارهایی مانند total_facilities, total_amount, average_amount و غیره باشد. خارج از این مرحله: تغییر frontend، تست‌نویسی، یا تغییر سایر endpointها. نکته حیاتی: endpoint باید خطاهای دیتابیس را مدیریت کند و در صورت عدم وجود داده، خروجی خالی (نه خطا) برگرداند.
**Excerpt:**
```
endpoint بک‌اند /api/stats/dashboard در فایل‌های ارائه شده وجود ندارد ... صفحه Dashboard (frontend/src/app/dashboard/page.tsx) ... از fetch('/api/stats/dashboard') استفاده می‌کند
```

### Step 3: اصلاح کامپوننت Dashboard برای کار در build استاتیک (ISR/SSG)
**Status:** `done` (100%)
**Scope:** این مرحله شامل تغییر frontend/src/app/dashboard/page.tsx است تا در build استاتیک (next export) کار کند. راه‌حل: استفاده از getStaticProps یا generateStaticParams به همراه revalidate برای ISR (Incremental Static Regeneration) یا استفاده از client-side fetch با fallback مناسب. همچنین باید خطای 500 را مدیریت کند و در صورت عدم دسترسی به endpoint، پیام خطای مناسب نشان دهد. خارج از این مرحله: تغییر endpoint بک‌اند، تغییر سایر صفحات، یا اضافه کردن کتابخانه‌های جدید. نکته حیاتی: در build استاتیک، fetch در زمان build اجرا می‌شود — باید داده‌ها در build time دریافت و در HTML استاتیک embed شوند.
**Excerpt:**
```
صفحه Dashboard (frontend/src/app/dashboard/page.tsx) در حالت build استاتیک (frontend/out/dashboard/index.html) فقط یک اسپینر بی‌نهایت نشان می‌دهد و هرگز داده‌ها را load نمی‌کند. دلیل: کامپوننت از fetch('/api/stats/dashboard') استفاده می‌کند که در زمان build استاتیک (next export) اجرا نمی‌شود.
```

### Step 4: اضافه کردن fallback و error handling در کامپوننت Dashboard
**Status:** `done` (100%)
**Scope:** این مرحله شامل بهبود error handling در frontend/src/app/dashboard/page.tsx است. باید حالت‌های مختلف را مدیریت کند: loading (اسپینر)، error (پیام خطا با دکمه retry)، empty (داده‌ای وجود ندارد)، و success (نمایش داده‌ها). همچنین باید timeout برای fetch (مثلاً 10 ثانیه) اضافه کند. خارج از این مرحله: تغییر endpoint بک‌اند، تغییر استایل‌ها، یا اضافه کردن کتابخانه‌های جدید. نکته حیاتی: error handling باید هم خطاهای شبکه و هم خطاهای HTTP (مانند 500) را پوشش دهد.
**Excerpt:**
```
const fetchDashboardData = async () => { setLoading(true); setError(null); try { const response = await fetch('/api/stats/dashboard'); if (!response.ok) { throw new Error(`HTTP error! status: ${response.status}`); } const data = await response.json(); setStats(data); } catch (err: any) { ... }
```

### Step 5: نوشتن تست واحد برای endpoint /api/stats/dashboard
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای endpoint جدید /api/stats/dashboard در بک‌اند است. تست‌ها باید موارد زیر را پوشش دهند: 1) درخواست موفق با داده‌های موجود، 2) درخواست موفق بدون داده (خروجی خالی)، 3) خطای دیتابیس (مثلاً connection error). تست‌ها باید با pytest و TestClient FastAPI نوشته شوند. خارج از این مرحله: تست‌های frontend، تست‌های integration، یا تست‌های endpointهای دیگر. نکته حیاتی: تست‌ها باید از دیتابیس تست (SQLite in-memory) استفاده کنند.
**Excerpt:**
```
endpoint بک‌اند /api/stats/dashboard در فایل‌های ارائه شده وجود ندارد
```

### Step 6: نوشتن تست واحد برای کامپوننت Dashboard (حالت‌های مختلف)
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای frontend/src/app/dashboard/page.tsx با استفاده از Jest و React Testing Library است. تست‌ها باید حالت‌های مختلف را پوشش دهند: 1) نمایش اسپینر در حالت loading، 2) نمایش داده‌ها در حالت success، 3) نمایش پیام خطا در حالت error، 4) نمایش پیام خالی در صورت عدم وجود داده. خارج از این مرحله: تست‌های integration با بک‌اند، تست‌های E2E، یا تست‌های سایر صفحات. نکته حیاتی: fetch باید mock شود تا وابستگی به بک‌اند نداشته باشیم.
**Excerpt:**
```
صفحه Dashboard (frontend/src/app/dashboard/page.tsx) در حالت build استاتیک (frontend/out/dashboard/index.html) فقط یک اسپینر بی‌نهایت نشان می‌دهد
```

### Step 7: به‌روزرسانی مستندات DATABASE_SCHEMA.md با جزئیات ستون amount
**Status:** `done` (100%)
**Scope:** این مرحله شامل به‌روزرسانی docs/DATABASE_SCHEMA.md برای اضافه کردن جزئیات دقیق ستون amount در جدول facilities است. باید شامل: نام ستون، نوع داده (DECIMAL(10,2) یا مشابه)، محدودیت‌ها (NOT NULL, DEFAULT 0)، و توضیح کاربرد آن. خارج از این مرحله: تغییر سایر بخش‌های مستندات، تغییر کد، یا اضافه کردن مستندات جدید. نکته حیاتی: مستندات باید با پیاده‌سازی واقعی هماهنگ باشد.
**Excerpt:**
```
طبق docs/DATABASE_SCHEMA.md این ستون وجود دارد اما ظاهراً وجود ندارد
```
