---
task_id: ff65d7f3-d78a-4042-a4a6-c35b8abc028f
title: '[منطق] عدم اشاره به وابستگی به Excel application'
type: logic_audit
priority: medium
execution_priority: 3000
status: suggested
external_status: pending
verification_status: pending
watched_id: b2586b68-22f8-4e8e-a7a8-9b513c5f70fe
project: mahdighandi1989/ALLIN1
created_at: '2026-05-17T16:08:44.056026+00:00'
updated_at: '2026-05-20T04:28:15.270147+00:00'
---

# [منطق] عدم اشاره به وابستگی به Excel application

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

کامپوننت‌ها با 'Excel application' تعامل دارند، اما مشخص نیست که آیا pipeline به نصب Excel روی سرور نیاز دارد یا از کتابخانه‌های جایگزین (مثل openpyxl یا pandas) استفاده می‌کند. این یک وابستگی خارجی است که باید مستند شود.

## 💥 پیامد (impact)
اگر pipeline در محیطی بدون Excel اجرا شود (مثلاً Docker container لینوکسی)، با خطا مواجه می‌شود. همچنین استفاده از Excel automation ممکن است باعث مشکلات performance و stability شود.

## 🛠 پیشنهاد رفع اولیه
تصمیم بگیرید که از کتابخانه‌های Python (مثل openpyxl برای .xlsm و xlrd برای .xls) استفاده کنید تا وابستگی به Excel حذف شود. اگر Excel ضروری است (مثلاً برای اجرای macroها)، این وابستگی را به وضوح در مستندات و requirements ذکر کنید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم اشاره به وابستگی به Excel application

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline data است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

کامپوننت‌ها با 'Excel application' تعامل دارند، اما مشخص نیست که آیا pipeline به نصب Excel روی سرور نیاز دارد یا از کتابخانه‌های جایگزین (مثل openpyxl یا pandas) استفاده می‌کند. این یک وابستگی خارجی است که باید مستند شود.

## 💥 پیامد (impact)
اگر pipeline در محیطی بدون Excel اجرا شود (مثلاً Docker container لینوکسی)، با خطا مواجه می‌شود. همچنین استفاده از Excel automation ممکن است باعث مشکلات performance و stability شود.

## 🛠 پیشنهاد رفع اولیه
تصمیم بگیرید که از کتابخانه‌های Python (مثل openpyxl برای .xlsm و xlrd برای .xls) استفاده کنید تا وابستگی به Excel حذف شود. اگر Excel ضروری است (مثلاً برای اجرای macroها)، این وابستگی را به وضوح در مستندات و requirements ذکر کنید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `data` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: medium
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `data` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: static)_

## Task Steps

### Step 1: بررسی و شناسایی تمام نقاط تعامل با Excel در pipeline data
**Status:** `pending` (0%)
**Scope:** این مرحله شامل جستجوی کامل در repository برای یافتن تمام فایل‌ها، ماژول‌ها و کامپوننت‌هایی است که با Excel application تعامل دارند. باید از ابزارهایی مانند grep برای جستجوی کلماتی مانند 'Excel', 'xlrd', 'openpyxl', 'pandas.read_excel', 'win32com', 'xlwings' و هر کتابخانه یا API مرتبط دیگر استفاده شود. همچنین باید فایل‌های requirements.txt، pyproject.toml یا setup.py برای شناسایی وابستگی‌های موجود بررسی شوند. خروجی این مرحله یک لیست جامع از تمام نقاط تماس با Excel است. خارج از این مرحله، تصمیم‌گیری در مورد جایگزینی یا مستندسازی وابستگی‌ها است.
**Excerpt:**
```
در pipeline `data` یک ناسازگاری منطقی پیدا شد: کامپوننت‌ها با 'Excel application' تعامل دارند، اما مشخص نیست که آیا pipeline به نصب Excel روی سرور نیاز دارد یا از کتابخانه‌های جایگزین (مثل openpyxl یا pandas) استفاده می‌کند. این یک وابستگی خارجی است که باید مستند شود.
```

### Step 2: تحلیل وابستگی‌های موجود در فایل‌های پیکربندی پروژه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل بررسی دقیق فایل‌های requirements.txt، pyproject.toml، setup.py یا هر فایل پیکربندی دیگری است که وابستگی‌های Python را مشخص می‌کند. باید مشخص شود که آیا کتابخانه‌هایی مانند openpyxl، xlrd، pandas (با قابلیت خواندن Excel) یا win32com در وابستگی‌ها وجود دارند یا خیر. همچنین باید بررسی شود که آیا وابستگی به خود Excel (از طریق COM یا ابزارهای مشابه) در مستندات یا کد ذکر شده است. خروجی این مرحله یک تحلیل از وضعیت فعلی وابستگی‌ها است.
**Excerpt:**
```
تصمیم بگیرید که از کتابخانه‌های Python (مثل openpyxl برای .xlsm و xlrd برای .xls) استفاده کنید تا وابستگی به Excel حذف شود. اگر Excel ضروری است (مثلاً برای اجرای macroها)، این وابستگی را به وضوح در مستندات و requirements ذکر کنید.
```

### Step 3: تصمیم‌گیری و پیاده‌سازی جایگزینی Excel automation با کتابخانه‌های Python (openpyxl/xlrd)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل جایگزینی تمام کدهایی است که از Excel application (از طریق COM automation، win32com، xlwings یا روش‌های مشابه) استفاده می‌کنند با کد معادل که از کتابخانه‌های خالص Python مانند openpyxl (برای فایل‌های .xlsm و .xlsx) و xlrd (برای فایل‌های .xls) استفاده می‌کند. این کار باید برای تمام نقاط تماس شناسایی شده در مرحله 1 انجام شود. اگر برخی عملیات (مانند اجرای macroها) بدون Excel امکان‌پذیر نیستند، باید در این مرحله شناسایی و برای مرحله بعدی (مستندسازی) علامت‌گذاری شوند. خارج از این مرحله، مستندسازی وابستگی‌های ضروری است.
**Excerpt:**
```
تصمیم بگیرید که از کتابخانه‌های Python (مثل openpyxl برای .xlsm و xlrd برای .xls) استفاده کنید تا وابستگی به Excel حذف شود.
```

### Step 4: مستندسازی وابستگی ضروری به Excel (در صورت وجود)
**Status:** `pending` (0%)
**Scope:** اگر در مرحله 3 مشخص شد که برخی عملیات (مانند اجرای macroهای VBA) بدون Excel امکان‌پذیر نیستند، این مرحله شامل مستندسازی این وابستگی به صورت واضح و کامل است. باید در فایل README.md یا یک فایل مستندات جداگانه (مانند DEPENDENCIES.md) ذکر شود که pipeline برای برخی عملیات خاص به نصب Excel روی سرور نیاز دارد. همچنین باید در فایل requirements.txt یا pyproject.toml یک وابستگی اختیاری (optional dependency) برای کتابخانه‌ای که با Excel تعامل دارد (مانند pywin32) اضافه شود. خارج از این مرحله، تغییر کد pipeline است.
**Excerpt:**
```
اگر Excel ضروری است (مثلاً برای اجرای macroها)، این وابستگی را به وضوح در مستندات و requirements ذکر کنید.
```

### Step 5: به‌روزرسانی فایل‌های وابستگی (requirements.txt / pyproject.toml) با کتابخانه‌های جدید
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن کتابخانه‌های openpyxl و xlrd به فایل‌های وابستگی پروژه (requirements.txt، pyproject.toml یا setup.py) است. اگر در مرحله 3 از کتابخانه‌های دیگری مانند pandas (با engine='openpyxl') استفاده شده، باید وابستگی مربوطه نیز اضافه شود. همچنین باید وابستگی‌های قدیمی مرتبط با Excel automation (مانند pywin32) در صورت عدم نیاز حذف شوند. خارج از این مرحله، نوشتن تست برای قابلیت جدید است.
**Excerpt:**
```
تصمیم بگیرید که از کتابخانه‌های Python (مثل openpyxl برای .xlsm و xlrd برای .xls) استفاده کنید تا وابستگی به Excel حذف شود.
```

### Step 6: نوشتن تست‌های واحد (unit tests) برای توابع جدید جایگزین Excel
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای تمام توابع جدیدی است که در مرحله 3 برای جایگزینی Excel automation ایجاد شده‌اند. تست‌ها باید شامل موارد زیر باشند: خواندن فایل‌های .xlsx، .xlsm و .xls با فرمت‌های مختلف، مدیریت خطاها (مانند فایل خراب یا فرمت ناشناخته)، و اطمینان از اینکه خروجی توابع جدید با خروجی توابع قدیمی (در صورت وجود) مطابقت دارد. تست‌ها باید با استفاده از pytest نوشته شوند و فایل‌های نمونه Excel برای تست در repository قرار گیرند. خارج از این مرحله، تست‌های یکپارچه‌سازی (integration tests) است.
**Excerpt:**
```
این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
```

### Step 7: نوشتن تست‌های یکپارچه‌سازی (integration tests) برای pipeline data با کتابخانه‌های جدید
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های یکپارچه‌سازی است که کل pipeline data را با استفاده از کتابخانه‌های جدید (openpyxl/xlrd) آزمایش می‌کند. تست‌ها باید شامل اجرای کامل pipeline با فایل‌های Excel نمونه و بررسی خروجی نهایی باشند. همچنین باید سناریوهای خطا مانند وجود فایل‌های خراب یا فرمت‌های پشتیبانی‌نشده را پوشش دهند. این تست‌ها باید در یک محیط شبیه‌سازی شده (مانند Docker container لینوکسی) اجرا شوند تا اطمینان حاصل شود که وابستگی به Excel حذف شده است. خارج از این مرحله، به‌روزرسانی مستندات کلی پروژه است.
**Excerpt:**
```
اگر pipeline در محیطی بدون Excel اجرا شود (مثلاً Docker container لینوکسی)، با خطا مواجه می‌شود.
```

### Step 8: به‌روزرسانی مستندات پروژه (README و هر مستند مرتبط دیگر)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل به‌روزرسانی تمام مستندات مرتبط پروژه (مانند README.md، CONTRIBUTING.md، و هر مستند معماری یا راهنمای توسعه) است تا تغییرات ایجاد شده را منعکس کند. باید ذکر شود که وابستگی به Excel حذف شده و از کتابخانه‌های openpyxl و xlrd استفاده می‌شود. همچنین باید نحوه نصب و استفاده از کتابخانه‌های جدید توضیح داده شود. اگر در مرحله 4 مستنداتی برای وابستگی ضروری به Excel ایجاد شده، باید به آن ارجاع داده شود. خارج از این مرحله، commit و push تغییرات است.
**Excerpt:**
```
این یک وابستگی خارجی است که باید مستند شود.
```

### Step 9: commit و push تمام تغییرات با پیام commit توصیفی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل commit کردن تمام تغییرات ایجاد شده در مراحل قبل (تغییرات کد، تست‌ها، مستندات و فایل‌های پیکربندی) با یک پیام commit توصیفی و دقیق است. پیام commit باید شامل توضیح مشکل (وابستگی به Excel)، راه حل (جایگزینی با openpyxl/xlrd)، و اشاره به issue مرتبط (در صورت وجود) باشد. سپس push به remote repository انجام می‌شود. خارج از این مرحله، ایجاد Pull Request و review است.
**Excerpt:**
```
در pipeline `data` یک ناسازگاری منطقی پیدا شد: کامپوننت‌ها با 'Excel application' تعامل دارند...
```
