---
task_id: 99232f72-3c02-476c-9500-31a81cdd51e1
title: Inspector Bridge Script در فایل‌های استاتیک فرانت‌اند تزریق شده است
type: bug
priority: high
execution_priority: 2000
status: suggested
external_status: pending
verification_status: pending
watched_id: b2586b68-22f8-4e8e-a7a8-9b513c5f70fe
project: mahdighandi1989/ALLIN1
created_at: '2026-05-17T16:04:24.772621+00:00'
updated_at: '2026-05-29T20:22:10.258410+00:00'
target_files:
- backend/static/dashboard/index.html
- backend/static/customers/index.html
- frontend/out/dashboard/index.html
---

# Inspector Bridge Script در فایل‌های استاتیک فرانت‌اند تزریق شده است

## Raw Idea

در تمام فایل‌های HTML استاتیک فرانت‌اند (backend/static/*/index.html و frontend/out/*/index.html) یک اسکریپت Inspector Bridge تزریق شده است که به یک WebSocket خارجی (wss://ai-creator-backend-q677.onrender.com) متصل می‌شود. این اسکریپت تمام رویدادهای کاربر (کلیک، اسکرول، تایپ، خطاهای جاوااسکریپت) را به یک سرور خارجی ارسال می‌کند. این یک نشت اطلاعات جدی است و حریم خصوصی کاربران را نقض می‌کند. همچنین این اسکریپت در فایل‌های استاتیک نسخه‌های مختلف (backend/static و frontend/out) با buildIdهای متفاوت (OhZSlB5nKg9L2-rYFH3j_ و WggLC3H8q930NO9G_QAs9) وجود دارد که نشان‌دهنده تزریق در زمان build یا بعد از آن است.
---
[scan #2 at 2026-05-17T16:04:24.803420+00:00]

---
[scan #3 at 2026-05-24T15:57:56.338565+00:00]
در تمام فایل‌های HTML خروجی (frontend/out/*/index.html و backend/static/*/index.html) یک اسکریپت Inspector Bridge با WebSocket هاردکد شده به آدرس 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_allin1' تزریق شده است. این اسکریپت تمام رویدادهای کاربر (کلیک، اسکرول،
---
[scan #4 at 2026-05-24T15:57:56.351516+00:00]
در فایل‌های HTML خروجی (backend/static و frontend/out) یک اسکریپت 'Inspector Bridge' به صورت خودکار تزریق شده است. این اسکریپت در تمام صفحات (index.html, dashboard/index.html, customers/index.html, facilities/index.html, login/index.html, 404/index.html) تکرار شده و حاوی کدهای ناقص و باگ است. در نسخ

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
Inspector Bridge Script در فایل‌های استاتیک فرانت‌اند تزریق شده است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/static/dashboard/index.html:2-323` — `Inspector Bridge Script` — اسکریپت کامل در تمام صفحات استاتیک تزریق شده است
  ```
  <!-- Inspector Bridge Script - Auto-injected -->
  <script>
  (function() {
    ...
    const WS_URL = 'wss://ai-creator-backend-q677.onrender.com/api/render/ws/bridge/gh_mahdighandi1989_allin1';
    ...
    window.addEventListener('click', function(e) {
      sendToInspector('click', {...});
    }, true);
    ...
    window.onerror = function(message, source, lineno, colno, error) {
      sendToInspector('error', {...});
    };
    ...
  })();
  </script>
  ```
- `backend/static/customers/index.html:2-323` — `Inspector Bridge Script` — در تمام صفحات استاتیک backend/static/*/index.html تکرار شده
  ```
  همان اسکریپت با محتوای یکسان
  ```
- `frontend/out/dashboard/index.html:2-323` — `Inspector Bridge Script` — در تمام صفحات استاتیک frontend/out/*/index.html نیز وجود دارد
  ```
  همان اسکریپت با buildId متفاوت (WggLC3H8q930NO9G_QAs9)
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Next.js 14 Static Export + FastAPI Static Files

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/app/layout.tsx` (سطر 12) — این فایل یک SafeClientWrapper دارد که سعی می‌کند خطاهای InspectorBridge را catch کند
- `frontend/src/app/InspectorBridge.tsx` (سطر 1) — فایل منبع احتمالی کامپوننت InspectorBridge
- `frontend/next.config.js` (سطر 1) — پیکربندی build که ممکن است اسکریپت را تزریق کند

## 🌐 نقشهٔ وابستگی‌ها
این اسکریپت در تمام صفحات استاتیک فرانت‌اند تزریق شده است و تمام رویدادهای کاربر را به یک سرور خارجی ارسال می‌کند.

## 🔍 Context و وضعیت فعلی
در تمام فایل‌های HTML استاتیک فرانت‌اند (backend/static/*/index.html و frontend/out/*/index.html) یک اسکریپت Inspector Bridge تزریق شده است که به یک WebSocket خارجی (wss://ai-creator-backend-q677.onrender.com) متصل می‌شود. این اسکریپت تمام رویدادهای کاربر (کلیک، اسکرول، تایپ، خطاهای جاوااسکریپت) را به یک سرور خارجی ارسال می‌کند. این یک نشت اطلاعات جدی است و حریم خصوصی کاربران را نقض می‌کند. همچنین این اسکریپت در فایل‌های استاتیک نسخه‌های مختلف (backend/static و frontend/out) با buildIdهای متفاوت (OhZSlB5nKg9L2-rYFH3j_ و WggLC3H8q930NO9G_QAs9) وجود دارد که نشان‌دهنده تزریق در زمان build یا بعد از آن است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] اعمال تغییر بدون شکستن تست‌های موجود
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. حذف کامل اسکریپت Inspector Bridge از تمام فایل‌های HTML استاتیک. این اسکریپت نباید در خروجی نهایی build فرانت‌اند وجود داشته باشد. باید فرآیند build را بررسی کرد تا مطمئن شویم این اسکریپت دوباره تزریق نمی‌شود.

## 💡 نمونه‌های قبل/بعد
**حذف اسکریپت**

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run build`
- `npm run lint`

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. اعمال تغییر بدون شکستن تست‌های موجود _(verify: backend_test)_
2. linter بدون warning عبور می‌کند _(verify: static)_
3. type-check موفق است _(verify: static)_

## Task Steps

### Step 1: شناسایی و فهرست‌بندی تمام فایل‌های HTML آلوده به اسکریپت Inspector Bridge
**Status:** `pending` (0%)
**Scope:** این مرحله شامل جستجوی کامل در دایرکتوری‌های backend/static/ و frontend/out/ برای یافتن تمام فایل‌های index.html است که حاوی اسکریپت Inspector Bridge با WebSocket خارجی (wss://ai-creator-backend-q677.onrender.com) هستند. همچنین باید buildIdهای مختلف (OhZSlB5nKg9L2-rYFH3j_ و WggLC3H8q930NO9G_QAs9) و هر buildId دیگری که ممکن است وجود داشته باشد شناسایی شود. خروجی این مرحله یک لیست کامل از مسیرهای فایل‌های آلوده است. این مرحله شامل حذف یا اصلاح فایل‌ها نمی‌شود.
**Excerpt:**
```
در تمام فایل‌های HTML استاتیک فرانت‌اند (backend/static/*/index.html و frontend/out/*/index.html) یک اسکریپت Inspector Bridge تزریق شده است که به یک WebSocket خارجی (wss://ai-creator-backend-q677.onrender.com) متصل می‌شود. ... همچنین این اسکریپت در فایل‌های استاتیک نسخه‌های مختلف (backend/static و frontend/out) با buildIdهای متفاوت (OhZSlB5nKg9L2-rYFH3j_ و WggLC3H8q930NO9G_QAs9) وجود دارد
```

### Step 2: حذف اسکریپت Inspector Bridge از تمام فایل‌های backend/static/*/index.html
**Status:** `pending` (0%)
**Scope:** این مرحله شامل باز کردن تمام فایل‌های index.html در دایرکتوری backend/static/ (مانند dashboard، customers و هر زیردایرکتوری دیگر) و حذف کامل بلوک اسکریپت Inspector Bridge از آن‌هاست. بلوک اسکریپت از <!-- Inspector Bridge Script - Auto-injected --> تا </script> که پس از آن قرار دارد باید به طور کامل حذف شود. این مرحله فقط backend/static را پوشش می‌دهد و frontend/out در مرحله بعدی اصلاح می‌شود.
**Excerpt:**
```
backend/static/dashboard/index.html:2-323 — Inspector Bridge Script — اسکریپت کامل در تمام صفحات استاتیک تزریق شده است ... backend/static/customers/index.html:2-323 — Inspector Bridge Script — در تمام صفحات استاتیک backend/static/*/index.html تکرار شده
```

### Step 3: حذف اسکریپت Inspector Bridge از تمام فایل‌های frontend/out/*/index.html
**Status:** `pending` (0%)
**Scope:** این مرحله شامل باز کردن تمام فایل‌های index.html در دایرکتوری frontend/out/ (مانند dashboard و هر زیردایرکتوری دیگر) و حذف کامل بلوک اسکریپت Inspector Bridge از آن‌هاست. بلوک اسکریپت مشابه مرحله قبل باید به طور کامل حذف شود. این مرحله فقط frontend/out را پوشش می‌دهد.
**Excerpt:**
```
frontend/out/dashboard/index.html:2-323 — Inspector Bridge Script — در تمام صفحات استاتیک frontend/out/*/index.html نیز وجود دارد ... همان اسکریپت با buildId متفاوت (WggLC3H8q930NO9G_QAs9)
```

### Step 4: بررسی و اصلاح فایل frontend/src/app/layout.tsx برای حذف ارجاع به InspectorBridge
**Status:** `pending` (0%)
**Scope:** این مرحله شامل بررسی فایل frontend/src/app/layout.tsx است که طبق cross-reference دارای SafeClientWrapper برای catch کردن خطاهای InspectorBridge است. باید مشخص شود که آیا این فایل هنوز حاوی ارجاع به InspectorBridge است یا خیر. اگر ارجاعی وجود دارد، باید حذف شود تا از بارگذاری مجدد اسکریپت جلوگیری شود. این مرحله شامل اصلاح منطق SafeClientWrapper برای عدم اشاره به InspectorBridge است.
**Excerpt:**
```
frontend/src/app/layout.tsx (سطر 12) — این فایل یک SafeClientWrapper دارد که سعی می‌کند خطاهای InspectorBridge را catch کند
```

### Step 5: بررسی build pipeline برای شناسایی منبع تزریق اسکریپت
**Status:** `pending` (0%)
**Scope:** این مرحله شامل بررسی فایل‌های build pipeline (مانند next.config.js، package.json scripts، فایل‌های CI/CD، و هر اسکریپت build سفارشی) برای یافتن منبع تزریق اسکریپت Inspector Bridge است. باید مشخص شود که آیا تزریق در زمان build (مثلاً از طریق یک پلاگین یا اسکریپت after-build) انجام شده یا بعد از build. این مرحله شامل اصلاح pipeline نیست، فقط شناسایی است.
**Excerpt:**
```
که نشان‌دهنده تزریق در زمان build یا بعد از آن است
```

### Step 6: بررسی dependencyها و پلاگین‌های npm برای یافتن package مخرب
**Status:** `pending` (0%)
**Scope:** این مرحله شامل بررسی فایل‌های package.json و package-lock.json (یا yarn.lock) در دایرکتوری frontend برای یافتن هرگونه dependency مشکوک که ممکن است اسکریپت Inspector Bridge را تزریق کرده باشد. همچنین باید پلاگین‌های webpack یا next.js که در build استفاده می‌شوند بررسی شوند. این مرحله شامل حذف dependencyها نیست، فقط شناسایی است.
**Excerpt:**
```
تزریق در زمان build یا بعد از آن
```

### Step 7: اضافه کردن هشدار امنیتی در README یا فایل مستندات
**Status:** `pending` (0%)
**Scope:** این مرحله شامل به‌روزرسانی فایل README.md یا اضافه کردن یک فایل SECURITY.md است که جزئیات حادثه تزریق اسکریپت Inspector Bridge را توضیح می‌دهد. این مستند باید شامل تاریخ وقوع، دامنه تأثیر (backend/static و frontend/out)، اقدامات انجام شده برای رفع مشکل، و توصیه‌هایی برای جلوگیری از وقوع مجدد باشد. این مرحله شامل تغییر کد نمی‌شود.
**Excerpt:**
```
این یک نشت اطلاعات جدی است و حریم خصوصی کاربران را نقض می‌کند
```

### Step 8: تست نهایی و تأیید عدم وجود اسکریپت در تمام فایل‌های استاتیک
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اجرای یک اسکریپت خودکار یا دستورات دستی برای تأیید نهایی است که هیچ فایل HTML در دایرکتوری‌های backend/static/ و frontend/out/ حاوی اسکریپت Inspector Bridge نیست. همچنین باید تأیید شود که فایل layout.tsx دیگر ارجاعی به InspectorBridge ندارد. این مرحله شامل commit و push تغییرات نیز می‌شود.
**Excerpt:**
```
در تمام فایل‌های HTML استاتیک فرانت‌اند (backend/static/*/index.html و frontend/out/*/index.html) یک اسکریپت Inspector Bridge تزریق شده است
```
