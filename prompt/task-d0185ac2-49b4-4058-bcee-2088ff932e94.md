---
task_id: d0185ac2-49b4-4058-bcee-2088ff932e94
title: '[Effectiveness] عدم نمایش معیارهای عملکردی (performance metrics) در outcome data'
type: logic_audit
priority: medium
execution_priority: 3000
status: suggested
external_status: pending
verification_status: pending
watched_id: b2586b68-22f8-4e8e-a7a8-9b513c5f70fe
project: mahdighandi1989/ALLIN1
created_at: '2026-05-24T15:59:23.748444+00:00'
updated_at: '2026-05-29T20:32:38.711233+00:00'
---

# [Effectiveness] عدم نمایش معیارهای عملکردی (performance metrics) در outcome data

## Raw Idea

## 🎯 هدف مطلوب (outcome target)
پس از افزودن metrics، زمان پاسخگویی API باید زیر ۲۰۰ms برای ۹۵٪ درخواست‌ها باقی بماند

## 📊 وضعیت فعلی
outcome data فقط error/warn/info را نشان می‌دهد و هیچ metricی برای latency, throughput, یا database connection pool usage وجود ندارد

## 🛠 اقدام پیشنهادی
افزودن middleware برای ثبت metrics درخواست‌ها (با استفاده از Prometheus client) و نمایش latency percentiles در outcome data

## ⚙️ ماهیت این finding
این یک effectiveness issue است — کد ممکن است syntactically کار کند ولی **outcome مطلوب** (مثل: «فرم باید ایمیل ارسال کند») حاصل نمی‌شود. verify باید outcome را اندازه بگیرد، نه فقط وجود فایل/خط.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[Effectiveness] عدم نمایش معیارهای عملکردی (performance metrics) در outcome data

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🔍 Context و وضعیت فعلی
## 🎯 هدف مطلوب (outcome target)
پس از افزودن metrics، زمان پاسخگویی API باید زیر ۲۰۰ms برای ۹۵٪ درخواست‌ها باقی بماند

## 📊 وضعیت فعلی
outcome data فقط error/warn/info را نشان می‌دهد و هیچ metricی برای latency, throughput, یا database connection pool usage وجود ندارد

## 🛠 اقدام پیشنهادی
افزودن middleware برای ثبت metrics درخواست‌ها (با استفاده از Prometheus client) و نمایش latency percentiles در outcome data

## ⚙️ ماهیت این finding
این یک effectiveness issue است — کد ممکن است syntactically کار کند ولی **outcome مطلوب** (مثل: «فرم باید ایمیل ارسال کند») حاصل نمی‌شود. verify باید outcome را اندازه بگیرد، نه فقط وجود فایل/خط.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] outcome target به‌صورت measurable بازنویسی شد
- [ ] کد تغییر کرد تا outcome target محقق شود
- [ ] test E2E که outcome را اندازه می‌گیرد عبور می‌کند
- [ ] metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: outcome target را به‌صورت قابل اندازه‌گیری بازنویسی کن (مثلاً: «email send rate > 95% در ۱۰۰ تلاش»).
گام ۲: کد را تغییر بده تا outcome محقق شود.
گام ۳: یک end-to-end test که outcome را اندازه می‌گیرد بنویس.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest -k 'outcome or e2e'`

## ⚠️ ریسک‌ها و موارد احتیاط
بهبود outcome ممکن است latency یا cost را افزایش دهد — قبل/بعد metric ها را compare کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: medium
- تخمین زمان: medium

## Acceptance Criteria

1. outcome target به‌صورت measurable بازنویسی شد _(verify: manual_only)_
2. کد تغییر کرد تا outcome target محقق شود _(verify: static)_
3. test E2E که outcome را اندازه می‌گیرد عبور می‌کند _(verify: backend_test)_
4. metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد _(verify: api_response)_

## Task Steps

### Step 1: افزودن middleware ثبت metrics درخواست‌ها با Prometheus client
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک middleware جدید در FastAPI است که metrics مربوط به latency، throughput و database connection pool usage را ثبت کند. از کتابخانه prometheus_client برای ثبت metrics استفاده می‌شود. middleware باید برای هر درخواست metrics را جمع‌آوری کند و آن‌ها را در قالب Prometheus format در endpoint /metrics در دسترس قرار دهد. خارج از این مرحله: تغییر outcome data، افزودن latency percentiles، یا اصلاح کد موجود برای نمایش metrics در outcome data. نکته حیاتی: middleware باید non-blocking باشد و latency اضافی به درخواست‌ها اضافه نکند.
**Excerpt:**
```
افزودن middleware برای ثبت metrics درخواست‌ها (با استفاده از Prometheus client) و نمایش latency percentiles در outcome data
```

### Step 2: افزودن latency percentiles به outcome data
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اصلاح outcome data است تا latency percentiles (مانند p50, p95, p99) را نمایش دهد. باید از metrics جمع‌آوری شده توسط middleware مرحله قبل استفاده کند و آن‌ها را به outcome data اضافه کند. خارج از این مرحله: تغییر middleware یا نحوه جمع‌آوری metrics. نکته حیاتی: latency percentiles باید به صورت real-time یا near-real-time محاسبه شوند و در outcome data نمایش داده شوند.
**Excerpt:**
```
نمایش latency percentiles در outcome data
```

### Step 3: بازنویسی outcome target به صورت measurable
**Status:** `pending` (0%)
**Scope:** این مرحله شامل بازنویسی outcome target فعلی (که vague است) به یک هدف measurable و قابل اندازه‌گیری است. باید مشخص کند که چه metrics باید اندازه‌گیری شوند و threshold آن‌ها چیست. خارج از این مرحله: تغییر کد یا پیاده‌سازی metrics. نکته حیاتی: outcome target باید به صورت عددی و قابل اندازه‌گیری باشد، مانند 'زمان پاسخگویی API زیر 200ms برای 95% درخواست‌ها'.
**Excerpt:**
```
outcome target به‌صورت measurable بازنویسی شد
```

### Step 4: تغییر کد برای محقق شدن outcome target
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تغییر کد موجود (احتمالاً middleware یا handlerهای API) است تا outcome target measurable محقق شود. باید latency را کاهش دهد یا throughput را افزایش دهد تا به threshold مشخص شده برسد. خارج از این مرحله: افزودن metrics جدید یا تغییر outcome data. نکته حیاتی: تغییرات باید performance API را بهبود بخشد و outcome target را برآورده کند.
**Excerpt:**
```
کد تغییر کرد تا outcome target محقق شود
```

### Step 5: نوشتن تست E2E برای اندازه‌گیری outcome
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن یک تست End-to-End است که outcome target را اندازه‌گیری می‌کند. تست باید چندین درخواست به API ارسال کند و latency آن‌ها را اندازه‌گیری کند و بررسی کند که 95% آن‌ها زیر 200ms هستند. خارج از این مرحله: تست‌های unit یا integration. نکته حیاتی: تست باید قابل تکرار باشد و در CI/CD اجرا شود.
**Excerpt:**
```
test E2E که outcome را اندازه می‌گیرد عبور می‌کند
```

### Step 6: افزودن metric/log برای تشخیص outcome rate در production
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن یک metric یا log جدید است که در production قابل تشخیص باشد و outcome rate را نشان دهد. باید از metrics موجود (مانند latency percentiles) استفاده کند و آن‌ها را به صورت aggregated در dashboard یا log نمایش دهد. خارج از این مرحله: تغییر middleware یا outcome data. نکته حیاتی: metric/log باید در production قابل مشاهده باشد و outcome rate را به صورت real-time نشان دهد.
**Excerpt:**
```
metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد
```

### Step 7: اجرای تست‌های موجود و linter/type-check برای اطمینان از عدم شکست
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اجرای تمام تست‌های موجود (unit, integration, E2E) و linter و type-check برای اطمینان از عدم شکست آن‌ها است. باید از دستورات `npm run test` یا `pytest` برای تست‌ها و `tsc --noEmit` یا `mypy` برای type-check استفاده شود. خارج از این مرحله: نوشتن تست جدید. نکته حیاتی: هیچ تستی نباید fail شود و linter باید بدون warning عبور کند.
**Excerpt:**
```
هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
linter بدون warning عبور می‌کند
type-check موفق است (`tsc --noEmit` / `mypy`)
```
