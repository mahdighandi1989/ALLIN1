fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
alembic==1.13.1
asyncpg==0.29.0
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
# Pin bcrypt to a version compatible with passlib 1.7.4. bcrypt >= 4.1 removed
# the `__about__` module and hard-errors on >72-byte inputs during passlib's
# backend self-test, which breaks all password hashing.
bcrypt==4.0.1
python-dateutil==2.8.2
psycopg2-binary==2.9.9
python-dotenv==1.0.1
redis==5.0.1
celery==5.3.6
httpx==0.26.0
openpyxl==3.1.2
# xlrd reads the legacy binary .xls format (openpyxl only handles .xlsx/.xlsm).
# Pinned to 2.0.1 — the last release; 2.x intentionally supports .xls only.
xlrd==2.0.1
structlog==24.1.0
prometheus-client==0.20.0
reportlab==4.1.0
