// AUTH_DISABLED has been removed. Authentication is ALWAYS enforced on both the
// backend and the frontend. This constant is kept only as a hard "false" guard
// for backward compatibility and must never be set to true again.
export const AUTH_DISABLED = false
