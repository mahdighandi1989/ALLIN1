---
title: "AI extract-to-DB: attribute each fact to the right entity, dedup, staleness-guard, and log"
tags: ["ai", "llm", "data-integrity", "dedup", "audit", "multi-entity"]
topic_canonical: "ai-extract-to-db-attribute-dedup-log"
source:
  type: "claude-code-task"
  origin: "claude-code"
  imported_at: "2026-07-04T09:40:00Z"
created_at: "2026-07-04T09:40:00Z"
updated_at: "2026-08-21T00:00:00Z"
merged_from: []
---

# AI extract-to-DB — attribute, dedup, staleness-guard, log

## 🎯 چالش / Challenge

می‌خواهیم از یک سندِ آزاد (نامه، ایمیل، PDF) داده‌های مفید استخراج و در پایگاه‌دادهٔ
واقعی ذخیره کنیم. خطرها: (۱) سند چند موجودیت (چند مشتری) را نام می‌برد و باید هر
داده به موجودیتِ **درست** برود؛ (۲) نباید رکوردِ تکراری ساخت؛ (۳) اگر داده‌ای از قبل
هست ولی سند به‌روزتر است باید به‌روز شود، و اگر سند کهنه‌تر است نباید نسخهٔ جدید را
خراب کند؛ (۴) اگر موجودیت پروفایل ندارد باید ساخته شود؛ (۵) هر نوشتن باید ردیابی (audit)
شود.

## 💡 راه‌حل / Solution

الگوی **propose → attribute+classify (server) → review → apply (dedup+staleness+log)**:

1. **Propose:** مدل برای هر واقعیت یک آیتم می‌دهد با کلیدِ موجودیت (شناسه اگر در متن
   هست، وگرنه نام) + key + value. مدل فقط پیشنهاد می‌دهد.
2. **Attribute + classify (سرور، قطعی):** هر واقعیت را به یک موجودیت تخصیص بده با
   اولویتِ صریح‌به‌مبهم: **شناسهٔ ذکرشده → موجودیتِ اصلیِ سند → تطبیقِ بی‌ابهامِ نام**.
   اگر هیچ‌کدام (نامِ ناشناخته بدون شناسه) → **UNRESOLVED؛ حدس نزن**، به کاربر گزارش بده
   («برای ثبت، شناسه لازم است»). سپس در برابرِ رکوردِ فعلی طبقه‌بندی کن: add / update /
   skip-same / skip-stale (برای فیلدهای تاریخ، فقط اگر تاریخِ سند جدیدتر است update).
3. **Review:** کاربر هر نوشتنِ پیشنهادی را با موجودیتِ مقصد + add/update می‌بیند و تیک می‌زند.
4. **Apply:** فقط تیک‌خورده‌ها؛ dedup/staleness را **دوباره** چک کن (رکورد ممکن است بین
   stage و apply تغییر کرده باشد)، موجودیت/پروفایل را در صورت نبود بساز، و **هر نوشتن را
   audit کن با کلیدِ موجودیت** تا هم در لاگِ سراسری و هم در لاگِ همان موجودیت دیده شود.

## ⚠️ نکات حیاتی / Pitfalls

- **تصادمِ per-batch:** چند واقعیت برای یک موجودیتِ نو، همگی قبل از commit در حافظه‌اند؛
  اگر برای هر آیتم دوباره query/insert کنی، رکوردهای در-حافظه را نمی‌بینی و روی PK یکتا
  تصادم می‌کنی. یک **کشِ per-account/per-entity** در طولِ batch نگه دار (هم برای
  ensure_entity هم برای پروفایل).
- **هرگز حدسِ شناسه نزن:** ساختِ پروفایل نیازمندِ کلید (شماره‌حساب) است؛ نامِ تنها کافی
  نیست — به‌جای جعلِ کلید، به کاربر بگو شناسه لازم است.
- **گاردِ کهنگی برای تاریخ‌ها:** تاریخِ کهنه‌تر نباید جدیدتر را جایگزین کند (یک تابعِ
  «آیا new جدیدتر از old است؟» با parseِ تاریخ).
- **audit را بعد از commit بنویس** تا لاگِ شبح برای نوشتنِ ناموفق نماند؛ کلیدِ موجودیت
  روی audit همان چیزی است که آن را در تبِ لاگِ پروفایل ظاهر می‌کند.
- **تستِ HTTPِ end-to-end** برای این مسیر ضروری است (ساختِ پروفایل، dedup، staleness،
  audit) چون منطقِ حساس به وضعیتِ DB است و mockِ خالص کافی نیست.

## 🔁 چطور در جای دیگر اعمال کنیم / How to Apply Elsewhere

- هر جا LLM از محتوای آزاد داده‌ای برای نوشتن در DB استخراج می‌کند: همین چهار فاز.
- allow-listِ کلیدها را نگه دار (نگاشت به ستون‌های ساختاری) ولی بقیه را در یک blobِ
  آزاد بریز تا انعطاف بماند.
- برای موجودیتِ چندگانه، تخصیص را صریح‌به‌مبهم بچین و «unresolved» را یک خروجیِ درجه‌یک
  (نه خطا) در نظر بگیر.

## 🔗 References

- ALLIN1: `services/letter_db_extract.py` + `routers/letter_ai.py` (apply-db) — AUDIT_LOG 2026-07-04
- مرتبط: `ai-edit-suggestions-review-first-with-locate-guard`, `conservative-dedup-compare-all-columns`,
  `upsert-keys-must-cover-every-caller`, `startup-merge-must-be-fill-empty-only`
## Update 2026-07-10 — misclassification ≠ duplication؛ گاردِ نوعِ موجودیت لازم است

گزارشِ «دوبله ثبت شده» گاهی اصلاً duplication نیست: در استخراجِ سندِ واقعی، متنِ
«سپردهٔ FD underlien» به‌عنوانِ facility برگشت، به typeِ catch-all (`other`) نگاشت
شد و یک موجودیتِ خیالی ساخت — dedupِ per-type سالم بود و ایمپورتِ مجدد هم دوبله
نمی‌ساخت؛ مشکل **طبقه‌بندیِ غلطِ نوعِ موجودیت** بود. درس‌ها:

1. **هر جا نگاشتِ catch-all داری (`other`)، یک گاردِ متنیِ قطعی هم بگذار:**
   ورودیِ catch-all که متنش نشانه‌های موجودیتِ دیگری را دارد (deposit/سپرده/
   underlien/Total/کارمزد) باید skip و شمارش شود، نه persist. فقط catch-all را
   گارد کن — typeهای whitelisted هرگز.
2. **قاعده را در پرامپت هم صریح بنویس** («سپرده هرگز facility نیست») — پرامپت
   احتمالِ خطا را کم می‌کند، گاردِ سرور تضمینش می‌کند.
3. **برچسبِ نمایشیِ catch-all گمراه می‌کند:** `other → "Credit Facility"` باعث شد
   کاربر فکر کند رکوردِ مستقلی است. برچسب‌های نگاشت را برای همهٔ مقادیرِ enum کامل
   نگه دار تا snake_caseِ خام یا برچسبِ گمراه‌کننده چاپ نشود.
4. **متن‌های سندی که سندِ بعدی بازمصرف می‌کند** (فهرستِ وثایق/مدارک) را verbatim
   استخراج و با سیاستِ «آخرین سند برنده است» ذخیره کن — با fill-empty اشتباه
   نشود؛ این‌جا تازگی مهم‌تر از حفظِ قبلی است.

## Update 2026-07-14 — «ستون دارد» ≠ «استخراج می‌شود»: سه‌جا را با هم چک کن (v58)

وقتی کاربر می‌پرسد «فلان داده ثبت می‌شود؟»، جواب سه لایهٔ جدا دارد که باید
هم‌زمان بررسی شوند — هر کدام می‌تواند به‌تنهایی شکسته باشد:
1. **ستونِ مدل** — هست؟ (اینجا owner/insurance_no/متراژها ستون داشتند)
2. **اسکلت/قواعدِ پرامپتِ استخراج** — از مدل خواسته می‌شود؟ (همین‌ها در
   پرامپت غایب بودند ⇒ هرگز استخراج نمی‌شدند با اینکه ستون بود)
3. **نگاشتِ persist** — کلیدِ استخراج‌شده به ستون نوشته می‌شود؟
پس هر فیلدِ جدید باید سه‌جا اضافه شود + whitelist/مدلِ PydanticِAPI برای
ویرایشِ دستی. تستِ درست هم دو نیمه دارد: assert کلیدها در EXTRACTION_PROMPT
(لایهٔ ۲) و persist سرتاسری تا ردیفِ DB (لایه‌های ۱و۳).
نکتهٔ ارزان: وقتی فهرستِ فیلدها schema-driven است (مثل CustomerProfile که
پرامپت را از ستون‌ها می‌سازد)، لایهٔ ۲و۳ خودکارند — الگوی برتر برای جدول‌های
دیگر هم همین است.

## Update 2026-07-16 — دادهٔ «تاریخ‌دارِ تکرارشونده» ستونِ تک نمی‌خواهد، جدولِ رویداد می‌خواهد (v64)

اگر واقعیتی در طولِ زمان چند بار رخ می‌دهد (چند ارزیابیِ ملک، ترهین/ترهین
مجدد/مازاد، فک رهن، تمدیدِ بیمه)، مدل‌کردنش با ستونِ تک + fill-empty یعنی
«فقط اولین مقدارِ دیده‌شده می‌ماند» و بقیهٔ تاریخچه بی‌صدا گم می‌شود. الگوی
درست: جدولِ event (نوع/تاریخ/مبلغ/منبع) با append-with-dedup به کلیدِ
(type, date, amount)؛ ستون‌های تکِ قبلی «آخرین» مقدار را نگه دارند (سازگاری
با فرم‌های موجود). در پرامپتِ استخراج صریح بگو «همه را فهرست کن، نه فقط
آخری» — پیش‌فرضِ مدل خلاصه‌کردن است.

و درسِ فراخوانی: وقتی facts ی که به مدل می‌دهی دستی و فیلدبه‌فیلد ساخته
می‌شود، هر جدول/ستونِ تازه از قلم می‌افتد (املاک سال‌ها در facts دستیارِ
نامه غایب بودند). سریال‌سازیِ schema-driven (حلقه روی ستون‌های ORM، حذفِ
housekeeping) این کلاس از باگ را برای همیشه می‌بندد — ستونِ آینده خودکار
به مدل می‌رسد.

## Update 2026-07-18 (v85) — انتسابِ رکوردِ بدونِ شماره: شواهدِ یکتا یا هیچ

وقتی سندی شمارهٔ حساب ندارد، انتساب فقط با شاهدِ «یکتا و بدونِ تناقض»:
تطبیقِ نامِ فارسیِ نرمال‌شده (ي/ی، ك/ک، ة/ه، ZWNJ، اعراب — زیرمجموعگیِ
توکنی با ≥۲ توکن) با «دقیقاً یک» پروفایل، یا تک‌شمارهٔ حسابِ موجود در
«نامِ فایل» آن‌هم فقط برای تک‌رکورد. شمارهٔ ۶رقمیِ داخلِ متن هرگز به
تنهایی حساب نیست (سندِ رهنی/شمارهٔ مدرک همان شکل را دارد) — این قاعده
باید هم در پرامپت (فقط با contextِ حساب emit کن) و هم در سرور (اصلاً
scan نکن) اعمال شود. دو سیگنالِ متناقض (نامِ فایل ↔ نامِ مشتری) هر دو
را باطل می‌کند ⇒ گزارشِ بازبینی با نامزدها — حدس ممنوع (review-first).
و رکوردِ بدونِ حساب را در حلقهٔ ادغامِ chunkها با کلیدِ نام نگه دار؛
حذفِ بی‌صدا یعنی گم‌شدنِ دادهٔ مالک.

## Update 2026-07-18 (v85) — برداشتِ دانش در هر مسیرِ استخراج + dedupِ سراسری

هر مسیری که فایلِ کاربر را می‌خواند (ایمپورت، پیوستِ نامه، …) باید
همان قراردادِ kb_items را داشته باشد و از «یک» مسیرِ نوشتن
(kb_store.upsert_entry) بگذرد. برای جریان‌هایی که به مرورِ زمان فایلِ
تکراری می‌گیرند، dedupِ درون‌سرفصلی کافی نیست — مدل ممکن است همان محتوا
را دفعهٔ بعد زیرِ عنوانِ دیگری بگذارد؛ `global_dedupe=True` هویتِ محتوا
را بر هویتِ سرفصل مقدم می‌کند. شکستِ KB هرگز نباید خودِ ایمپورت را
شکست بدهد (best-effort).

## Update 2026-07-23 (v86) — هر «کار روی حساب» باید به لاگِ فعالیتِ همان حساب هم برسد

قراردادِ سیستم: زبانهٔ «لاگِ کارها»ی پروفایل فقط جدولِ audit را می‌خوانَد.
پس هر مسیری که کاری روی یک حساب انجام/ثبت می‌کند (لاگِ روزانه، ایمپورت،
نامه، …) علاوه بر جدولِ خودش باید یک record_audit با همان account_no
بزند وگرنه از دیدِ کاربر «ثبت نشده» است — نوشتن در journal/task کافی
نیست. موقعِ ساختنِ فیچرِ جدید این را جزوِ definition-of-done بگیر.

## Update 2026-07-29 (v88) — لحنِ خروجیِ مولد را با few-shot از آرشیوِ خودِ کاربر بساز

وقتی کاربر می‌گوید خروجیِ بازنویسیِ مدل «بی‌روح/بچه‌گانه» است، قاعدهٔ
سبکِ بیشتر در system کافی نیست — چند نمونهٔ «واقعی از آرشیوِ خودِ او»
را در پیام بگذار و یک قاعدهٔ صریحِ مقایسهٔ ذهنی («اگر جمله‌ات در آن
نامه‌ها نمی‌نشیند، بازنویسی‌اش کن») اضافه کن. سه احتیاطِ الزامی:
(۱) گاردِ «فقط سبک» — نمونه‌ها منبعِ واقعیت نیستند و هیچ نام/عدد/
تاریخی از آن‌ها نباید نشت کند؛ (۲) خودِ سندِ در حالِ ویرایش را با
امضای پیشوندِ متن از نمونه‌ها حذف کن وگرنه متنِ خراب، الگوی خودش
می‌شود؛ (۳) سقفِ تعداد/طول تا پرامپت باد نکند و نمونهٔ کوتاه/بی‌مایه
(زیرِ آستانهٔ طول) اصلاً انتخاب نشود.

## Update 2026-08-07 (v95) — رویدادِ چرخهٔ عمر (خروج/برگشت) را روی خودِ رکورد مهر کن، حذف نکن

برای «برگشت/استرداد»ِ یک رکوردِ عملیاتی (چکِ ضمانتی که پس داده می‌شود)،
الگوی درست: ستون‌های released/date/note روی همان رکورد + endpointِ
idempotentِ اختصاصی + ثبتِ audit ذیلِ همان حساب (درسِ v86) — نه حذفِ
رکورد و نه جدولِ موازی. حالتِ جدیدِ فرم (reversal) را با prop/branchِ
اختیاری بساز تا مسیرِ عادی بیت‌به‌بیت دست‌نخورده بماند و هارنس همین
دست‌نخوردگی را صریح assert کند.

## Update 2026-08-07 (v98) — «یافت نشد» را تریاژ کن: تک‌ناسازگاری = هشدارِ تایپی، دوناسازگاری = ایجاد

وقتی عملیاتی روی رکوردِ مرجعی می‌نشیند که پیدا نمی‌شود، خطای خامِ
not-found کاربر را بلاتکلیف می‌گذارد. الگوی درست: اجزای کلید را جدا
بسنج — اگر فقط «یکی» ناآشناست، به احتمالِ زیاد خطای تایپیِ کاربر است ⇒
هشدار بده و «مقادیرِ واقعیِ» طرفِ آشنا را در پیام بیاور تا خطا فوراً
دیده شود؛ فقط وقتی «همهٔ» اجزا ناآشنا هستند به‌عنوانِ موجودیتِ واقعاً
جدید بساز — آن‌هم با داده‌هایی که کاربر همان لحظه در فرم داده (نه حدس)،
و رویدادِ اصلی را در همان تراکنش ذیلش ثبت کن.

## Update 2026-08-07 (v99) — فرمِ سندِ مشتق: قاب مشترک، محتوا ورودی‌محور، مقادیرِ مرجع فقط پیش‌فرض

وقتی از روی workbookِ نمونه فرمِ جدیدی می‌سازی: (۱) هر عددی که در نمونه
ثابت به نظر می‌رسد (GL، کدِ تراکنش، عنوان) را «ورودی با پیش‌فرض» کن نه
ثابتِ کد — کاربر قبلاً دوبار از سرِ همین hardcodeها برگشت داده بود؛
(۲) عناصری از نمونه که کاربر صریحاً نخواسته (باکس‌ها/حروفِ اضافه) را
عامدانه حذف و در هارنس «غیبتشان» را assert کن؛ (۳) دادهٔ فرم باید از
همان مسیرِ استخراجِ اسنادِ موجود هم پرشدنی باشد — شِمای پرامپت + persistِ
fill-empty + ستون‌های DB را هم‌زمان با فرم اضافه کن تا فرم و استخراج از
روزِ اول یک رکورد را بخوانند/بنویسند.

## Update 2026-08-07 (v100) — «مثل فرم قبلی» یعنی همان کامپوننت، نه بازسازی ظاهر منبع

v99 برای سندِ IRR یک کامپوننتِ جدید ساخت که ظاهرِ workbookِ اکسل (باکسِ
خانه‌خانهٔ BRANCH/ACCOUNT/TRAN. CODE و جدولِ NARRATIVE) را کپی می‌کرد؛
مالک رد کرد: «گفتم شکل اسناد مثل همون اسناد قبلی باشه... فقط محتوا و
ورودی‌ها تغییر کنه». درسِ الزام‌آور: وقتی کاربر می‌گوید فرمِ جدید «مثل
فرمِ موجود» باشد، منظورش «همان کامپوننت/قابِ رندرِ موجودِ خودِ سیستم»
است — عکس‌های منبعی که می‌فرستد فقط «محتوا/فیلدها» را تعریف می‌کنند، نه
ظاهر را. راهِ درست: کامپوننتِ موجود را با propهای اختیاریِ کوچک تعمیم
بده (این‌جا فقط `amountText` برای نمایشِ verbatimِ «تعدادِ چک» به‌جای
مبلغِ فرمت‌شده) و محتوای متفاوت را در همان اسلات‌های موجود (A/c No با
همان الگویِ خط‌تیره‌ای، description، extraLines) بریز. کامپوننتِ ظاهرِ
منبع‌ساز قرنطینه شد (REMOVAL_CANDIDATES)، حذف نشد.

## Update 2026-08-07 (v101) — فرمِ پرورودی باید از استخراجِ قبلی suggest/auto-fill کند؛ پیوندها را در استخراج صریح بخواه

وقتی فرمی فیلدهای زیادی دارد که داده‌شان قبلاً با ایمپورت استخراج و در
DB نشسته، «datalist از رکوردهای همان حساب + auto-fill با انتخابِ رکوردِ
لنگری (شماره چک / مرجعِ تسهیلات)» را از روزِ اول بساز — مالک صریحاً
گفت تایپِ دستیِ داده‌ای که سیستم دارد نباید لازم باشد. پیش‌نیازش این
است که «پیوند» بینِ موجودیت‌ها در خودِ شِمای استخراج صریح باشد: چکِ
ضمانتی بدونِ `facility_ref` قابلِ‌اتصال به تسهیلاتش نیست و تسهیلاتِ
بدونِ `tenor_months` نمی‌تواند «مدت» را پیشنهاد بدهد. الگو: فیلدِ
پیوند/جزئیات را به شِمای پرامپت اضافه کن + persistِ fill-empty (مسیرِ
قبلی دست‌نخورده) + در فرانت یک `applyXDetails(ref)` که با هر دو کلید
(مرجعِ بانکی در name و idِ داخلی) match می‌کند. تستِ پیوند را روی
persist بنویس نه فقط پرامپت.

## Update 2026-08-07 (v102) — ورودی‌هایی که در یک حالتِ فرم بی‌معنا هستند را همان‌جا پنهان کن، نه با fallbackِ خاموش

وقتی حالتِ جدیدی به فرمی چندحالته اضافه می‌شود، هر ورودیِ به‌ارث‌رسیده
را با «معنای دامنه‌ایِ» آن حالت بسنج، نه فقط با کارکردِ فنی‌اش: انتخابِ
Borrower/Guarantor برای چکِ ریالی بی‌معناست چون صادرکننده هرگز
نمی‌تواند گیرندهٔ تسهیلاتِ اماراتی باشد — ماهیتِ سند خودش جواب را تعیین
می‌کند. الگو: (۱) ورودیِ بی‌معنا را «فقط در همان حالت» شرطی پنهان کن
(در حالت‌های دیگر دست‌نخورده — قانونِ ۲)؛ (۲) هر fallbackِ خاموش به
مقدارِ غلطِ دامنه‌ای (این‌جا نامِ صاحب‌حساب به‌جای صادرکننده) را به
اعتبارسنجیِ صریح با پیامِ توضیح‌دار تبدیل کن؛ (۳) در هارنس هم غیبت در
آن حالت و هم بازگشت در حالت‌های دیگر را assert کن.

## Update 2026-08-08 (v103) — دستورِ انسانی به مدلِ استخراج‌گر: بلوکِ نانس‌دار + گزارشِ اجباریِ اعمال + پاک‌شدنِ بعدِ مصرف

وقتی به پرامپتِ استخراج «دستورِ آزادِ کاربر» اضافه می‌کنی، چهار قاعده
الزامی است: (۱) **نشانگرِ بلوک باید نانسِ هر-اجرا داشته باشد** — نشانگرِ
ثابت را هم خودِ متنِ کاربر و هم سندِ مهاجم می‌توانند جعل کنند و بازبینِ
اجرایی این را با پروبِ واقعی اثبات کرد؛ پرامپت هم صریح بگوید ادعای
مشابه در جای دیگر معتبر نیست. (۲) **گزارشِ اعمال اجباری است، سکوت
ممنوع** — از مدل بخواه فیلدِ گزارش (فارسی، مشخص: کدام دستور، کجا، چه
تغییری) برگرداند؛ اگر برنگرداند، پاسخِ سرور باید هشدارِ صریحِ «گزارشی
نداد» بگذارد و مسیرِ بدونِ-مدل (پارسرِ قطعی) گزارشِ صادقانهٔ «قابلِ
اعمال نبود» بدهد — کاربر باید همیشه بتواند ارتباطِ دستور↔فایل را
راستی‌آزمایی کند. (۳) **دستور بعد از مصرفِ موفق باید خودش پاک شود**
(یافتهٔ major): متنِ سندمحور (مثل شماره حساب) اگر در باکس بماند، بی‌صدا
روی دستهٔ بعدیِ مشتریِ دیگر اعمال می‌شود — پاک‌سازی بعد از موفقیتِ
دسته، نه در addFiles (یادداشتِ بینِ دو فایلِ یک دسته نباید بپرد).
(۴) **جعبهٔ خالی = پرامپتِ بیت‌به‌بیتِ قبلی** را با تست قفل کن تا مسیرِ
موجود دست نخورد. و درسِ فرا-روشی: بازبین‌هایی که کد را «اجرا» می‌کنند
(تست/هارنس/پروبِ زنده) در یک راند چیزهایی پیدا کردند که خواندنِ صرفِ
کد نمی‌داد — این الگو را برای فیچرهای حساس تکرار کن.

## Update 2026-08-18 (v106) — کارِ پس‌زمینهٔ طولانی باید ری‌استارت را زنده بماند: ورودی را با job ذخیره کن، در بوت resume کن، تلاش را سقف بزن

هر background jobی که فقط در حافظهٔ پروسه زندگی می‌کند، روی پلتفرمی که
پروسه را می‌کُشد (OOMِ فایلِ سنگین، autodeployِ «هر مرج») یک بمبِ
ساعتی است — و علامتش گمراه‌کننده است: کاربر «خطای تحلیل» می‌بیند و
سراغِ مدل/پرامپت می‌گردد، در حالی که لاگِ سرور
(«==> Instance restarted» + «Marked N interrupted…») مقصرِ واقعی را
نشان می‌دهد؛ اول لاگِ زیرساخت را بخوان. الگوی درمان: (۱) ورودیِ خام +
پارامترهای اجرا را با خودِ سطرِ job در DB ذخیره کن (LargeBinary)؛
(۲) reconciliationِ بوت به‌جای error کردن، از همان بایت‌ها resume کند
— سطر running می‌ماند تا pollingِ کلاینت بی‌تغییر جواب بگیرد؛
(۳) **سقفِ تلاش الزامی است**: بدونِ آن، فایلی که خودش عاملِ OOM است
حلقهٔ restart→resume→OOM می‌سازد؛ در سقف، پیامِ صادقانه با راهِ عملی
بده («فایل را تقسیم کن»)، نه خطای عمومی؛ (۴) blob را بلافاصله بعدِ
اتمام پاک کن تا جدول باد نکند؛ (۵) مسیرِ قبلی (error برای سطرهای
بدونِ blob) را به‌عنوانِ fallbackِ سطرهای قدیمی نگه دار.

## Update 2026-08-20 (v107) — ماکِ کاربر = ground truth؛ اختلافِ معنایی با پیاده‌سازیِ قبلی را «ثبت و اعلام» کن، حدس نزن

وقتی مالک با اسکرین‌شاتِ ماک می‌گوید «شکل دقیقاً این بشود»، دو تعهدِ
هم‌زمان داری: (۱) عینِ ماک را پیاده کن — حتی جزئیاتی مثل فاصله به‌جای
خط‌تیره بعدِ شعبه، ترتیبِ DEBIT/CREDIT، و سلولِ جدای ارز؛ (۲) اگر
جایی از ماک با «معنای مالیِ» پیاده‌سازیِ قبلی فرق دارد (این‌جا: جفتِ
GL/کدِ تراکنشِ برگشتی با v95 برعکس بود)، در سکوت نه تصحیحش کن نه
کورکورانه رد شو — عینِ ماک را بساز، اختلاف را در AUDIT_LOG ثبت کن و
در پیامِ تحویل صریح به مالک بگو تا اگر ماک فقط illustrative بوده،
برگرداند؛ مسیرِ برگشت را هم یک‌قدمی نگه دار (swapِ props، نه بازنویسی).
و وقتی فرم چند حالت دارد، بازطراحیِ یک حالت باید کامپوننتِ جدیدِ همان
حالت باشد در همان قابِ مشترک — حالت‌های دیگر بیت‌به‌بیت دست‌نخورده
(هارنسِ رگرسیونِ همهٔ حالت‌ها همان لحظه اجرا شود، نه بعداً).

## Update 2026-08-20 (v109) — بازطراحیِ تأییدشدهٔ یک حالت را برای حالت‌های خواهر «با همان کامپوننت» تعمیم بده

وقتی مالک بازطراحیِ یک حالت (برگشتی) را دید و پسندید و گفت «برای آن دو
سندِ دیگر هم همین کار را بکن»، تعمیمِ درست بازاستفادهٔ همان کامپوننتِ
جدید با پارامترهای حالتِ مقصد است (این‌جا stamp خالی + عنوان/GLهای
خودِ حالتِ عادی) — نه کپیِ layout در کامپوننتی تازه. جاهایی که «عمداً»
باید فرق کند را صریح نگه دار (مهرِ برگشتی روی حالتِ عادی نمی‌رود؛
جفتِ GL هر حالت مالِ خودش می‌ماند) و specِ خروجی‌ها (Excel/Word) را در
همان قدم همگام کن تا چاپِ وب و فایل‌ها هم‌شکل بمانند — و چون spec
مشترک است، فقط شاخهٔ buildSlips همان حالت عوض می‌شود، رندرکننده‌ها
هیچ.

## Update 2026-08-21 (v110) — چک‌لیستِ سه‌لایه حالا پنج‌لایه است: ویرایشِ دستی و دیتاستِ مصرف‌کننده را هم بشمار

وقتی کاربر سربرگ‌های جدولِ هدفش را می‌دهد و می‌پرسد «این‌ها از قبل هست؟»،
پاسخِ درست ممیزیِ ستون‌به‌ستون است در «همهٔ» لایه‌ها — و v110 نشان داد
دو لایهٔ دیگر غیر از سه‌تای v58 (ستونِ مدل، پرامپتِ استخراج، persist)
هم می‌توانند به‌تنهایی شکسته باشند: (۴) **whitelist ویرایشِ دستیِ API**
— فیلدهای v58 سال‌ها ستون و استخراج داشتند ولی در _WRITABLE و مدل‌های
Write/Patch نبودند، یعنی کاربر نمی‌توانست اصلاحِ دستی کند؛ (۵) **دیتاستِ
مصرف‌کننده (need_data پیوست‌ساز)** — داده ذخیره می‌شد ولی عرضه نمی‌شد و
شرحِ کاتالوگ هم به مدل نمی‌گفت وجود دارد (فیلدی که در شرحِ کاتالوگ
نیامده عملاً نامرئی است). قاعدهٔ عملی: برای هر ستونِ جدولِ هدف، پنج‌جا
را با یک تستِ سرتاسری قفل کن: persist با همهٔ فیلدها + حضور در پرامپت +
خروجیِ fetch_datasets + شرحِ کاتالوگ + قابلِ‌نوشتن بودنِ دستی.
